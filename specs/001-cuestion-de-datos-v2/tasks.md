# Plan de Trabajo Ejecutable — Cuestión de Datos v2.0

**Versión:** 1.0.0 · **Fecha:** 2026-07-06
**Fuente:** [`plan.md`](./plan.md) §8 · Los IDs de requisitos (`RF-###`, `RNF-###`) vienen de [`spec.md`](./spec.md)

> **Cómo leer este archivo (para Camilo):** las tareas están en orden de ejecución. Cada una dice *qué* se hace, *por qué*, *qué necesitas hacer tú manualmente* (cuentas, claves, decisiones) y *cómo verificar que quedó bien* antes de pasar a la siguiente. Las marcadas 🧑 requieren acción humana tuya; las demás las puede ejecutar un agente de desarrollo. Marca `[x]` al completar. No saltes fases: cada una asume la anterior verificada.

---

## FASE 0 — Preparación de cuentas y herramientas externas

*Objetivo: tener todas las credenciales e infraestructura gratuita lista ANTES de escribir código, para que ninguna tarea posterior se bloquee esperando un registro.*

- [ ] **T-001 🧑 Preparar las bases de datos (local y despliegue).**
  *El desarrollo diario usa PostgreSQL LOCAL en Docker (Constitución Art. II.2); el servicio gestionado es solo para DESPLIEGUE y puede crearse más tarde (se necesita a partir de T-206/T-701).*
  1. **Local (requisito para desarrollar):** instala [Docker Desktop](https://www.docker.com/products/docker-desktop/). La base local se levantará con el `compose.yaml` que se crea en T-105 — aquí solo asegura que `docker compose version` funciona.
  2. **Despliegue (puede diferirse):** regístrate en [supabase.com](https://supabase.com) (o [neon.tech](https://neon.tech), equivalente) con tu cuenta de GitHub; crea el proyecto `cuestion-de-datos`; en el editor SQL ejecuta `CREATE EXTENSION IF NOT EXISTS vector; CREATE EXTENSION IF NOT EXISTS pg_trgm;`; guarda la cadena de conexión (`postgresql://usuario:clave@host-remoto:5432/base`) en tu gestor de contraseñas.
  - ✅ *Verificación:* `docker compose version` responde; y, cuando crees la gestionada, `SELECT extname FROM pg_extension;` lista `vector` y `pg_trgm`.

- [ ] **T-002 🧑 Verificar/crear las API keys de los modelos.**
  1. **Gemini (defecto):** ya tienes `GOOGLE_API_KEY` en `.env.local` de v1.0. Confirma en [aistudio.google.com](https://aistudio.google.com) que sigue activa y revisa los límites del tier actual (necesitamos ~10 llamadas por investigación).
  2. **Anthropic (comparativa OE3):** crea una key en [console.anthropic.com](https://console.anthropic.com) → `ANTHROPIC_API_KEY`. Carga el crédito mínimo (USD 5 basta para las corridas de evaluación). *Solo se usa en la Fase 6.*
  3. **Socrata:** confirma tu `SOCRATA_APP_TOKEN` actual en [datos.gov.co](https://www.datos.gov.co/profile/edit/developer_settings) (aumenta los límites de tarifa de la API).
  - ✅ *Verificación:* cada key responde a una llamada mínima de prueba (`curl` de ejemplo en quickstart.md §7).

- [ ] **T-003 🧑 Crear el servicio de hosting del backend.**
  1. Regístrate en [railway.app](https://railway.app) (o render.com) con GitHub.
  2. Todavía NO despliegues nada; solo confirma que la cuenta puede crear servicios web con Python. El despliegue real es T-701.
  - ✅ *Verificación:* panel accesible, plan y límites anotados.

- [ ] **T-004 Preparar el repositorio.**
  1. Crear rama `v2` en el repo actual (`cuestion-de-datos-MinTIC-2026`, renombrado desde `cuestion-de-datos-MinTIC-2025`). Todo el trabajo v2.0 ocurre en ramas hijas de `v2` con PRs.
  2. Añadir `LICENSE` (MIT — compromiso de software libre de la propuesta) y `CHANGELOG.md` con la entrada `2.0.0-dev`.
  3. Actualizar `.gitignore`: añadir `backend/.env`, `__pycache__/`, `.venv/`, `*.pyc`.
  - ✅ *Verificación:* rama `v2` existe; `git log` limpio; ningún secreto en el historial.

## FASE 1 — Reorganización del repositorio (sin cambios funcionales)

*Objetivo: estructura de plan.md §3 con la v1.0 funcionando igual que antes. Hacerlo primero evita mezclar refactor con features nuevos.*

- [x] **T-101 Mover el frontend a `frontend/`.** Mover `components/`, `pages/`, `styles/`, `public/`, `data/`, `utils/`, configs de Next/Tailwind/PostCSS y `package.json` a `frontend/`. Actualizar rutas relativas si alguna se rompe.
  - ✅ `cd frontend && npm install && npm run dev` sirve la app idéntica a v1.0 en `localhost:3000`.
- [x] **T-105 Crear `compose.yaml` en la raíz del repositorio.** Define, como mínimo: servicio PostgreSQL con extensión pgvector con **versión fijada por tag completo o digest** (nunca `latest` ni alias flotante como único pin), volumen persistente, healthcheck y variables configurables vía `.env` (usuario, clave, base, puerto). Documentar en el README del repo el comando `docker compose up -d db`. Este archivo es la vía oficial de desarrollo local (Constitución Art. II.2); Supabase/Neon quedan solo para despliegue.
  - ✅ `docker compose up -d db` deja Postgres sano (healthcheck OK); las extensiones `vector` y `pg_trgm` quedan creadas por el script `init` del contenedor local; `psql` o `docker compose exec db psql ... -c "SELECT extname FROM pg_extension;"` lista ambas; los datos sobreviven a `docker compose restart`.
- [x] **T-102 Crear el esqueleto de `backend/`.** Estructura de carpetas de plan.md §3, `pyproject.toml` (Python 3.12; dependencias de runtime/dev: fastapi, uvicorn, langgraph, langgraph-checkpoint-postgres, langchain-core, langchain-google-genai, langchain-anthropic, sqlalchemy async, alembic, psycopg[binary,pool], pgvector, httpx, pydantic-settings, pytest, pytest-asyncio, respx, ruff, pip-audit). Registrar el marcador pytest `integration` en `[tool.pytest.ini_options]` sin `addopts` global que excluya integración. Preparar, si se desea para T-205, el extra opcional `[project.optional-dependencies].benchmark-embeddings` con marcadores de versión para `sentence-transformers` y `torch` (`<VERSION_COMPATIBLE_PY312>`), pero NO instalarlas ni tratarlas como runtime hasta la decisión de T-205. Crear `app/main.py` con `GET /v2/health` (200 `ok` y 503 `degraded` como `HealthResponse`, excepción explícita al sobre estándar), settings async, transformación validada de `DATABASE_URL` según plan.md §12, inicialización idempotente del checkpointer (`AsyncPostgresSaver.setup()`) contra la base local creada en T-105, `.env.example` con todas las variables de plan.md §12/quickstart.md §2, sin valores reales ni secretos, y un mínimo de pruebas deterministas sin red (`backend/tests/test_settings.py` y/o `backend/tests/test_health.py`) para que pytest no falle por ausencia de tests.
  - ✅ Con la base de T-105 arriba, `uvicorn app.main:app` responde en `/v2/health`; antes de T-203/T-204 el check `catalog_index` puede estar `degraded/not_initialized` y la respuesta global ser `503`; el checkpointer inicializa sus tablas; la app usa SQLAlchemy async + psycopg v3; `pytest -m "not integration"` ejecuta al menos una prueba determinista real y termina en verde; `pytest -m integration` es comando válido aunque no tenga casos hasta las fases de integración.
- [x] **T-103 Configurar CI básico.** `.github/workflows/ci.yml`: jobs `backend` (ruff + `pytest -m "not integration"` + pip-audit) y `frontend` (build de Next). El job backend consume las pruebas mínimas creadas en T-102 y añade un servicio PostgreSQL+pgvector con healthcheck cuando ejecute pruebas de contrato o integración que toquen DB; las unitarias puras pueden correr sin DB. La suite `pytest -m integration` queda separada para corridas etiquetadas/diarias según pruebas.md. Debe correr en cada PR a `v2`.
  - ✅ El primer PR muestra los dos checks en verde; el job backend exporta un `DATABASE_URL` de CI compatible con psycopg v3 y confirma extensiones `vector`/`pg_trgm` antes de pruebas con DB. Verificado en [PR #1](https://github.com/Grajales-Camilo/cuestion-de-datos-MinTIC-2026/pull/1): `Backend` y `Frontend` en verde. `pip-audit` corre en modo informativo (`continue-on-error: true`) por incompatibilidad actual de `langgraph`/`langgraph-checkpoint`/`langgraph-sdk` 1.x con `langchain-core < 1` que aún requieren `langchain-google-genai`/`langchain-anthropic`; volver bloqueante antes de T-301 o T-701.
- [x] **T-104A Migraciones iniciales (SIN la tabla vectorial definitiva).** Alembic configurado; migración 001 crea todas las tablas de `data-model.md` EXCEPTO `catalog_embeddings`, cuya dimensión depende del benchmark de embeddings (T-205 → T-104B). Modelos SQLAlchemy en `app/db/models.py` espejando el documento exactamente (nombres, constraints y FKs), incluidos `official_publishers`, `official_publisher_aliases` con índice parcial único `alias_normalized WHERE ambiguous=false`, `worker_instances`, campos de token por `retention_class`, elegibilidad/PII, metadatos de revisión PII, cortes por evidencia, eventos (`agent_run_events`), `quantitative_claims` con `source_hash` de contenido reproducible, `technical_metrics.source_run_hash UNIQUE`, semillas/configuración eval, constraints de dominio y `eval_case_results.agent_run_id ON DELETE SET NULL`.
  - ✅ Con la base local ya levantada en T-105, `alembic upgrade head` crea las tablas; `psql`/`docker compose exec db psql ... -c "\\dt"` las lista; NO existe aún `catalog_embeddings`; duplicar un alias no ambiguo falla y dos ambiguos con el mismo texto son válidos; `eval_case_results.agent_run_id` permite `NULL`; los CHECK de status/retention/PII/elegibilidad/confianza rechazan valores inválidos; `technical_metrics.source_run_hash` impide duplicados.
- [x] **T-106 Registro canónico inicial de publicadores oficiales.** Crear fixture versionado, script de carga y endpoint admin `POST /v2/admin/publishers/reload` para `official_publishers` y `official_publisher_aliases` según `data-model.md` y `contracts/validacion-calidad.md`: `id`, nombre canónico, alias normalizados, tipo de entidad, vigencia, sucesor, estado, fuente de verificación y fecha de actualización. Incluir el conjunto mínimo para arrancar la ingesta: DANE, ministerios, gobernaciones/alcaldías piloto, universidades públicas, empresas industriales y comerciales del Estado y establecimientos públicos ya identificados. No exige cobertura de catálogo completo porque esa medición depende de T-201.
  - ✅ Cargar el fixture en la base local resuelve nombre canónico, alias único, mayúsculas/tildes y entidad abreviada; alias no único queda marcado `ambiguous=true` y produce `publisher_verification_status="unknown"`; publicador privado queda `private_or_non_official`. Verificado: `backend/data/official_publishers.json` (16 publicadores con las 7 categorías `entity_type` no genéricas, cada uno y cada alias con `verification_source` real verificado por búsqueda web — DANE, 9 ministerios, Gobernación de Antioquia, Alcaldías de Bogotá/Medellín, Universidad Nacional, SENA, Fondo Nacional del Ahorro — más `known_private_publishers` con Universidad de los Andes); `backend/app/db/publishers.py` (normalización, carga idempotente, `resolve_publisher`); `backend/scripts/load_official_publishers.py`; endpoint `POST /v2/admin/publishers/reload` con `X-Admin-Token` (401 sin token, 401 con token inválido, 202 con resumen) en `backend/app/main.py`. `pytest -m integration -k publishers` (9 casos) contra Postgres local confirma: alta/actualización idempotente, resolución por nombre canónico, insensibilidad a mayúsculas/tildes, resolución por alias abreviado (`MinTIC`), alias ambiguo (`Secretaria de Educacion`, compartido entre las dos alcaldías) → `unknown`/`publisher_alias_ambiguous`, publicador privado conocido (Universidad de los Andes) → `private_or_non_official`/`publisher_private`, publicador desconocido → `unknown`/`publisher_unknown`, y que un alias no ambiguo duplicado viola el índice parcial `uq_official_alias_unambiguous`. La resolución de `official_publisher_id`/`status` es la misma que T-201 escribirá en `catalog_datasets.publisher_verification_status`; T-106 no tiene datasets propios que actualizar todavía. `pytest -m "not integration"` (19 casos), `ruff check .` y prueba manual end-to-end con `uvicorn` en verde.

## FASE 2 — Índice semántico del catálogo (RF-301…304, RNF-010)

*Objetivo: pasar de 5 datasets hardcodeados a ~8.000 buscables semánticamente. Es el cimiento del agente: sin esto no hay v2.0.*

- [x] **T-201 Cliente de la Discovery API (ESC-07).** `scripts/ingest_catalog.py`: pagina `https://api.us.socrata.com/api/catalog/v1?domains=www.datos.gov.co&only=dataset` (respetando límites de tarifa), filtra tabulares con API activa y normaliza según plan.md §5. Upsert en `catalog_datasets` + `catalog_columns`, enlazando publicador contra `official_publishers`/aliases, distinguiendo `metadata_synced_at` de `data_updated_at`, calculando `pii_risk_level`, `eligibility_status` y `eligibility_reasons`. Registra `ingest_runs`.
  - ✅ Correr el script llena ≥ 7.000 datasets; re-correrlo no duplica nada (RF-304). Verificado: corrida real completa contra la Discovery API real produjo **8.415 datasets** (100% de `resultSetSize`) y **108.563 columnas**, 0 fallidos; segunda corrida completa confirmó idempotencia exacta (`nuevos: 0, actualizados: 8415`, mismo conteo total, sin duplicados, `api_active=true` en las 8.415 filas). Nombres de campo reales de la Discovery API difieren de los citados en plan.md §5.3 (`data_updated_at` real en vez de `rowsUpdatedAt`; publicador en `owner.display_name`, no en `classification.domain_metadata`) — confirmado con peticiones reales, documentado en `backend/app/catalog/normalize.py`. Se agregaron `backend/app/quality/pii_patterns.yaml` (fixture de patrones PII, sin esquema previo en specs) y `backend/app/quality/eligibility.py` (mapeo de elegibilidad corregido contra `contracts/validacion-calidad.md` §3.1: publicador no verificado → `diagnostic_only`, no `blocked`). `resolve_publisher()` (T-106) se extendió con `reference_date` opcional para vigencia institucional/sucesor sin romper el comportamiento previo. 123 pruebas (103 `pytest -m "not integration"` + 20 `pytest -m integration` contra Postgres real) y `ruff check .` en verde. Con el fixture inicial de 16 publicadores, 163/8415 datasets quedan `verified`/`eligible` o `diagnostic_only`; el resto queda `blocked` por `pii_unknown` (default seguro del Art. VI.1) — comportamiento esperado, la ampliación del fixture PII/publicadores es trabajo de seguimiento (T-201A cubre publicadores; no existe aún una tarea equivalente para PII).
- [x] **T-201A Auditoría de cobertura de publicadores.** Después de la primera ingesta real, generar `backend/data/official_publishers_coverage.json` con conteos por `publisher_verification_status`, aliases no únicos detectados, candidatos a alias nuevos y cobertura de datasets tabulares activos. Actualizar el fixture solo con procedencia verificable.
  - ✅ Nuevo módulo puro `backend/app/catalog/coverage.py` (conteos por `publisher_verification_status`, aliases `ambiguous=true` agrupados, candidatos `unknown` priorizados por volumen de datasets) + CLI `backend/scripts/audit_publisher_coverage.py`, probados con 5 casos unitarios (`backend/tests/test_publisher_coverage.py`) y 3 de integración contra Postgres real (`backend/tests/integration/test_publisher_coverage_queries.py`). Auditoría inicial contra los 8.415/8.417 datasets reales ya ingeridos por T-201: 163 `verified` (1,94%) — brecha explícita frente al 90%. Se amplió `backend/data/official_publishers.json` de 16 a 61 publicadores (45 nuevos: 23 entidades nacionales — UPRA, Superintendencias de Servicios Públicos/Financiera, Invima, Ideam, DNP, Icfes, Unidad para las Víctimas, Función Pública, Cancillería, ARN, ANDJE, Medicina Legal, ESAP, MinMinas, Prosperidad Social, Corte Constitucional, Contraloría y Procuraduría Generales, Presidencia, ANI, ICA, MinVivienda —, 10 gobernaciones — Valle del Cauca, Boyacá, Casanare, Santander, Sucre, Magdalena, Atlántico, Caldas, Meta, Amazonas —, 10 alcaldías de mayor volumen — Palmira, Pereira, Bucaramanga, Neiva, Barranquilla, Chía, Fusagasugá, Yopal, Guadalajara de Buga, San Pedro de los Milagros — y Universidad del Quindío + Gobernación de Risaralda), cada uno con `verification_source` real verificado por búsqueda web (sitio institucional `.gov.co`/`.edu.co`), misma disciplina que T-106: sin listas inventadas y sin heurística de coincidencia difusa (alias exactos sobre el texto crudo observado en `catalog_datasets.publisher` cuando difiere del nombre canónico). Recarga del fixture + re-ejecución completa de `scripts/ingest_catalog.py` (idempotente, confirmado por `datasets_new≈0` en la segunda corrida) elevó la cobertura real a **2.376/8.416 = 28,23% `verified`** (+2.213 datasets `unknown`→`verified` netos frente a la línea base de 163, ~14.5× la cobertura inicial). **Brecha explícita documentada** (`coverage_gap: true`, campo `gap_explanation` en el reporte): no se alcanza el 90% porque el catálogo tiene una cola larga de 1.500+ textos de `publisher` distintos sin resolver (la mayoría con ≤10 datasets cada uno: notarías, ESE hospitalarias, cámaras de comercio —privadas, no estatales—, personas naturales como responsable de datos, y cientos de alcaldías municipales de bajo volumen); cerrar esa brecha requiere onboarding sistemático municipio por municipio, fuera del alcance verificable de un único PR y candidato natural a apoyarse en el maestro DIVIPOLA de T-202. Ningún alias no único (`ambiguous=true`, 1 caso: "Secretaria de Educacion" compartido por Bogotá/Medellín) asigna publicador automáticamente — verificado en `test_resolve_ambiguous_alias_is_unknown` y en el propio reporte de cobertura. `ruff check .`, `pytest -m "not integration"` (108 casos) y `pytest -m integration` (23 casos + 1 skip) en verde contra Postgres local real. Seguimiento operativo (agilizar rondas futuras de ampliación, no forma parte del criterio de aceptación de T-201A): `backend/app/db/publishers_admin.py` + CLI `backend/scripts/add_publishers.py` fusionan entidades nuevas desde un CSV (`id,canonical_name,entity_type,verification_source,aliases,ambiguous_aliases,active`) sobre el fixture existente sin editar el JSON a mano, con validación de `entity_type`, detección de colisión de alias/`canonical_name` contra otras entidades y modo `--dry-run`; sigue exigiendo `verification_source` real verificado por un humano por cada fila (no automatiza la verificación, solo la fusión mecánica). 11 pruebas unitarias en `backend/tests/test_add_publishers.py`. Documentado en `quickstart.md` §3.

Segunda ronda (petición explícita de Camilo de una lista curada de 47 entidades responsables + las 32 gobernaciones departamentales): usando `add_publishers.py` sobre un CSV verificado, se agregaron 45 publicadores más (total **106**, hasta las 32 gobernaciones completas — antes 12) y 26 alias nuevos (incluye ampliar `funcion-publica`, `prosperidad-social`, `minhacienda`, `minvivienda` y `upra` ya existentes), cada uno con `verification_source` verificado por búsqueda web antes de escribirse. Dos casos se resolvieron explícitamente SIN agregarlos como oficiales, siguiendo la misma disciplina de no inventar ni clasificar mal: **Confederación Colombiana de Cámaras de Comercio (Confecámaras)** — verificado como "entidad privada, de utilidad común, gremial y sin ánimo de lucro" (fuente: confecamaras.org.co) — se registró en `known_private_publishers` (no en `official_publishers`), no como estatal; **"Paratec"** no se agregó porque no es una entidad legal propia sino una plataforma técnica (`paratec.xm.com.co`) operada por XM S.A. E.S.P., empresa de **naturaleza mixta** (no estatal pura, verificado), por lo que no hay una clasificación de `entity_type` verificable sin arriesgar una asignación incorrecta — pendiente de decisión humana si se detectan datasets reales bajo ese nombre. Re-ingesta completa + auditoría: cobertura sube a **2.433/8.416 = 28,91% verified**. `ruff check .`, `pytest -m "not integration"` (119 casos) y `pytest -m integration` (23 + 1 skip) en verde.

Tercera ronda (evaluación del dataset MinTIC `fn2v-r4gu` — "Hoja de Ruta Nacional de Datos Abiertos Estratégicos del Estado Colombiano", el mismo del que sale la lista de 47 entidades — y su uso como técnica de verificación): la columna `enlace_portal_dato_abierto` de ese dataset apunta a `dataset_id` reales de `catalog_datasets`; cruzar esos 86 enlaces contra el texto real de `publisher` en la base (en vez de adivinar/buscar) reveló 24 textos crudos sin resolver para entidades ya verificadas, cada uno confirmado además por la columna `entidades_responsables` del propio dataset de MinTIC como evidencia cruzada. Se aplicaron con `add_publishers.py`: 12 alias nuevos sobre entidades existentes (`apc-colombia`, `minagricultura`, `colombia-compra-eficiente`, `supertransporte`, `mindefensa`, `minambiente`, `dane`, `registraduria`, `servicios-postales-nacionales`, `mintransporte`, `auditoria-general`, `instituto-nacional-salud`) y 1 entidad nueva verificada (**INVIAS** — Instituto Nacional de Vías, establecimiento público adscrito a MinTransporte). Dos casos adicionales se identificaron como NO estatales y se registraron en `known_private_publishers` en vez de `official_publishers`: **RUNT 2.0** (Registro Único Nacional de Tránsito) opera bajo una concesión privada (Concesión RUNT 2.0 S.A.S., contrato 604 de 2022 con MinTransporte, verificado) — mismo patrón que "Paratec"/XM de la ronda anterior; **"serviciorues"** (RUES) confirmado como servicio de Confecámaras (privado). Se descartaron deliberadamente por riesgo de mala atribución: un nombre de persona natural ligado a un dataset de la DIAN (los alias institucionales no deben identificar personas, aunque el roadmap confirmaba su procedencia con DIAN) y dos textos genéricos de oficina ("OficinaTI", "Gestión de Tecnologías de la Información") que aparecen con múltiples datasets en el catálogo pero solo una coincidencia confirmada cada uno contra el roadmap — agregarlos como alias no ambiguos habría asignado automáticamente datasets de otras entidades sin evidencia. Re-ingesta completa + auditoría: cobertura sube a **2.611/8.416 = 31,02% verified** (+178 datasets). `ruff check .`, `pytest -m "not integration"` (119 casos) y `pytest -m integration` (23 + 1 skip) en verde. Evaluación de la API: el endpoint público estable es `https://www.datos.gov.co/resource/fn2v-r4gu.json` (SODA v2.1, mismo patrón que `discovery_client.py`); el endpoint `/api/v3/views/{id}/query.json` dado inicialmente es la API interna no documentada de la UI de Socrata y no debe usarse para integraciones. El dataset es útil como fuente de procedencia y como técnica de verificación cruzada de alias (no de descubrimiento de entidades nuevas más allá de las 48 que ya cubre); no cierra la brecha del 90% porque solo cubre el conjunto "prioritario" de MinTIC, no la cola larga de ~1.500 textos de `publisher` distintos del catálogo completo.
- [x] **T-202 Carga del maestro DIVIPOLA.** Script que descarga el dataset oficial DIVIPOLA de datos.gov.co (dataset del DANE, id `gdxc-w37w` o su vigente — confirmar en el portal) y llena `divipola_entries` con `name_normalized` y `alt_names`.
  - ✅ Id `gdxc-w37w` confirmado vigente en el portal (2026-07-08): "DIVIPOLA- Códigos municipios", publicado bajo licencia DANE (CC BY-SA 4.0), recurso SODA `https://www.datos.gov.co/resource/gdxc-w37w.json`, 1.122 filas, columnas `cod_dpto`/`dpto`/`cod_mpio`/`nom_mpio`/`tipo_municipio`/`longitud`/`latitud`. El recurso solo lista municipios (incluye `tipo_municipio` "Isla" y "Área no municipalizada" bajo el mismo nivel de 5 dígitos); las filas `level="department"` se derivan agrupando los pares `(cod_dpto, dpto)` únicos que ya traen esas mismas filas, porque el DANE no publica en este id un recurso separado de departamentos — divergencia menor de plan.md/data-model.md documentada en el docstring de `backend/app/db/divipola.py`, mismo espíritu que las divergencias de Discovery API documentadas en T-201. Nuevos módulos: `backend/app/catalog/divipola_client.py` (cliente SODA paginado con `$limit`/`$offset` ordenado por `:id`, timeout 10s + 1 reintento en 5xx/timeout, mismo patrón que `discovery_client.py`), `backend/app/db/divipola.py` (normalización `name_normalized`, `KNOWN_ALT_NAMES` curado para Bogotá D.C./Santafé de Bogotá, derivación de departamentos, upsert idempotente por `code` vía `pg_insert...on_conflict_do_update`, mismo patrón que `app/db/publishers.py`), CLI delgado `backend/scripts/load_divipola.py`. Corrida real contra Postgres local: **1.122 municipios + 33 departamentos = 1.155 filas** (`creados: 1150, actualizados: 5` en la primera corrida sobre una base con residuos previos de prueba); segunda corrida confirma idempotencia exacta (`creados: 0, actualizados: 1155`, mismo conteo total, sin duplicados). Criterios de aceptación verificados contra la base real: `SELECT count(*) FROM divipola_entries WHERE level='municipality';` → **1122** (≥ 1.100); `SELECT code, name FROM divipola_entries WHERE level='municipality' AND name_normalized % 'CARMEN VIBORAL' ORDER BY similarity(name_normalized, 'CARMEN VIBORAL') DESC LIMIT 1;` → **05148, EL CARMEN DE VIBORAL** (mejor coincidencia trigram, por encima de "El Carmen" y "Carmen de Carupá"). 24 pruebas unitarias sin red (`tests/test_divipola_normalize.py`, `tests/test_divipola_client.py`: normalización de tildes/mayúsculas, filas incompletas rechazadas, alias conocidos, deduplicación de departamentos, paginación/reintento/token del cliente HTTP) + 4 de integración contra Postgres real (`tests/integration/test_divipola.py`: upsert crea departamentos+municipios, idempotencia, búsqueda trigram; `tests/integration/test_divipola_live.py`: una página real del dataset produce filas válidas, deliberadamente pequeña como el equivalente de T-201 para Discovery API). `ruff check .` y `pytest -m "not integration"` (137 casos totales, 18 nuevos de T-202) en verde; `pytest -m integration -k divipola` (4 casos) en verde contra Postgres local real.
- [x] **T-205 🧑 Benchmark de embeddings y decisión (ANTES de crear la tabla vectorial).** Notebook `notebooks/01_benchmark_embeddings.ipynb`: instalar entorno con `pip install -e ".[dev,benchmark-embeddings]"`, fijando antes en `backend/pyproject.toml` versiones compatibles con Python 3.12 para `sentence-transformers`, PyTorch y dependencias relevantes del candidato local. Comparar `intfloat/multilingual-e5-large` (local), `gemini-embedding-2` (gestionado) y, si se justifica en `research.md`, otro modelo multilingüe actual, sobre una muestra de los metadatos ya ingeridos (T-201) y el maestro DIVIPOLA cargado (T-202), con 30+ consultas de prueba. Evaluar TODOS los criterios de `research.md` §1: recuperación en español, calidad en consultas territoriales colombianas, dimensión de vectores, latencia, costo, ejecución local, dependencia de proveedor, reproducibilidad, tamaño del índice y compatibilidad pgvector. Registrar versión de Python, versiones de librerías, identificador y versión exacta del modelo, CPU/RAM/dispositivo, tamaño de lote, parámetros de codificación, normalización de vectores, semillas, tiempo de carga separado de latencia por consulta, costo/configuración del candidato gestionado y comandos/notebook de reproducción. Si se mantiene `vector(<DIM>)`, aceptar solo `DIM <= 2000`; si se elige una dimensión mayor, documentar cambio a `halfvec` antes de T-104B. **Tú decides** el modelo y con él la dimensión; registrar la decisión y su justificación en `research.md` §1 y propagar el resultado a `data-model.md`, dependencias definitivas de `pyproject.toml`, `.env.example`, `plan.md` y `quickstart.md`.
  - ✅ **Decisión (2026-07-09, firmada por Juan Camilo Grajales B. en `notebooks/01_benchmark_embeddings.ipynb` §7):** `gemini-embedding-2` (Google, gestionado), 768 dimensiones, `vector(768)` + `vector_cosine_ops` (no requiere `halfvec`). Corrida real contra el fixture congelado (`notebooks/fixtures/`: 200 datasets reales de T-201 vía `scripts/export_benchmark_fixture.py`, 1.155 filas reales de T-202) con 34 consultas anotadas a mano (11 territoriales, verificadas contra `divipola_master.json` real — 3 códigos DIVIPOLA mal citados en un borrador previo se corrigieron antes de la corrida final: Medellín 05001, Risaralda 66, Tunja 15001). Resultado: recall@10 empatado en ambos candidatos (94,12% general, 90,91% territorial — no discrimina); `gemini-embedding-2` gana en latencia real (p95 41,8ms vs 120,2ms del local) y sobre todo en costo de infraestructura: el pico de RAM medido del local (`PeakRSSTracker`, muestreo real, no `ram_total_gb` de la máquina) fue **2.430,7 MB**, incompatible con el tier gratuito de Railway (0,5 GB) y, como RF-302 exige embeber la consulta "en caliente" en el mismo proceso del backend, esa RAM quedaría reservada permanentemente dentro del tier Hobby (~USD 5/mes ya presupuestado en `plan.md` §2) sin importar el volumen de uso. Costo real de la API gestionada: $0,0092 por el corpus+consultas de prueba (45.818 tokens reales) y $0,39 para una reindexación completa del catálogo activo (~8.416 datasets, evento raro no recurrente) — marginal al volumen del piloto (`spec.md` SUP-02). Se acepta perder los criterios 7 y 8 (dependencia de proveedor y reproducibilidad, donde el local es superior) por precedente arquitectónico ya existente (capa multi-proveedor del LLM, RF-206) y porque cambiar de modelo más adelante tiene costo conocido y acotado (regenerar el índice, ~$0,39 al tamaño actual). Propagación completa en el mismo PR: `research.md` §1 (`DECIDIDA`, tabla de evidencia + justificación completa), `data-model.md` (`<DIM>` → `768` en `catalog_embeddings`, dos ocurrencias), `plan.md` (fila de stack de embeddings + condición de T-104B), `backend/.env.example` y `quickstart.md` (`EMBEDDING_MODEL=gemini-embedding-2`), `backend/pyproject.toml` (comentario aclarando que no se agrega dependencia de runtime nueva — `langchain-google-genai`/`httpx` ya estaban). T-104B queda desbloqueada (pendiente de implementar la migración Alembic en sí, fuera del alcance de T-205).
- [x] **T-104B Migración definitiva de `catalog_embeddings`.** Con el modelo y la dimensión decididos en T-205: migración Alembic que crea `catalog_embeddings` con `vector(<dimensión elegida>)` + `vector_cosine_ops` si `DIM <= 2000`, o `halfvec(<dimensión elegida>)` + operador compatible si `DIM > 2000`. Actualizar `data-model.md` reemplazando `<DIM>` por el valor real en el mismo PR.
  - ✅ Implementado con `gemini-embedding-2`, 768 dimensiones: `backend/app/db/models.py` agrega `CatalogEmbedding` con `dataset_id text` PK/FK a `catalog_datasets.id ON DELETE CASCADE`, `embedding Vector(768) NOT NULL`, `model text NOT NULL` e índice SQLAlchemy `Index(..., postgresql_using="hnsw", postgresql_ops={"embedding": "vector_cosine_ops"})`; se eligió `ON DELETE CASCADE` porque la relación es 1:1 dependiente y sigue el criterio ya usado por `catalog_columns` para metadatos derivados del dataset. Migración nueva `backend/migrations/versions/20260709_0002_catalog_embeddings.py`, `down_revision="20260708_0001"`, crea solo tabla e índice; NO ejecuta `CREATE EXTENSION`, coherente con plan.md §2/T-105. Registro de tipo pgvector para SQLAlchemy async + psycopg v3 centralizado en `backend/app/db/engine.py` con `register_vector_async` en evento `engine.sync_engine.connect`; se aplicó a `main.py` y scripts existentes que crean engines de aplicación. El checkpointer (`AsyncPostgresSaver`) no requiere registro pgvector porque no lee/escribe columnas `vector`; las escrituras futuras de T-203 deben usar el helper `create_app_async_engine`.
  - ✅ Verificación local real contra Docker Postgres (`POSTGRES_PORT=5433`, `DATABASE_URL=postgresql://usuario:clave@localhost:5433/cuestion_de_datos`): antes de migrar, `alembic current` → `20260708_0001`, `SELECT to_regclass('public.catalog_embeddings')` → `NULL`, y `docker compose exec db psql ... -c "\dx"` ya listaba `vector 0.8.0` y `pg_trgm 1.6` creadas por el init del contenedor. `alembic upgrade head` ejecutó `Running upgrade 20260708_0001 -> 20260709_0002, Add catalog embeddings table.`. `docker compose exec db psql -U usuario -d cuestion_de_datos -c "\d catalog_embeddings"` mostró `embedding vector(768) not null`, PK btree en `dataset_id`, índice `ix_catalog_embeddings_embedding_hnsw hnsw (embedding vector_cosine_ops)` y FK `ON DELETE CASCADE`; consulta a `pg_indexes` confirmó `CREATE INDEX ix_catalog_embeddings_embedding_hnsw ON public.catalog_embeddings USING hnsw (embedding vector_cosine_ops)`. Prueba transaccional ORM insertó y leyó un `CatalogEmbedding` de 768 floats con `model=gemini-embedding-2` usando `SelectorEventLoop` en Windows y luego hizo rollback (`insert_and_select_ok model=gemini-embedding-2`). `Select-String data-model.md '<DIM>'` no devolvió resultados. `ruff check .`, `pytest -m "not integration"` (137 passed, 28 deselected) y `pytest -m integration` (27 passed, 1 skipped) en verde.
- [x] **T-203 Generación de embeddings (ESC-07).** `scripts/build_embeddings.py`: construye `embedding_text` (plan.md §5.4), genera embeddings por lotes con el modelo seleccionado y hace upsert en `catalog_embeddings`.
  - ✅ Implementado en `backend/app/catalog/embeddings.py` + CLI `backend/scripts/build_embeddings.py`: reutiliza `build_embedding_text()` para auditar `catalog_datasets.embedding_text`, exige `EMBEDDING_MODEL=gemini-embedding-2`, fija `output_dimensionality=768`, valida dimensión de cada vector, usa `create_app_async_engine()` para SQLAlchemy async + psycopg v3 con pgvector registrado, hace upsert idempotente en `catalog_embeddings`, y bloquea mezcla de modelos salvo regeneración explícita con `--replace-existing-model`. Validación real contra Docker Postgres (`DATABASE_URL=postgresql://usuario:clave@localhost:5433/cuestion_de_datos`): después de `python scripts/ingest_catalog.py` (`8416` datasets nuevos, `0` fallidos), `python scripts/build_embeddings.py --batch-size 25 --only-missing` completó el índice; el intento previo con lote 100 persistió 800 y luego falló por `504 Deadline Exceeded`, por eso el reintento idempotente reportó `Embeddings upserted: 7616 | selected: 7616 | active_datasets: 8416 | model: gemini-embedding-2 | dimension: 768`. SQL directo confirmó `active_datasets=8416`, `embeddings=8416`, `active_missing_embeddings=0`, un único `model=gemini-embedding-2`, `min(vector_dims(embedding))=768`, `max(vector_dims(embedding))=768`, y `8416` datasets activos con `embedding_text` no vacío. Pruebas: `ruff check .`, `pytest -m "not integration"` (143 passed, 31 deselected), `pytest -m integration` (30 passed, 1 skipped) y `tests/integration/test_build_embeddings.py` valida contra Postgres real upsert, idempotencia, dimensión 768 y rechazo de modelos mezclados con cliente de embeddings falso.
- [x] **T-204 Endpoint de búsqueda.** `GET /v2/catalog/search` según `contracts/api-rest.md` §8: valida `q`, limita `k`, excluye datasets inactivos, marca `index_stale` y devuelve `latest_observed_cutoff_at` solo como pista, nunca como corte de evidencia.
  - ✅ Implementado en `backend/app/catalog/search.py` y expuesto en `backend/app/main.py` sin implementar todavía herramientas del agente T1-T5 ni grafo. Usa `catalog_embeddings` existente y genera solo el embedding de la consulta (`RETRIEVAL_QUERY`, `gemini-embedding-2`, 768 dimensiones); la similitud devuelta sigue siendo `1 - (embedding <=> query_embedding)` sobre pgvector/vector_cosine_ops y el orden combina esa similitud con un reordenamiento lexical acotado sobre un subconjunto candidato para recuperar columnas relevantes sin regenerar embeddings. Validaciones de contrato: `q` se trimmea y exige 3-500 caracteres; `k` exige 1-25 y `k=999` devuelve `422 VALIDATION_ERROR` sin recorte silencioso; `api_active=false` se excluye; `index_stale` se calcula con `CATALOG_STALE_AFTER_DAYS`; `latest_observed_cutoff_at` viaja solo como pista y no se expone `data_cutoff_at`. Verificación local real contra Docker Postgres en `localhost:5433` tras `alembic upgrade head`, ingesta real y embeddings existentes/restaurados: `catalog_datasets=8423`, `catalog_embeddings=8416`, `active_datasets=8416`; `GET /v2/catalog/search?q=deserción escolar&k=5` respondió `200` en `627,93 ms` y retornó datasets MEN en top-5 (`sras-4t5p`, `ji8i-4anb`); `k=999` respondió `422 VALIDATION_ERROR`; marcar temporalmente `sras-4t5p` como `api_active=false` lo excluyó del top-5 y luego se restauró a `true`. Pruebas: `ruff check .`, `pytest -m "not integration"` (147 passed, 33 deselected) y `pytest -m integration` (32 passed, 1 skipped, 147 deselected) en verde contra Postgres real.

Seguimiento (revisión de código post-merge, dos huecos detectados y cerrados en el mismo estilo de T-201A): (1) el reordenamiento lexical dependía de `SYNONYMS_ES`, un diccionario de 2 entradas sin documentar, ajustado aparentemente solo para que "deserción escolar" pasara — se documentó en `backend/app/catalog/search.py` su origen real (similitud coseno de los datasets MEN ~0,60-0,63 fuera del top-25 frente a ~0,68-0,70 de competidores, medido con el índice real), su alcance explícito (parche acotado, no tesauro general) y su límite conocido (no generaliza; ampliar solo con evidencia real, no por intuición); se agregaron 3 pruebas nuevas en `backend/tests/test_catalog_embeddings.py` que verifican que consultas sin sinónimo conocido (`"trafico portuario maritimo"`) no se rompen ni inventan términos. (2) RNF-010 ("medir al menos 100 consultas representativas... p95 ≤ 1 s, cobertura ≥ 90%", `pruebas.md` §2.3) nunca se había implementado — la única evidencia de latencia era una consulta manual (627,93 ms). Se agregó `backend/app/catalog/search_benchmark.py` (percentiles + generador determinista de consultas representativas a partir de categorías reales de `catalog_datasets` y departamentos reales de `divipola_entries`, sin anotación de relevancia — eso sigue siendo responsabilidad de `research.md` §1) con pruebas unitarias en `backend/tests/test_catalog_search_benchmark.py`, y `backend/tests/integration/test_catalog_search_rnf010.py` que corre 100 consultas reales contra `/v2/catalog/search` con el índice real. **Evidencia real (aislada, catálogo completo restaurado — `catalog_datasets=8423`, `catalog_embeddings=8416`, `active_datasets=8416`):** p50=454,5 ms, **p95=921,3 ms** (dentro del presupuesto de 1.000 ms), p99=1.150,6 ms; cobertura=100,00% (8.416/8.416 activos, ≥ 90% exigido); excluidos de la búsqueda por `api_active=false`=0,08% (7/8.423); activos pero no elegibles como evidencia (RF-401, gap ya documentado en T-201A, no filtrado por esta búsqueda)=100,00% — se reportan ambas exclusiones por separado porque mezclarlas en un solo porcentaje ("excluidos por api_active/elegibilidad") es engañoso: la búsqueda sí devuelve resultados para el 100% de los activos, la no-elegibilidad es un gate posterior de la capa de calidad, no de este endpoint. Nota de aislamiento: la prueba se salta con diagnóstico claro (no falla) si otra prueba de la misma sesión ya vació `catalog_datasets` antes de que corriera — problema de orden compartido de la suite, no de RNF-010; corrida aislada (`pytest -m integration -k rnf010`) para evidencia real. `ruff check .`, `pytest -m "not integration"` (158 passed) y `pytest -m integration` (32 passed, 2 skipped) en verde.
- [ ] **T-206 Cron de ingesta (ESC-07).** `.github/workflows/ingest-cron.yml` semanal (domingo 3:00 UTC) que ejecuta T-201+T-203 contra la base productiva usando secrets del repo. Implementa `POST /v2/admin/ingest` y `GET /v2/admin/ingest/runs` (RF-701).
  - ✅ Ejecución manual del workflow termina en verde y crea un `ingest_runs` nuevo.
- [x] **T-207 🧑 Carga de tipologías territoriales DNP (research.md §18).** `scripts/load_tipologias_dnp.py --archivo <ruta> --vigencia <año>`: lee las hojas `Municipios`/`Departamentos` del archivo `Base de Datos y Resultados` que el DNP publica cada vigencia en `colaboracion.dnp.gov.co` (sin URL estable año a año — **tú descargas el archivo a mano**, mismo patrón operativo que T-205), valida `CodDANE_txt`/`cod_dep_txt_corto` contra `divipola_entries.code` existente y hace upsert en `territorio_tipologia` (`data-model.md` §6). Agregar `openpyxl` a `pyproject.toml`.
  - ✅ **Divergencia encontrada en la práctica:** la hoja `Departamentos` trae dos columnas candidatas — `cod_dep_txt` (5 dígitos, p. ej. `05000`) y `cod_dep_txt_corto` (2 dígitos, `05`) —; solo la segunda coincide con `divipola_entries.code`. Se documentó en el docstring de `app/db/tipologias.py`, mismo espíritu que la divergencia de `tipo_municipio` documentada en T-202. Además, la fila de encabezados NO está en la misma posición en ambas hojas (fila 2 en `Municipios`, fila 3 en `Departamentos`); `_find_header` la ubica buscando la columna clave, no por índice fijo — 2 pruebas unitarias cubren ambos casos con un workbook sintético (`tests/test_tipologias_normalize.py`).
  - ✅ Nuevos módulos: `app/db/tipologias.py` (parseo puro `municipio_row_to_entry`/`departamento_row_to_entry`/`rows_to_entries`, separado de la lectura del archivo `read_workbook_entries`; upsert idempotente `upsert_territorio_tipologia` con pre-chequeo de `divipola_entries.code` conocido antes de insertar, para no abortar la transacción completa por una fila con código desconocido), `app/db/models.py` (`TerritorioTipologia`, FK a `divipola_entries.code`, CHECK de `level`), migración `migrations/versions/20260711_0003_territorio_tipologia.py`, CLI `scripts/load_tipologias_dnp.py`. `openpyxl>=3.1,<4` agregado a `pyproject.toml` (dependencia de runtime, no solo de test).
  - ✅ **Corrida real** contra Postgres local (`alembic upgrade head` aplicó `20260711_0003` sin error) con `divipola_entries` cargada por `scripts/load_divipola.py` (1.122 municipios + 33 departamentos reales) y el archivo real `01_ResultadosTipologias2026.xlsx` del DNP (vigencia 2026): **`Filas leídas: 1135 | municipios: 1103 | departamentos: 32 | creados: 1135 | actualizados: 0 | saltados: 0`** — coincide exactamente con el criterio de aceptación (~1.103 municipios + 32 departamentos, 0 saltados porque `divipola_entries` ya estaba completa). Segunda corrida con el mismo archivo confirma idempotencia exacta: `creados: 0, actualizados: 1135`, mismo total. Verificación puntual de filas reales: `05001` (Medellín) → `Ciudades grandes`/`ESP`; `11001` (Bogotá) → `Bogotá`/`ESP`; `05148` (El Carmen de Viboral) → tipología `2`/categoría `2`. 14 pruebas nuevas en verde (`pytest -m "not integration" tests/test_tipologias_normalize.py`: 11; `pytest -m integration tests/integration/test_tipologias.py`: 3, incluye el caso de código DIVIPOLA desconocido saltado y reportado sin romper la carga). `ruff check .` en verde.

## FASE 3 — Agente multi-paso (RF-201…209)

*Objetivo: el grafo LangGraph con sus herramientas reemplaza el bucle manual de v1.0, con durabilidad desde el primer día.*

- [x] **T-300 Prueba de concepto de durabilidad (ANTES de construir el grafo completo).** Prototipo mínimo (un endpoint + un grafo de juguete de 3 nodos) que demuestre de punta a punta la semántica única de plan.md §11: `worker_instance_id` + lease con TTL y renovación, cierre idempotente solo de instancias con lease vencida, eventos SSE numerados persistidos en `agent_run_events` con reserva atómica de `last_event_seq` vía `UPDATE agent_runs ... RETURNING last_event_seq` dentro de la misma transacción que inserta el evento, evento terminal `error` con `status`, `thread_id=run_id`, reconexión con `Last-Event-ID` sin pérdida ni duplicación, reinicio real ⇒ `interrupted/RUN_INTERRUPTED`, heartbeat vencido ⇒ `interrupted/HEARTBEAT_EXPIRED`, duración máxima ⇒ `failed/RUN_TIMEOUT`, DELETE de corrida activa y limpieza de checkpoints con `adelete_thread`. Cubrir dos emisores concurrentes, timeout compitiendo con evento normal, detector de worker perdido, escritura duplicada del evento terminal, rollback tras reservar secuencia y reintento idempotente. Si algo del diseño no funciona en la práctica, se corrige plan.md/contratos AQUÍ, no en la Fase 5.
  - ✅ Implementado en `backend/app/agent/` (nuevo): `durability.py` (`reserve_and_emit_event` — reserva atómica de `seq` + insert en la misma transacción; `write_terminal_event_once` — UPDATE condicional `WHERE terminal_event_written_at IS NULL` que reserva `seq` y fija el estado terminal en una sola sentencia, garantía de como máximo un evento terminal), `worker_lease.py` (lease de `worker_instances`, renovación, `close_stale_running_runs` para arranque idempotente), `heartbeat_sweep.py` (barrido periódico: `RUN_TIMEOUT`/`HEARTBEAT_EXPIRED`/`WORKER_LOST`, en ese orden de prioridad), `toy_graph.py` (grafo LangGraph de 3 nodos compilado con `interrupt_after=NODES`, cada nodo emite un evento `step`), `runner.py` (orquesta la corrida completa dentro de una única llamada `asyncio.to_thread(..., loop_factory=SelectorEventLoop)` en Windows — un solo loop para toda la corrida evita reabrir conexiones psycopg-async entre pasos; cancelación cooperativa vía `threading.Event`, NO `Task.cancel()`, porque cancelar la tarea que envuelve `to_thread` no interrumpe el hilo subyacente). Endpoints nuevos en `backend/app/main.py` bajo el prefijo deliberado `/v2/_poc/agent/...` (NO el contrato final de T-304): `POST /v2/_poc/agent/query`, `GET /v2/_poc/agent/stream/{run_id}` (SSE con reconexión `Last-Event-ID`, poll cada 300ms sobre `agent_run_events`, ping cada 15s), `DELETE /v2/_poc/agent/runs/{run_id}` (cancelación cooperativa con `DELETE_ACTIVE_GRACE_S` + `adelete_thread` + borrado de la fila). Lifespan extendido: registra `worker_instance_id`, corre el cierre idempotente de huérfanas, y lanza dos tareas de fondo (renovación de lease cada `WORKER_LEASE_TTL_S/3`, barrido cada 2s).
  - ✅ **Decisiones de alcance documentadas:** sin `Authorization: Bearer` (RF-801) en los endpoints del PoC — no está en la lista literal de "qué construir" de T-300, pertenece a T-304; el `run_access_token` igual se genera y su hash se persiste (columnas NOT NULL de `agent_runs`) pero se descarta sin exponerlo. **Aclaración de ambigüedad de plan.md §11 encontrada en la práctica** (documentada en `research.md` §2, 2026-07-09): el texto normativo permite `RUN_INTERRUPTED` **o** `WORKER_LOST` sin regla de desambiguación; se adoptó la convención `RUN_INTERRUPTED` = arranque idempotente, `WORKER_LOST` = barrido periódico en operación. Se verificó además que el piso `WORKER_LEASE_TTL_S >= 30` (constraint ya existente en `config.py`) implica que reiniciar el proceso inmediatamente después de matarlo NO produce interrupción instantánea si la lease aún no venció — comportamiento correcto (protege despliegues solapados), pero exige que la demo/prueba de kill real espere ese piso antes de reconectar.
  - ✅ **Demo real de matar el proceso** (criterio de aceptación, ejecutada de verdad, no simulada): `backend/tests/integration/test_t300_durability.py::test_real_process_kill_demo_interrupts_and_reconnects_without_loss` lanza el backend real como subproceso (`subprocess.Popen`), crea una corrida, consume 2 eventos reales por HTTP/SSE, mata el proceso con `Popen.kill()` (`TerminateProcess` en Windows — confirmado `returncode != 0`, terminación dura, no shutdown limpio), espera el vencimiento real de la lease (30s + margen), levanta un segundo subproceso apuntando a la misma base, reconecta con `Last-Event-ID` y verifica: eventos faltantes recibidos exactamente una vez cada uno, sin huecos, y estado terminal `error`/`RUN_INTERRUPTED`. Corrida real: **PASSED en 52,32s**. Log real observado: servidor #1 registra `202 Accepted` + `200 OK` (stream) antes del kill; servidor #2 registra el arranque, y la reconexión trae `seq=3, event=error` con el código `RUN_INTERRUPTED`.
  - ✅ **Pruebas de concurrencia real** (`pytest -m integration -k t300_durability`, `asyncio.gather` con conexiones/sesiones independientes contra Postgres real, nunca awaits secuenciales disfrazados) — **11 casos, todos en verde**: dos emisores concurrentes (12 llamadas simultáneas a `reserve_and_emit_event`) producen `seq` 1..12 contiguo sin huecos ni duplicados; timeout compitiendo con un evento normal produce exactamente un evento terminal; detector de worker perdido (`WORKER_LOST`) vía barrido periódico, repetirlo no duplica; `write_terminal_event_once` llamado dos veces es no-op la segunda vez; reservar `seq` y forzar rollback sin insertar el evento permite que el número se reutilice sin hueco observable; reconexión repetida con el mismo `Last-Event-ID` no pierde ni duplica eventos; una instancia nueva con lease vigente en la corrida existente NO la interrumpe (`closed == 0`); el arranque idempotente ejecutado dos veces cierra la corrida huérfana una sola vez (`closed_first=1, closed_second=0`, un solo evento terminal); duración máxima excedida transiciona a `failed/RUN_TIMEOUT` (nunca `interrupted`); heartbeat vencido transiciona a `interrupted/HEARTBEAT_EXPIRED`; y la demo de kill real de arriba. `ruff check .` y `pytest -m "not integration"` (167 passed, incluye 9 pruebas unitarias nuevas en `tests/test_t300_durability_unit.py`) en verde. Verificado manualmente además con `uvicorn` real: ciclo completo query→stream→respuesta, reconexión parcial con `Last-Event-ID`, `DELETE` de corrida terminada (204→404 idempotente) y `DELETE` de corrida activa (cancelación cooperativa observada en 0,54s, muy por debajo de `DELETE_ACTIVE_GRACE_S=5s`).
- [x] **T-301 Capa multi-proveedor LLM.** `app/llm/factory.py`: devuelve el chat model según `LLM_PROVIDER`/`LLM_MODEL` (google | anthropic), con structured output y conteo de tokens unificado (RF-206).
  - ✅ Implementado en `backend/app/llm/factory.py`: `get_chat_model`/`get_chat_model_from_settings` instancian `ChatGoogleGenerativeAI`/`ChatAnthropic` según `llm_provider`/`llm_model`, fallando temprano (antes de red) con `LLMConfigurationError` ante proveedor desconocido o API key ausente/vacía. `get_structured_chat_model` envuelve `with_structured_output` de forma uniforme (mismo código para ambos proveedores, sin ramas). **Decisión de diseño verificada leyendo el código fuente de ambos paquetes** (no asumida): `langchain-google-genai` y `langchain-anthropic` exponen el uso crudo en formatos distintos (`usage_metadata.prompt_token_count`/`candidates_token_count` en Google vs `usage.input_tokens`/`output_tokens` en Anthropic), pero ambos pueblan `AIMessage.usage_metadata` con el mismo esquema normalizado de LangChain — por eso `usage_from_message` lee siempre ese campo normalizado y no necesita ramas por proveedor. `estimate_cost_usd`/`sum_usage` calculan `estimated_cost_usd` con una tabla de precios USD/1M tokens versionada en el módulo (fecha de vigencia 2026-07-09, fuente `ai.google.dev`/`platform.claude.com`, fácil de extender para T-602) y agregan tokens/costo entre pasos de una misma corrida, produciendo valores compatibles con las columnas `llm_provider`/`llm_model`/`input_tokens`/`output_tokens`/`estimated_cost_usd` de `agent_runs`. `ainvoke_chat_model` traduce errores definitivos del SDK (`google.api_core.exceptions.GoogleAPICallError`, `anthropic.APIError`: cuota, auth, modelo inexistente) a `LLMProviderError`, distinguible para que T-303/T-304 lo mapeen a `LLM_PROVIDER_ERROR` (contracts/api-rest.md §6).
  - ✅ Prueba unitaria instancia ambos proveedores (con mock) y el conteo de tokens/costo funciona: `backend/tests/test_llm_factory.py`, 17 casos — instancia real de `ChatGoogleGenerativeAI`/`ChatAnthropic` con API keys falsas (sin red), proveedor desconocido y API key faltante fallan con `LLMConfigurationError`, `with_structured_output` funciona igual para ambos proveedores, `usage_from_message`/`estimate_cost_usd`/`sum_usage` normalizan tokens y calculan costo correctamente, modelo sin tarifa configurada falla con `LLMPricingError`, mezclar proveedores en `sum_usage` falla con `ValueError`. `ruff check .` y `pytest -m "not integration"`: **184 passed** (167 preexistentes + 17 nuevos), en verde.
- [x] **T-302 Implementar las herramientas T1–T5** (las ÚNICAS invocables por el enrutador LLM, `contracts/agent-tools.md`) en `app/tools/`, cada una con esquema Pydantic, sanitización (T4), guardia SoQL estructural (T5) y pruebas unitarias con HTTP mockeado (respx). `resolver_geografia` (T3) depende de T-202 y de que `divipola_entries` esté cargada con el maestro DIVIPOLA; no debe volver al arreglo hardcodeado de v1.0. La guardia usa gramática restringida propia en `app/tools/soql_parser.py`, canonicaliza la consulta, prohíbe `SELECT *`, limita `OFFSET`, valida alias/literales, aplica la guardia PII/elegibilidad antes de Socrata y mantiene lista blanca de funciones. Los nodos deterministas T6 y T7 NO van aquí: se implementan en T-401 y T-403.
  - ✅ Implementado en `backend/app/tools/`: `soql_parser.py` (lexer + parser recursivo-descendente propio para la gramática restringida: `SELECT/WHERE/GROUP BY/HAVING/ORDER BY/LIMIT/OFFSET`, funciones agregadas/escalares de lista blanca, predicados `LIKE`/`IS [NOT] NULL`, canonicalización estable, límites de complejidad, política PII `medium`, más lista negra de defensa en profundidad); `soda_client.py` (cliente SODA genérico con `$select/$where/$group/$having/$order/$limit/$offset`, mismo patrón timeout/reintento que `discovery_client.py`); `catalog_lookup.py` (lecturas/escrituras compartidas de `catalog_datasets`/`catalog_columns`); `buscar_catalogo.py` (T1, envuelve `search_catalog` de T-204 y añade columnas completas + descripción truncada a 300 chars); `perfilar_dataset.py` (T2, `asyncio.gather` para count/null-ratio/distinct-values concurrentes, pista de corte heurística por nombre/tipo de columna, cachea `null_ratio`/`sample_values` — nunca para PII `medium/high/unknown`); `resolver_geografia.py` (T3, similitud trigram sobre `name_normalized` + coincidencia exacta contra `alt_names` normalizados en Python ya que la base no tiene `unaccent`, `like_pattern` con vocales acentuables → `_`); `explorar_valores.py` (T4, sanitiza `'`/`%`/`_`/`\` antes de interpolar en `LIKE ... ESCAPE '\'`); `ejecutar_soql.py` (T5, solo llama a Socrata tras pasar la guardia).
  - ✅ **Decisiones de diseño documentadas** (no evidentes en el contrato, resueltas y anotadas en el propio código): códigos de error de rechazos estructurales (sentencias múltiples, cláusulas/funciones fuera de lista blanca, `SELECT *`, literales inválidos, complejidad, `OFFSET > 5000`, alias no resoluble en `ORDER BY`) unificados bajo `SOQL_FORBIDDEN`, reservando `SOQL_UNKNOWN_COLUMN` solo para columnas referenciadas que no existen en `catalog_columns`; `ORDER BY` solo resuelve contra alias declarado o columna presente en el propio `SELECT` (evita la ambigüedad entre ambos códigos de forma determinista); funciones agregadas prohibidas dentro de `WHERE` (inválido en SQL/SoQL); `rows` de T5 no se trunca a 50 (viaja completo hasta 1000 filas para la futura Evidencia de T6); `llm_view.rows_shown` es solo metadato para quien construya el mensaje hacia el LLM (T-303). Verificado contra Postgres real (no solo mocks): `resolver_geografia` corrido a mano contra los datos DIVIPOLA ya cargados resuelve "Bogotá"/"bogota"/"Bogotá D.C."/"Santafé de Bogotá" → `11001` y "Carmen de Viboral" → `05148`.
  - ✅ Suite de pruebas de herramientas en verde (casos de pruebas.md §2.1, no §4.1): `test_soql_guard.py` (29 casos: aceptación, todos los rechazos estructurales listados en pruebas.md, `SOQL_UNKNOWN_COLUMN` con columnas válidas, complejidad, defensa en profundidad, límites de `LIMIT`/`OFFSET`, canonicalización estable, elegibilidad/PII antes de Socrata, política PII `medium`), `test_divipola_geo.py` (8 casos — **nota de nomenclatura:** pruebas.md sugiere `test_divipola.py`, pero ya existe `tests/integration/test_divipola.py` de T-202 y pytest sin paquetes falla por colisión de nombre de módulo entre ambos; se optó por `test_divipola_geo.py`, dejando además `tests/integration/test_resolver_geografia.py` con los mismos casos contra Postgres real), `test_tools_buscar_catalogo.py` (4), `test_tools_perfilar_dataset.py` (5), `test_tools_explorar_valores.py` (7), `test_tools_ejecutar_soql.py` (8). `ruff check .` y `pytest -m "not integration"`: **245 passed** (184 preexistentes tras mergear T-301 + 61 nuevos), en verde.
- [x] **T-401 Implementar `app/quality/` (nodo determinista T6).** Exactamente según `contracts/validacion-calidad.md`: elegibilidad separada de calidad, 4 dimensiones, pesos, reglas duras, política PII `unknown`/`medium`/`high`, agregación mínima, inferencia de `data_cutoff_at` sobre las filas de la evidencia, fallback `data_updated_at_fallback` con mensaje correcto, salida ampliada (`eligibility_status`, razones, corte, política de filas), mensajes en `messages_es.py`, `validator_version`. Módulo puro, probado en aislamiento — no requiere el grafo.
  - ✅ Implementado en `backend/app/quality/`: `validator.py` (`validate_evidence(EvidenceDraft) -> QualityResult`, único punto de entrada, sin I/O), `cutoff.py` (`infer_data_cutoff` sobre las `rows` de la evidencia — nunca sobre `catalog_datasets`, busca la mejor columna temporal por nombre y confianza, año suelto se ancla a 31-dic), `placeholders.py`/`placeholders.yaml` (detección contextual: patrones genéricos versionados + códigos de codebook provistos por el llamador — nunca dígitos sueltos como `9`/`99` sin codebook, evitando falsos positivos), `messages_es.py` (mensajes en español claro, sin nombres de checks). Reutiliza `app.quality.eligibility.compute_column_eligibility` (T-201) para elegibilidad por columna combinada con las razones ya vigentes a nivel de dataset (el draft las recibe ya calculadas, T6 no vuelve a resolver publicador/PII) y `app.tools.soql_parser.parse_soql` (T-302) para `traceability.query_reproducible`, detectar columnas de uso numérico y el alias `count(*)` que verifica `aggregation_min_count >= 5` por fila en PII `medium`.
  - ✅ **Decisiones de diseño documentadas** (conflicto de precisión detectado en el propio contrato, resuelto por jerarquía documental — no silenciosamente): la tabla de `completeness.null_ratio` declara "Puntos: 60" pero su regla interna usa una escala 0-100 (`≤5%→100`, `≤20%→60`, `≤50%→30`, `>50%→0`); se interpretó esa escala como porcentaje del máximo de 60 (p. ej. `≤20%` → 60% de 60 = 36 puntos), sumado a `completeness.placeholder_values` (0 o 40 directos) — el ejemplo JSON del contrato (`score: 60`) parece una ilustración abreviada que omite el check de placeholders; se siguió el texto normativo por jerarquía (constitution.md > ... > contracts/), documentado en el docstring de `validator.py`. Además, `row_count == 0` fuerza `classification="no_recomendada"` (no solo D1=0): un `score_total` numérico con D1=0 pero las otras tres dimensiones perfectas daría exactamente 75="alta", contradiciendo "no se presenta como hallazgo". Los casos 11/14 del contrato (§5, resolución de alias/vigencia de publicador) ya están cubiertos por `test_publishers_vigencia.py`/`test_add_publishers.py`/`test_publisher_coverage.py` (T-106/T-201): T6 no resuelve publicadores, solo consume `eligibility_status`/`eligibility_reasons` ya calculados.
  - ✅ Los casos de prueba obligatorios del contrato (§5) pasan: `test_quality_validator.py` (31 casos — los 14 de §5, más fronteras explícitas de pruebas.md §2.1: edad 12/13/24/25/48/49 meses, `null_ratio` = 5,0% exacto, `score_total` = 74 vs 75, y verificación de que ningún `warning_user` menciona nombres técnicos de checks), `test_quality_cutoff.py` (6), `test_quality_placeholders.py` (6). `ruff check .` y `pytest -m "not integration"`: **288 passed** (245 preexistentes tras mergear T-301/T-302 + 43 nuevos), en verde.
- [x] **T-403 Módulo de afirmaciones cuantitativas (nodo determinista T7, RF-208).** `app/quality/claims.py` según `contracts/agent-tools.md` T7: DSL JSON de fórmulas, evaluador seguro, formato es-CO de `display_value`, canonicalización de `source_hash` como hash de contenido reproducible entre corridas (sin `evidence_id`, `claim_id` ni `run_id`), definición normativa de “cifra”, persistencia en `quantitative_claims`, y verificador de cifras huérfanas para el texto del sintetizador. Módulo puro, probado en aislamiento.
  - ✅ Implementado en `backend/app/quality/claims.py` (mismo patrón dataclass-puro de `validator.py`, T-401: no es el sobre `{"ok": ...}` de T1-T5 porque T6/T7 son nodos internos del grafo, no herramientas invocables por el LLM): `ClaimSpec`/`EvidenceContext` de entrada, `build_claims` devuelve `ClaimsBuildResult(claims, rejected)`. Evaluador seguro del DSL (`_eval_node`/`_resolve_col`/`_resolve_agg`/`_apply_op`, sin `eval`/`exec`, lista blanca cerrada de nodos `col`/`const`/`agg`/`op`); `format_es_co` (coma decimal, separador de miles); `compute_source_hash` (JSON canónico UTF-8, claves ordenadas, sin espacios, sobre exactamente los 9 campos del contrato); `find_figures`/`find_orphan_figures` (detector de cifras huérfanas con enmascarado previo de UUID/id de dataset/fechas ISO y largas, más heurísticas de código con cero a la izquierda y referencia de sección).
  - ✅ **Decisiones de diseño documentadas** (no evidentes en el contrato, resueltas en el docstring del módulo): (1) resolución de `{"col": "<columna>"}` cuando `source_row_indexes` referencia más de una fila — el propio ejemplo de T7 en el contrato usa dos filas con `col` simple sin agregación; se adoptó que `col` recolecta valores NO NULOS entre las filas referenciadas y exige que exista exactamente UN valor distinto (cubre tanto el caso de una sola fila como el ejemplo del contrato, donde cada columna tiene un único valor no nulo entre las filas); más de un valor distinto exige `agg` explícito, cero valores no nulos se rechaza como "operando nulo" — las agregaciones (`sum/avg/min/max/count`) sí exigen que TODAS las filas referenciadas tengan valor (nunca descartan datos en silencio, Art. I). (2) `ratio` se implementa idéntico a `div` (a/b, división por cero rechazada): el contrato no define una semántica distinta y no se inventa una (Art. III). (3) `compute_source_hash` ordena `source_row_indexes` ascendentemente de forma intrínseca (no depende de que el llamador lo haga): bajo este DSL el orden nunca afecta `raw_value` (`col` es búsqueda de valor único, las agregaciones son conmutativas), por lo que reordenar los índices de entrada no es "semánticamente relevante" y produce el mismo hash, mientras que cambiar el CONTENIDO de las filas referenciadas o el conjunto de índices sí lo cambia (pruebas.md §2.1).
  - ✅ Casos de pruebas.md §2.1 (claims) en verde en `backend/tests/test_claims.py` (29 casos): claim `direct` con formato es-CO, claim `derived` con división/porcentaje reproduciendo el ejemplo literal del contrato (8.3721 con rounding=1 ⇒ "8,4 %"), el propio ejemplo de dos filas de T7 con `col` simple, `sum()`/`avg()`, los 4 rechazos exigidos (columna inexistente, operando no numérico, operando nulo, división por cero) más operación DSL/agregación no permitida y columna no declarada; detector de cifras huérfanas (definición normativa completa: enteros, decimales coma/punto, porcentajes, monedas, miles, rangos, años analíticos; exclusión de UUID, id de dataset, fecha completa, DIVIPOLA, número de sección) en caso positivo y negativo; estabilidad de `source_hash` entre corridas independientes con el mismo contenido (simulando UUIDs de corrida/evidencia/claim distintos, que `compute_source_hash` ni siquiera recibe como parámetro), cambio de fórmula/fila/`raw_value`/unidad/redondeo cambia el hash, y reordenar `source_row_indexes` no lo cambia (orden no semánticamente relevante bajo este DSL) mientras que referenciar un conjunto distinto de índices sí. `ruff check .` y `pytest -m "not integration"`: **317 passed** (288 preexistentes tras mergear T-401 + 29 nuevos), en verde.
- [x] **T-303 Integrar el grafo del agente.** `app/agent/graph.py` según plan.md §1: planificador → enrutador → herramientas T1–T5 (T-302) → nodo validador T6 (T-401) → nodo constructor de afirmaciones T7 (T-403) → sintetizador restringido (solo `display_value` de claims; cifras huérfanas bloquean y fuerzan re-síntesis, máx. 2 intentos, luego `failed`), con presupuestos del contrato de tools, checkpointer PostgreSQL, heartbeat, eventos numerados en `agent_run_events` y persistencia de `agent_runs`/`agent_steps` (RF-703, RF-209), sobre la base del PoC de T-300. Prompts en `app/agent/prompts/`, versionados.
  - ✅ **Reabierta y vuelta a cerrar (2026-07-10, research.md §14):** una verificación independiente encontró que `explorar_valores` (T4, T-302) construía `LIKE ... ESCAPE '\'`, sintaxis SQL estándar que la gramática SoQL de Socrata NO soporta (`400 query.compiler.malformed`); T4 nunca había funcionado contra el servicio real, enmascarado antes por el bug de `base_url`. Corregido (se retira la cláusula `ESCAPE`; Socrata trata `\` como escape por defecto, verificado empíricamente). Añadido `tests/integration/test_explorar_valores_live.py` (2 casos contra Socrata real) y regresión en `test_tools_explorar_valores.py` que verifica que el `$where` real nunca contiene `ESCAPE`. Reverificado con una corrida real que ejercita `explorar_valores` end-to-end (`tool:explorar_valores` → `{"ok": true}` contra Socrata real).
  - ✅ Implementado en `backend/app/agent/graph.py` (`build_graph`, `GraphDependencies`, `PlannerOutput`/`RouterOutput`/`SynthesisOutput`), `backend/app/agent/persistence.py` (persistencia transaccional de pasos/eventos/evidencia+calidad/claims), prompts versionados en `backend/app/agent/prompts/{planner,router,synthesizer}_v1.md`, CLI real en `backend/scripts/run_agent.py`. El PoC de T-300 queda aislado (`start_toy_run_task`/`build_toy_graph`), sin eliminarse (sus pruebas de durabilidad siguen vigentes); `start_run_task` pasa a significar el grafo real.
  - ✅ **Criterio literal verificado con ejecución real** (Postgres + Gemini + Socrata reales, sin mocks): la pregunta *"¿Cuál fue el promedio de deserción escolar en el departamento de Antioquia entre 2018 y 2022, según el Ministerio de Educación?"* contra el dataset real `ji8i-4anb` (MEN) produjo `status=completed`, `claims[]` con 2 elementos aceptados (`build_claims`/T7), `quality.classification=alta`/`score_total=100`, en 7 pasos (≤10), con `synthesis_attempts=2` — el reintento por cifras huérfanas (`find_orphan_figures`) se activó realmente sobre un intento con cifra huérfana y lo corrigió antes de aceptar la respuesta final, demostrando RNF-003 en runtime, no solo en pruebas guionadas. Matar el proceso a mitad de corrida (T-300, ya verificado) deja `interrupted/RUN_INTERRUPTED` coherente al reiniciar.
  - ✅ **Bloqueo original y su resolución (research.md §13):** la primera ejecución real con la pregunta original de ESC-02 ("Oriente antioqueño") terminó honestamente en `no_evidence` — correcto per Art. I (nunca se relajó PII ni se fabricó una dimensión territorial inexistente), pero reveló, al ejecutar contra datos reales, cuatro problemas invisibles en pruebas con mocks: (1) el clasificador PII casi nunca resolvía `low` (0 de 8.418 datasets `eligible` en el catálogo real) por un bug de anclas en `pii_classifier.py`; (2) `httpx.AsyncClient()` sin `base_url` en `runner.py` hacía que TODA llamada real a Socrata fallara instantáneamente, indistinguible de `SOCRATA_TIMEOUT`; (3) `_count_alias` en `validator.py` exigía alias explícito en `count(*)`, bloqueando evidencia PII `medium` genuinamente agregada; (4) `ClaimSpecPayload.claim_type` como `str` libre permitía que el LLM alucinara valores inválidos. Las cuatro correcciones están documentadas con justificación en research.md §13 y cubiertas con pruebas de regresión (`test_pii_classifier.py`, `test_quality_validator.py`). La pregunta original sobre "Oriente antioqueño" queda como seguimiento separado (falta fixture de subregiones DIVIPOLA), no bloquea T-303.
  - ✅ `ruff check .` y `pytest -m "not integration"`: 332 passed (1 fallo preexistente de aislamiento de entorno local ajeno a estos cambios, ver research.md §13); `pytest -m integration -k "t303 or explorar_valores_live"`: 3 passed contra PostgreSQL/Socrata reales. Pruebas nuevas: `test_agent_graph.py`, `test_t303_agent_graph.py`, `test_explorar_valores_live.py` (integración), regresiones en `test_pii_classifier.py`/`test_quality_validator.py`/`test_tools_explorar_valores.py`. CI Backend/Frontend en verde (PR #22).
- [x] **T-304 Endpoints del agente.** `POST /v2/agent/query` (endpoint público siempre `retention_class=user`, emite `run_access_token` una sola vez y guarda solo el hash), `GET /v2/agent/stream/{run_id}` (SSE con `id:` de secuencia, `Last-Event-ID`, autorización Bearer y evento terminal con `status`), `GET /v2/agent/runs/{run_id}` (Bearer, esquemas para `running`, `completed`, `no_evidence`, `interrupted`, `failed`) y `DELETE /v2/agent/runs/{run_id}` (borrado RF-803 + `adelete_thread`, incluida corrida activa), según el contrato REST; rate limiting global por proceso; cierre de corridas huérfanas y timeout según plan.md §11. Si un acceso encuentra corrida vencida, ejecuta borrado oportunista y responde `404 RUN_NOT_FOUND`.
  - ✅ **Evidencia real 2026-07-10:** `uvicorn app.main:app` + `curl.exe -N` con Bearer recibió `id: 1`; tras cortar el cliente y reconectar con `Last-Event-ID: 1` recibió exactamente `id: 2`…`id: 11`, cero duplicados y terminal `event: answer`. El mismo stream sin Bearer devolvió `401`. El borrado devolvió `204`, el `GET` posterior `404` y `SELECT count(*) FROM checkpoints WHERE thread_id = <run_id>` devolvió `0`. `pytest -m integration -k t304`: `1 passed`.
  - ⚠️→✅ **Reabierta en revisión independiente (2026-07-11, PR #23) y cerrada tras el fix:** `research.md` §4 y `contracts/api-rest.md` §7b exigen copiar métricas agregadas no identificables a `technical_metrics` **antes** del borrado, tanto para el `DELETE` de RF-803 como para el borrado oportunista por expiración — no solo para el barrido periódico de T-306. La implementación original de PR #23 no lo hacía (`grep -rn "TechnicalMetric(" backend/app/` no encontraba ningún escritor). **Corregido:** `durability.delete_run_with_metrics` (`app/agent/durability.py`) copia `run_month`/`source_run_hash = sha256(run_id + RETENTION_HASH_SALT)`/`retention_class_origin`/`status_final`/`llm_provider`/`llm_model`/`steps_used`/`latency_ms`/`input_tokens`/`output_tokens`/`estimated_cost_usd`/`deletion_reason` (`user_requested` | `retention_expired`) e inserta con `ON CONFLICT (source_run_hash) DO NOTHING` (idempotente ante reintentos) en la MISMA transacción que borra `agent_runs`; `_delete_run_async` (`app/main.py`) la invoca tras borrar checkpoints. Los 3 llamadores (`require_run_access` oportunista, `delete_agent_run` RF-803, `poc_delete_agent_run` T-300) propagan `deletion_reason`/`retention_hash_salt`; falta `RETENTION_HASH_SALT` configurado → `500 INTERNAL` claro (`_require_retention_hash_salt`), nunca borrado silencioso sin métricas. **Evidencia real 2026-07-11:** corrida real completa (pregunta sobre cobertura educativa en Antioquia) → `DELETE` → `204` → `SELECT * FROM technical_metrics WHERE source_run_hash = sha256(run_id+salt)` devolvió la fila esperada (`run_month=2026-07`, `retention_class_origin=user`, `status_final=no_evidence`, `llm_provider=google`, `llm_model=gemini-2.5-flash`, `latency_ms=18308`, `estimated_cost_usd=0.015833`, `deletion_reason=user_requested`); `agent_runs`/`checkpoints` en `0`. `ruff check .` limpio; `pytest -m "not integration"`: 344 passed (1 fallo conocido preexistente de `test_llm_factory`); `pytest -m integration -k "t300 or t303 or t304 or explorar_valores_live"`: 15 passed. Prueba nueva: `test_delete_without_retention_hash_salt_fails_clearly`; `test_t304_agent_endpoints.py` extendida para verificar la fila de `technical_metrics` tras el borrado.
- [x] **T-306 Job de retención y purga.** Implementado RF-804 como servicio/CLI idempotente del backend y endpoint admin `POST /v2/admin/retention/run`: corridas `user`, corridas `eval`, `technical_metrics` vencidas, borrado oportunista compartido con T-304, copia atómica de métricas con `source_run_hash = sha256(run_id + RETENTION_HASH_SALT)`, snapshot eval, `adelete_thread(run_id)`, borrado físico y reintento idempotente ante fallos. La ejecución única usa `pg_try_advisory_xact_lock(20260707, 804)` y responde `already_running` sin efectos; quickstart documenta CLI/endpoint externo cada 6 horas con SLA máximo de 24 h.
  - ✅ Evidencia real 2026-07-10: `uvicorn app.main:app --reload --port 8016` + `curl.exe -X POST -H "X-Admin-Token: …" /v2/admin/retention/run` sobre una corrida `user` y una `technical_metrics` vencidas devolvió `202 {"status":"accepted","reason":null,"planned":{"user_runs":1,"eval_runs":0,"technical_metrics":1},"executed":{"user_runs":1,"eval_runs":0,"technical_metrics":1}}`; conteos dirigidos antes/después: `agent_runs 1→0`, métrica vencida `1→0`, copia `retention_expired` estable `0→1`. `python scripts/run_retention_sweep.py` reutiliza el servicio y devolvió cero pendientes. `tests/integration/test_retention_sweep.py`: 4 passed contra PostgreSQL real (reloj simulado: `user`/`eval` + snapshot `SET NULL`, purga de métricas, lock concurrente real, endpoint `already_running` y reintento tras fallo simulado de checkpoints); `ruff check .` limpio. `pytest -m "not integration"`: 346 passed, 1 fallo conocido de `test_llm_factory` por `ANTHROPIC_API_KEY` local (research.md §13), no regresión de T-306.
- [x] **T-305 Casos de honestidad.** Verificado manualmente ESC-03 (RF-205, RNF-005, Constitución Art. I.3): ante preguntas sin respuesta en el catálogo, el agente produce `no_evidence_report` sin cifras inventadas.
  - ✅ Evidencia real 2026-07-11 (Postgres + Gemini + Socrata, sin mocks): las tres semillas candidatas para T-601 — (1) *"¿Cuál es el precio promedio del arriendo en Marte en 2025 según datos.gov.co?"* (`run_id=3730b8ae-907d-4038-b416-18c4a77b90b5`, 7 pasos), (2) *"¿Cuántos volcanes activos hay en Venus según datos.gov.co?"* (`run_id=a6e4a5a3-9508-47da-a7cf-2e9fda363f75`, 7 pasos) y (3) *"¿Cuál fue el número de avistamientos de sirenas en Colombia en 2025 según datos.gov.co?"* (`run_id=ae5c6fe6-c3b4-47a6-8162-e367ab3e772d`, 5 pasos) — terminaron `status=no_evidence`, `claims=[]`, `evidence=[]`, `narrative=null` y `no_evidence_report` con razón y sugerencias de reformulación. Las cifras de años solo reproducen la pregunta; no se presentó ninguna cifra como respuesta. El intento adicional sobre "estaciones de teletransporte" no se cuenta: falló antes de sintetizar con `LLM_PROVIDER_ERROR` por `RouterOutput.reasoning_summary` >500 caracteres; requiere seguimiento separado, no se ocultó como caso exitoso.

## FASE 4 — Verificación integrada de las capas deterministas (RF-401…404, RF-208)

*Nota de reordenamiento: T-401 y T-403 se ejecutan dentro de la Fase 3 (antes de T-303) porque el grafo los integra; se conservan sus IDs. Esta fase verifica la integración de punta a punta con datos reales.*

- [x] **T-402 Verificación integrada de calidad y claims.** Con el grafo completo (T-303): el nodo `quality_validator` corre SIEMPRE tras cada `ejecutar_soql` (imposible saltarlo); el objeto `quality` viaja en cada Evidencia; el sintetizador ajusta la narrativa según la clasificación (`baja` ⇒ mencionar limitación; `no_recomendada` ⇒ evidencia no elegible como sustento); los claims heredan las advertencias de su evidencia fuente.
  - ✅ **Cierre formal 2026-07-18 (rama `feat/t402-formal-closure`, HEAD base `d8b9c51`):** acta `backend/eval/reports/t402-integrated-quality-claims-closure.md`. Se reprodujo la evidencia sobre el HEAD actual y se cerraron los tres huecos de prueba restantes en `backend/tests/integration/test_t402_quality_claims_integration.py` (sin tocar código productivo): (A) una corrida con **dos T5 exitosos ⇒ exactamente dos pasos `quality_validator`** (`test_dos_t5_en_una_corrida_producen_dos_t6_y_persistencia_1a1`), demostrando que la arista fija `_after_t5` no depende del LLM; (B) conteo **1:1 `evidence_results`/`quality_reports`** con helpers `_evidence_quality_counts` y `_evidence_ids_without_exactly_one_quality`; (D/G) cadena `claim.evidence_id → evidence.evidence_id → evidence.quality.warnings_user` verificada sobre la **respuesta final** (extensión de la prueba baja). Invariantes A–G confirmados; `blocked` cubierto en `tests/test_quality_validator.py` (casos 12) y comparte la ruta de exclusión de claims con `diagnostic_only`.
  - ✅ **Corrida viva real** (`execute_legacy_agent_run_async`, Postgres real + Gemini + Socrata): `run_id=3ff03835-0512-4f2a-a6b6-fd8b0f927ed0`, dataset `v8jr-kywh` (MinMinas, `verified`/`low`/`eligible`), SoQL `SELECT sum(cantidad_volumen_suministrado), count(*) GROUP BY tipo_de_combustible LIMIT 1000`, `alta`/`score=100`, T5=1→T6=1, 1 claim (`1.715.000.807`) vinculado a la evidencia, persistencia 1:1:1, `completed`, un único terminal (`answer`), cero cifras huérfanas.
  - ✅ Verificación: `pytest tests/test_agent_graph.py tests/test_quality_validator.py tests/test_claims.py -q` 88 passed; `pytest tests/integration/test_t402_quality_claims_integration.py -q` 5 passed; `pytest -m "not integration" -q` 869 passed; `ruff check .` limpio; `ruff format --check` del archivo modificado limpio. `golden-v1`/`golden-v2`/`metrics.py`/contratos/runtime intactos; T-617 no iniciada.
  - ⚠️ Progreso 2026-07-11 (rama `feat/t305-honesty-cases`, aún no es PR propia): la verificación integrada expuso un defecto real, ya corregido, y queda pendiente la parte de datos en vivo de la estrategia original.
    - **Defecto encontrado y corregido:** `_claim_builder_node` (`backend/app/agent/graph.py:744-749`) filtraba evidencia solo por `eligibility_status != "eligible"`, sin revisar `classification`. Una evidencia de publicador verificado/PII baja (`eligible`) con `score_total < 35` (p. ej. fuente incompleta) podía sustentar un claim igual — viola contracts/validacion-calidad.md §3.1/§6.2 y RF-404. Fix: se agrega el filtro `classification == "no_recomendada"`.
    - **Prueba unitaria** (`backend/tests/test_agent_graph.py::test_claim_builder_skips_eligible_no_recomendada_evidence` + control positivo `test_claim_builder_builds_claims_for_eligible_alta_evidence`): confirmado que falla sin el fix (revertido con `git stash` para comprobarlo) y pasa con él.
    - **Matriz de integración real** (`backend/tests/integration/test_t402_quality_claims_integration.py`, Postgres real vía Docker, router/sintetizador guionados, 4 pruebas, todas verdes): `alta` → claims reproducibles, cero cifras huérfanas, `synthesis_attempts=1`; `baja` (corte >48 meses + columna mayormente vacía, ESC-05) → `warnings_user` no vacío, SIGUE sustentando claims (RF-402: `baja` no es regla dura), y se prueba que la advertencia llega intacta al payload que ve el sintetizador (`synthesizer.received_warnings`); `no_recomendada` con evidencia `eligible` (fuente incompleta, `source_url` ausente) → aunque el router pide un claim sobre ella, `claim_builder` lo rechaza y la corrida termina en `no_evidence` — regresión confirmada revirtiendo el fix contra Postgres real; `diagnostic_only` (publicador no verificado) → T5 se ejecuta igual mas no sustenta claims. Las 4 pruebas verifican además que `quality_validator` corrió exactamente una vez y que `evidence_results`/`quality_reports` quedan persistidos para toda evidencia, tenga o no claims.
    - **Hallazgo (resuelto vía prompt, con límite documentado):** al construir el escenario `baja` se detectó que si la narrativa del sintetizador repite literalmente una cifra de `warnings_user` (p. ej. "60% de registros vacíos"), `_orphan_figures` la bloquea como cifra huérfana — solo acepta números que coincidan con `claims[].display_value`. Tensión real entre RF-402/403 (mostrar la advertencia) y RF-208/Art. I.1 (solo citar cifras respaldadas por claims), no cubierta explícitamente por el contrato. Resuelto reforzando `backend/app/agent/prompts/synthesizer_v1.md` (ver siguiente punto) para pedir parafraseo cualitativo — no se tocó `_orphan_figures`/su lista blanca, así que la garantía dura sigue siendo "ninguna cifra sin `display_value`", nunca "el LLM real obedecerá el prompt". Eso último solo lo confirma el conjunto dorado con LLM real (Art. IV.2), no una prueba determinista de T-402.
    - **Refuerzo del prompt del sintetizador (2026-07-11):** `backend/app/agent/prompts/synthesizer_v1.md` ya no dice solo "explica limitaciones en español claro"; ata la regla a `quality.classification` de cada evidencia: `baja` ⇒ la narrativa de esa evidencia DEBE mencionar la limitación de `warnings_user` parafraseada en términos cualitativos (nunca repitiendo cifras/fechas/porcentajes textuales, con ejemplo concreto de qué sí y qué no escribir); `no_recomendada` ⇒ nunca presentarla como sustento. Prueba de regresión determinista: `backend/tests/test_agent_graph.py::test_synthesizer_prompt_ties_narrative_limitation_to_classification`, que falla si alguien vuelve a simplificar el prompt a la instrucción genérica anterior. `ruff check .` limpio; `pytest -m "not integration"` 349 passed (1 fallo preexistente no relacionado en `test_llm_factory.py`, documentado, no causado por este cambio); `pytest -m integration` 60 passed/2 skipped sin regresiones (los sintetizadores guionados de la matriz de integración no leen el prompt, así que no dependían de este cambio).
    - **Comprobaciones con datos reales en vivo (2026-07-11, Socrata + Gemini reales, sin mocks):** se sembraron en el Postgres local, con el código real de ingesta (`app.catalog.ingest._process_item`, no una versión reescrita) y embeddings reales (`gemini-embedding-2`), dos datasets reales de datos.gov.co: `2pnw-mmge` ("Evaluaciones Agropecuarias Municipales EVA", Ministerio de Agricultura, `publisher_verification_status=verified`, `pii_risk_level=low`, `eligibility_status=eligible`, `data_updated_at=2019-11-24`) para el caso ESC-05 de corte antiguo, y `sras-4t5p` (estadísticas de deserción escolar del MEN, `verified`/`medium`/`eligible`, `data_updated_at=2025-11-13`) para el caso positivo. De paso, `t2ca-uae5` (Gobernación de Antioquia, producción agrícola) se descartó como candidato porque el clasificador PII real lo dejó `blocked`/`pii_unknown` — confirmación en vivo de que el default seguro del Art. VI.1 sigue operando sobre datos reales nunca antes vistos por el fixture.
      - Con el backend real (`uvicorn`, `LLM_PROVIDER=google`, `LLM_MODEL=gemini-2.5-flash`) corriendo local y `POST /v2/agent/query` real, se ejecutaron 16 corridas de punta a punta contra estos dos datasets (run_ids documentados en el historial de esta sesión, p. ej. `15923f8e…`, `f8ac0f8a…`, `3e9bdc54…` para el caso viejo y `b79cefba…`, `64d0056e…`, `421dabbb…` para el positivo).
      - **Lo que sí quedó demostrado end-to-end con datos y LLM reales (el objetivo central de T-402):** el mecanismo de T6 funciona correctamente sin mocks. Contra `2pnw-mmge` con una consulta real filtrada a un año histórico (2010, dentro del rango real del dataset), la corrida real ejecutó T5 contra Socrata, obtuvo filas reales, y `quality_validator` calculó `timeliness.score=15`, `basis=data_updated_at_fallback`, `"Corte de actualización: 2019-11-24 (~79 meses)"` — la advertencia ESC-05 se generó correctamente a partir de una fuente oficial real, sin datos guionados. Contra `sras-4t5p` (fresco, `~7 meses`) el mismo mecanismo calculó `timeliness.score=100`, confirmando el contraste correcto entre fuente vieja y fuente fresca.
      - **Lo que NO se logró en esa primera pasada: ninguna de las 16 corridas terminó en `status=completed`** (todas en `no_evidence` o `failed`), por 4 patrones de fricción real del router LLM/T7 — ninguno era el defecto de `no_recomendada` ya corregido (ese fix se verificó aparte, de forma determinista, en la matriz de arriba).

- [x] **Investigación de robustez del router T-303 (2026-07-11, mismo día, misma rama).** Los 4 patrones de fricción de la sección anterior se investigaron a fondo, se corrigieron y se reconfirmaron con LLM real. Alcance deliberadamente separado de T-402 (esto es T-303/T7, no T6), pero se documenta aquí porque nació de las comprobaciones en vivo de T-402.
  1. **`RouterOutput.reasoning_summary`/`PlannerOutput.recommended_next_action` exceden max_length y tumban la corrida.** Causa raíz: `with_structured_output(..., include_raw=True)` (real en `runner.py`) valida con Pydantic y, si falla, antes se descartaba el `AIMessage.tool_calls[0]["args"]` crudo y se lanzaba `LLMProviderError` sin ningún reintento (a diferencia de la corrección de SoQL o de síntesis, que sí reintentan). Fix: `app/llm/factory.py` — `ainvoke_structured_chat_model` acepta `schema` opcional; si `parsing_error` es exclusivamente `string_too_long` en campos de primer nivel, trunca esos campos al `max_length` declarado y revalida localmente, sin gastar una llamada adicional al proveedor. Deliberadamente conservador: si algún error no es `string_too_long`, no se toca nada. Pruebas: `backend/tests/test_llm_factory.py` (recupera con `schema`, no recupera sin `schema`, no recupera un campo faltante, no recupera si hay un error mixto).
  2. **SoQL con cláusula FROM alucinada.** Causa raíz doble: (a) la gramática no reconoce FROM como palabra clave, así que el rechazo real llegaba como un error genérico de tokenización ("carácter no reconocido" en el guion del id del dataset) o de parseo ("token inesperado"), sin decirle al LLM cuál fue el error real — en ejecución real esto le tomó varios intentos fallidos (probó comillas, guion bajo) autocorregirse; (b) `SOQL_FORBIDDEN` nunca contaba para el presupuesto de correcciones (`soql_corrections`, solo `SOQL_SYNTAX` lo hacía), así que el router podía repetir el mismo error hasta agotar el presupuesto general de pasos en vez de cortar en 3 intentos como `SOQL_SYNTAX`. Fix: `app/tools/soql_parser.py` — `parse_soql` detecta la palabra FROM fuera de literales string tras un rechazo y da un mensaje específico y accionable; `app/agent/graph.py` — `_CORRECTABLE_SOQL_ERROR_CODES` ahora incluye `SOQL_FORBIDDEN`/`SOQL_UNKNOWN_COLUMN` además de `SOQL_SYNTAX`; `router_v1.md` gana una regla explícita ("SoQL nunca usa FROM"). Pruebas: `backend/tests/test_soql_guard.py` (mensaje accionable contra los 4 SoQL reales que fallaron en ejecución, y que un FROM dentro de un literal string no dispara un falso positivo), `backend/tests/test_agent_graph.py::test_soql_forbidden_also_allows_only_two_corrections` (antes agotaba `MAX_SOQL_CALLS=4`, ahora corta en 3 como `SOQL_SYNTAX`).
  3. **`explorar_valores` (T4) contra columnas numéricas.** Causa raíz: el contrato (agent-tools.md §T4) ya decía "columna de texto", pero la función envolvía CUALQUIER columna en `upper(...) LIKE ...` sin validar `catalog_columns.data_type`; Socrata rechaza eso con `query.soql.type-mismatch`, un error de uso de herramienta (no de sintaxis) que el LLM no sabe autocorregir y que agotaba el presupuesto de pasos repitiendo la misma llamada rota. Fix: `app/tools/explorar_valores.py` valida `data_type` (comparación insensible a mayúsculas — se confirmó en datos reales que la Discovery API no sigue una convención única) antes de llamar a Socrata y rechaza con el nuevo código `EXPLORAR_VALORES_NOT_TEXT` sin gastar la llamada; requiere `engine` (antes no lo tenía), ya cableado en `runner.py`. Contrato actualizado en el mismo cambio (`contracts/agent-tools.md` §T4). Pruebas: `backend/tests/test_tools_explorar_valores.py` (columna numérica y de fecha rechazadas sin llamar a Socrata, columna inexistente, insensibilidad a mayúsculas) y `backend/tests/integration/test_explorar_valores_live.py` actualizada para sembrar la fila real de catálogo que ahora exige.
  4. **DSL de claims derivados rechaza formas inventadas por el LLM.** Causa raíz: mismo patrón que el hallazgo ya corregido de T-303 sobre `claim_type` (documentado en el propio código) pero sin aplicar a `formula`, que seguía siendo `dict[str, Any]` sin ninguna restricción de forma — el LLM inventaba shapes fuera de la DSL real de `app/quality/claims.py` y `build_claims` los rechazaba en tiempo de ejecución tras gastar un paso completo. Fix: `app/agent/graph.py` — `FormulaNode` es ahora una unión discriminada recursiva (`ConstFormulaNode | ColFormulaNode | AggFormulaNode | OpFormulaNode`, cada una con `extra="forbid"`) que refleja exactamente las 4 formas que `_eval_node` reconoce; `ClaimSpecPayload.formula: FormulaNode | None`. Limitación verificada y documentada: el modelo Python acepta recursión sin tope, pero el esquema de function-calling que ve el LLM (`convert_to_openai_tool`) corta la expansión en profundidad 1 dentro de `OpFormulaNode.args` — cubre "sumar una columna sobre varias filas" (el caso real que falló), no "operación de operación". Pruebas: `backend/tests/test_agent_graph.py` (acepta las 5 formas válidas incluyendo `op` anidado a nivel de `model_validate`, rechaza 4 formas inventadas).
  - **Confirmación con LLM real (no solo pruebas unitarias), mismos dos datasets reales que la sección anterior:** dos corridas de re-verificación contra un backend real nuevo. La corrida "positiva" (deserción escolar MEN) ya no repitió ninguno de los 4 patrones (SoQL limpio sin FROM, sin caída por longitud, sin mal uso de `explorar_valores`) y terminó en `no_evidence` por una razón legítima **nueva**, no un bug: el dataset tiene `pii_risk_level=medium` y una fila por ciudad/año no cumple `aggregation_min_count>=5`, así que el router correctamente se abstuvo de construir un claim y lo explicó en `no_evidence_report` — comportamiento honesto esperado (Art. I.3), no algo a corregir aquí. La corrida "ESC-05" (EVA/Ministerio de Agricultura, papa en Boyacá 2010) llegó mucho más lejos que cualquiera de los 16 intentos originales: ejecutó un SoQL agregado limpio (`SELECT sum(rea_cosechada_ha)..., sum(producci_n_t)... WHERE departamento='BOYACA' AND cultivo='PAPA' AND a_o=2010`, sin FROM), `quality_validator` calculó `timeliness.score=15` sobre datos reales, y `claim_builder` construyó dos claims reales (`47.924 hectáreas`, `834.732 toneladas`) sin tocar la DSL `derived`/`agg` porque el propio SoQL ya pre-agregaba a una fila. La corrida terminó bloqueada en el sintetizador por cifras huérfanas — pero la única cifra huérfana fue **"2010"**, el año de la pregunta, capturado por una regla ya existente y documentada (`synthesizer_v1.md`: "un año analítico también es una cifra"; Art. I.1). Es un comportamiento conservador preexistente, no un hallazgo nuevo de esta investigación, y queda fuera de este alcance.
  - Verificación: `ruff check .` limpio; `pytest -m "not integration"` 372 passed (1 fallo preexistente no relacionado); `pytest -m integration` 60 passed/2 skipped. Los datasets reales sembrados para las corridas en vivo se removieron de Postgres al terminar (dejarlos rotos otras suites de integración por FK con `official_publishers`, confirmado y corregido durante esta misma verificación); lo que queda como evidencia durable son los `run_id` documentados y este texto, no las filas de catálogo.
    - **Pendiente para cerrar T-402:** evidencia consolidada y PR hacia `v2`.

- [ ] **Investigación T-403 "el agente no funciona" (2026-07-11, sesión nueva, `docs/prompt-t403-agente-no-funciona.md`).** Diagnóstico previo (misma fecha) sobre `eval_runs.id=3097ac89-...` (`success_rate=0.18`, 0/40 positivos `completed`) más una reproducción real aislada (`run_id=32ad6d77-...`, dataset `4hyg-wa9d` encontrado, SoQL correcto, T6 `eligible`/91, T7 rechazó los 3 claims). Esta entrada corrige, con pruebas deterministas, la causa raíz confirmada por esa reproducción y dos causas contiguas del mismo cuello de botella router→claims. **No se corrió el conjunto dorado ni ninguna corrida con LLM real en esta sesión** (riesgo real de otra sesión trabajando en paralelo sobre el mismo Postgres local, detectado por un archivo nuevo no rastreado en `docs/`) — por eso queda `[ ]`, no `[x]`: falta la reconfirmación con LLM real que exige Art. IV.2 antes de dar esto por cerrado.
  1. **Repair loop LLM-asistido para salida estructurada inválida.** Causa raíz: `ainvoke_structured_chat_model` solo recuperaba `string_too_long` localmente; cualquier otro error de validación (campo obligatorio ausente, forma de nodo inventada) lanzaba `LLMProviderError` de inmediato sin darle al modelo oportunidad de corregirse, a diferencia de las correcciones de SoQL/síntesis que sí reintentan. Fix: `app/llm/factory.py` — hasta `MAX_STRUCTURED_REPAIR_ATTEMPTS=2` reintentos reales contra el mismo modelo, devolviéndole el JSON inválido y los errores de Pydantic condensados (`_repair_request_message`); solo se activa si `input` es una lista de mensajes y hay `schema`. El `usage_metadata` de los intentos fallidos se suma al mensaje final (`_merge_usage_metadata`) para que el conteo de tokens/costo de la corrida sea real (RNF-009, Art. VII.3), no solo el del último intento. Pruebas: `backend/tests/test_llm_factory.py` (`test_repairs_missing_required_field_via_second_attempt`, `test_repair_loop_sums_usage_metadata_across_failed_attempts`, `test_repair_loop_gives_up_after_max_attempts`, `test_repair_loop_does_not_engage_without_message_list_input`; las pruebas previas de "no recupera" se renombraron para aclarar que describen solo la recuperación local, no el repair loop nuevo).
  2. **`explorar_valores` sin `termino_busqueda` (campo obligatorio del contrato).** Causa raíz confirmada en la reproducción en vivo (`INVALID_INPUT`, `"termino_busqueda: Field required"`, run `fea461c4-...` del prompt de diagnóstico): `RouterToolInput` declaraba TODOS los campos de TODAS las herramientas como opcionales, así que una llamada sin `termino_busqueda` pasaba la validación de `RouterOutput` sin problema y el fallo solo aparecía después, dentro de la herramienta, gastando un paso completo. Fix: `app/agent/graph.py` — `RouterOutput` gana un `model_validator` (`_tool_input_trae_los_campos_obligatorios`) que exige los campos obligatorios por acción según `contracts/agent-tools.md` §T1-T5 (`_REQUIRED_TOOL_INPUT_FIELDS`); el rechazo es ahora un error de Pydantic que el repair loop del punto 1 puede corregir sin gastar presupuesto de investigación, en vez de un `INVALID_INPUT` de la herramienta que sí lo gasta.
  3. **T7 rechazaba los 3 claims de la reproducción real por mezclar categoría y cifra en un `direct`.** Causa raíz confirmada línea por línea contra `run_id=32ad6d77-...`: el router propuso claims `direct` con `columns=["nombre_evento", "total_casos"]` (dos columnas); `_build_direct` (`app/quality/claims.py`) exige exactamente una fila y una columna, así que rechazó los 3 y la corrida terminó `no_evidence` pese a que T1/T5/T6 habían funcionado de punta a punta. El prompt explicaba la DSL de `formula` pero no la regla de cardinalidad de `direct`. Fix: `router_v1.md` gana una regla explícita con ejemplo mal/bien calcado del caso real; `app/agent/graph.py` — `ClaimSpecPayload` gana un `model_validator` (`_direct_usa_una_fila_y_una_columna`) que adelanta la misma regla de `_build_direct` como error de Pydantic (corregible por el repair loop), en vez de un rechazo silencioso de T7 que se descubre solo al final. Pruebas: `backend/tests/test_agent_graph.py` (`ClaimSpecPayload` con `direct` de 2 columnas o 2 filas rechazado por `model_validate`).
  4. **`termination_reason="STEP_BUDGET_EXCEEDED"` mezclaba agotamiento real con parada preventiva.** Causa raíz confirmada en la reproducción aislada (`run_id=fea461c4-...`): terminó en el paso 7 de 10 con esa etiqueta, pero el grafo reserva preventivamente los pasos mínimos para poder cerrar (`_router_force_finish`/`_minimum_after_router`) — no había agotado el presupuesto real, decidió no empezar una acción que no le cabía. La misma etiqueta también se usaba para agotar `MAX_SOQL_CALLS` (presupuesto distinto: número de consultas, no de pasos) y, al agotar `MAX_SOQL_CORRECTIONS`, el router forzaba `finish` sin asignar ninguna razón. Fix: `app/agent/graph.py` — cuatro razones distintas: `STEP_BUDGET_EXCEEDED` (agotamiento real: `steps_used >= max_steps` al entrar al router, verificado ANTES de invocar al LLM para no gastar la llamada), `INSUFFICIENT_BUDGET_FOR_ACTION` (parada preventiva, con o sin que el LLM ya haya devuelto `finish` por su cuenta), `SOQL_CALL_BUDGET_EXCEEDED` (`t5_calls >= MAX_SOQL_CALLS`) y `SOQL_CORRECTION_EXHAUSTED` (antes sin razón asignada). Pruebas: `backend/tests/test_agent_graph.py` (`test_router_stops_without_llm_call_when_steps_already_exhausted` prueba que el LLM NI SIQUIERA se invoca cuando `steps_used` ya alcanzó `max_steps`; `test_router_forces_finish_when_soql_call_budget_exhausted`; `test_step_budget_forces_no_evidence_without_exceeding_limit` actualizado a `INSUFFICIENT_BUDGET_FOR_ACTION`; `test_soql_syntax_allows_only_two_corrections_after_initial_attempt`, `test_soql_forbidden_also_allows_only_two_corrections` y `test_at_most_four_soql_calls_per_run` ahora verifican la razón exacta).
  - **Investigado y descartado como bug activo (Hallazgo 3 del diagnóstico, `SOQL_FORBIDDEN`):** la reproducción real generó un SoQL agregado legítimo y aceptado; la guardia de solo lectura no rechazó lectura legítima en este caso. No se tocó `app/tools/soql_parser.py` en esta sesión — el hallazgo #2 de la investigación de robustez de arriba (2026-07-11, misma rama) ya cubrió el patrón real observado (FROM alucinado).
  - **Fuera de alcance deliberado de esta sesión (recomendado por el diagnóstico, no implementado):** separar la decisión de navegación (`RouterOutput.action`) de la propuesta de claims (`claim_specs_by_evidence`) en un nodo `claim_planner` posterior a T6 — el cambio de arquitectura de mayor alcance del diagnóstico, que requeriría su propia verificación con LLM real antes de tocar el flujo del grafo; reintento acotado con backoff para errores transitorios del proveedor LLM (504/timeout) — el docstring de `ainvoke_chat_model` documenta explícitamente que esa responsabilidad es del grafo, no de `app/llm/factory.py`, y no existe hoy en ninguno de los dos lugares (Art. IV.4 lo exige; queda como hallazgo abierto, no corregido aquí); instrumentación de diagnóstico por caso en `eval_case_results.metrics` (`last_node`, `nodes_visited`, etc.) — toca `backend/eval/`, explícitamente fuera de alcance por instrucción del prompt de diagnóstico.
  - Verificación: `ruff check .` limpio; `pytest -m "not integration"` 421 passed (1 fallo preexistente no relacionado en `test_llm_factory.py::test_get_chat_model_from_settings_sin_key_falla_claro`, confirmado preexistente con `git stash`/`git stash pop` antes de este cambio). No se corrió `pytest -m integration` en esta sesión (mismo motivo de concurrencia sobre Postgres local).
  - **Pendiente para cerrar esta investigación:** reconfirmación con LLM real (Art. IV.2) contra al menos el caso `pilot-003-salud-vigilancia` que motivó el hallazgo #3, y evidencia consolidada en PR hacia `v2`.

- [ ] **Ronda 2 de la investigación T-403 (2026-07-12): respuesta a la evaluación externa (`docs/instrucciones-evaluacion-agente-post-ajustes.md`).** Un agente evaluador (rol separado: prueba y reporta, no edita código) corrió la puerta 1 sobre la ronda 1 y la RECHAZÓ: `pytest -m "not integration"` no colectaba (`openpyxl` declarado en `pyproject.toml` pero ausente del `.venv`) y, al excluir esos módulos, `test_get_chat_model_from_settings_sin_key_falla_claro` fallaba de verdad (el helper `settings()` no aislaba `.env` local, así que heredaba una `ANTHROPIC_API_KEY` real). Señaló además que las puertas 2–8 de su propio documento de evaluación no tenían pruebas nuevas todavía. Esta entrada corrige ambos bloqueos y cierra las puertas 2, 3, 4, 6 y 7 con código real + pruebas nuevas (no se tocó `backend/eval/`).
  1. **Puerta 1 (bloqueante, corregida):** `pip install -e ".[dev]"` sincronizó el `.venv` con `pyproject.toml` (instaló `openpyxl`); `tests/test_llm_factory.py::settings`, `tests/test_settings.py::settings` y `tests/test_eval_run_error_handling.py::_settings` ahora pasan `Settings(_env_file=None, **values)` para que las pruebas no dependan de qué claves reales tenga el `.env` de la máquina que las corre. `pytest -m "not integration"` completo (sin exclusiones): 433 passed antes de las siguientes puertas, 471 passed al final.
  2. **Puerta 2 — esquema discriminado real por acción (no solo validación Python posterior).** El primer fix de la ronda 1 (`_REQUIRED_TOOL_INPUT_FIELDS` sobre un `RouterToolInput` único) validaba localmente pero el JSON Schema que ve el proveedor (`convert_to_openai_tool`) seguía mostrando todos los campos opcionales. Fix: `app/agent/graph.py` — `tool_input` es ahora una unión de 6 modelos `extra="forbid"` (`BuscarCatalogoToolInput`/`PerfilarDatasetToolInput`/`ResolverGeografiaToolInput`/`ExplorarValoresToolInput`/`EjecutarSoqlToolInput`/`FinishToolInput`, mismo patrón que `FormulaNode`), con un `model_validator` que ata el tipo recibido a `action` (la unión sola no evita que un payload con la forma de otra herramienta pase si `action` dice otra cosa). Verificado inspeccionando el schema real, no solo `model_validate()`: cada rama del `anyOf` tiene su `required`/`additionalProperties=false` correctos. **Hallazgo colateral documentado en la prueba:** dentro de `OpFormulaNode.args`, el corte de profundidad-1 ya conocido no se manifiesta como una rama ausente sino como una rama de schema vacío (`{}`, que en JSON Schema acepta cualquier valor) — `build_claims` sigue siendo la última línea de defensa ahí, no el schema. Pruebas: `tests/test_agent_router_contracts.py` (9 pruebas).
  3. **Puerta 3 — repair loop probado a nivel de grafo + error terminal específico.** El repair loop de la ronda 1 solo tenía pruebas a nivel de `ainvoke_structured_chat_model` (`test_llm_factory.py`). Se agregó `tests/test_agent_structured_repair.py` (7 pruebas) probando `_router_node` directamente con un doble que imita el sobre `{raw, parsed, parsing_error}` real, confirmando: se repara con feedback de validación, cuesta exactamente 1 paso del grafo (no 2) pese a 2 llamadas internas al LLM, el `usage_metadata` de ambos intentos se suma, y agotar los intentos termina en un código específico. Para esto último: `app/llm/factory.py` gana `LLMStructuredOutputError(LLMProviderError)`, lanzada solo cuando el repair loop se agota (nunca para una caída real del proveedor); `app/agent/graph.py` la atrapa primero vía `_llm_terminal_error` y mapea a `STRUCTURED_OUTPUT_INVALID` (no `retryable`) en vez de `LLM_PROVIDER_ERROR` genérico — antes ambas causas eran indistinguibles para diagnóstico (RF-703). Contrato actualizado: `contracts/api-rest.md` §4 documenta el nuevo código.
  4. **Puerta 4 — la herramienta nunca ve un input inválido.** `tests/test_agent_tool_recovery.py` (4 pruebas, grafo completo con `build_graph`) confirma con un doble de herramienta que hace `assert "termino_busqueda" in raw_input`: la corrección ocurre DENTRO del mismo turno del router (vía la puerta 2+3), la herramienta ejecuta una sola vez tras la corrección, un input inválido que nunca se corrige agota el repair loop sin llamar a la herramienta ni una vez, y un guion determinista confirma que el grafo SÍ entrega `EXPLORAR_VALORES_NOT_TEXT` de vuelta al router en `observations` para que pueda pivotar a `ejecutar_soql` directo (lo que un LLM real haga con esa señal se verifica aparte, Art. IV.2).
  5. **Puerta 6 — evidencia elegible ya no puede terminar en cero claims silenciosamente.** Causa raíz nueva (no cubierta por la ronda 1): si `claim_builder` rechazaba TODOS los claims propuestos, o el router simplemente no proponía ninguno, la corrida pasaba derecho al sintetizador con `claims=[]` sin que el router tuviera oportunidad de corregirse. Fix: `app/agent/graph.py` — `claim_builder` ahora puede volver a `router` UNA vez (`MAX_CLAIM_REPAIR_ATTEMPTS=1`, estado `claim_repair_attempts`/`claim_builder_retry_pending`, arista condicional `_after_claim_builder`) cuando el total de claims aceptados en toda la corrida sigue en cero y queda evidencia elegible sin cubrir; el router recibe `rejected_claims` en su payload y el prompt (`router_v1.md`) le pide corregir según la razón exacta en vez de repetir el mismo error. **Bug real encontrado por la propia prueba de la puerta 7 y corregido en la misma sesión:** el primer diseño de este retry no reservaba presupuesto para la vuelta extra router+claim_builder+synthesizer, y `test_steps_used_never_exceeds_configured_max_on_every_route` reprodujo `steps_used=9 > max_steps=8`. Fix: el retry ahora exige `remaining_after_this_step >= 3` además de intentos disponibles. Pruebas: `tests/test_agent_claim_planning.py` (8 pruebas), incluyendo el caso obligatorio de regresión (filas `nombre_evento`/`total_casos`) de punta a punta con grafo completo.
  6. **Puerta 7 — etiquetas de presupuesto con detalle de diagnóstico.** Además de las 4 etiquetas de la ronda 1, `agent_steps.detail` ahora incluye `requested_action`/`remaining_steps`/`required_steps` (o `t5_calls`/`correction_key` según la causa) para las paradas por presupuesto — antes esos datos solo existían implícitamente en el código, no en lo persistido (RF-703). Todas las decisiones de presupuesto se resolvieron para ocurrir ANTES de `_record` (antes, 3 de las 4 causas se registraban con la acción original en vez de la anulación real, porque el registro ocurría antes de esas comprobaciones). Pruebas: `tests/test_agent_budget_semantics.py` (10 pruebas), incluyendo el camino feliz completo cabiendo en el presupuesto por defecto de 10 pasos.
  - **Fuera de alcance de esta ronda (igual que la ronda 1):** separar `claim_planner` en un nodo dedicado; reintento con backoff para errores transitorios del proveedor LLM; instrumentación en `eval_case_results.metrics`; el renderer determinista de SoQL de la puerta 5 (condicional en el propio documento de evaluación: "si se introduce", no se introdujo). La puerta 8 (`tests/integration/test_agent_redesign_acceptance.py`, un archivo nuevo con 6-7 escenarios contra Postgres real) y la puerta 9 (smoke real con 5-8 casos del golden set, gasta cuota real de Gemini/Socrata) quedan explícitamente sin ejecutar en esta sesión.
  - **Verificación:** `ruff check .` limpio; `pytest -m "not integration"` completo (sin exclusiones): 471 passed, 0 failed. `pytest -m integration` focalizado (no la suite completa, mismo cuidado de concurrencia de la ronda 1; `pg_stat_activity` confirmó 0 conexiones ajenas antes de correr) sobre `tests/integration/test_t303_agent_graph.py` + `tests/integration/test_t402_quality_claims_integration.py`: 5 passed contra Postgres real.
  - **Pendiente para cerrar esta ronda:** puerta 8 (archivo de aceptación de integración dedicado) y puerta 9 (smoke real, requiere que el usuario autorice gastar cuota de Gemini/Socrata) del documento de evaluación; reconfirmación con LLM real de toda la investigación T-403 (sigue pendiente desde la ronda 1).

- [ ] **Ronda 3 de la investigación T-403 (2026-07-12): incidente crítico — el fix de la puerta 2 tumbaba el 100% de las corridas reales.** El usuario autorizó un smoke real de la ronda 2 (8 casos, golden-v1) con el agente evaluador: **0/8, y los 7 `run_id` verificables terminaron `failed`/`INTERNAL` sin un solo `agent_steps` ejecutado más allá del planner**, con `Unknown field for Schema: anyOf` repetido en la traza. El usuario cuestionó directamente si las 471 pruebas locales de la ronda 2 eran reales o "acomodadas para pasar" — pregunta justa: **ninguna de esas pruebas hablaba con un proveedor LLM real**, así que ninguna podía atrapar esto.
  1. **Causa raíz #1 (mía, de la ronda 2) — confirmada y reproducida localmente sin gastar cuota.** `tool_input` (puerta 2, unión de 6 modelos `extra="forbid"`) se verificó solo contra `convert_to_openai_tool` (el conversor de OpenAI, que este proyecto NUNCA usa). El conversor real (`LLM_PROVIDER=google` → `langchain_google_genai._function_utils`) es código completamente distinto: para una propiedad con `anyOf` SIN rama `null`, intenta construir un campo `anyOf` en el proto `Schema` que el SDK instalado no reconoce — `_convert_pydantic_to_genai_function(RouterOutput)` lanza `ValueError: Unknown field for Schema: anyOf` de forma determinista, sin red. Confirmado también que el `FormulaNode | None` preexistente (con rama `null`) NO dispara este choque específico — la causa es exclusiva de uniones sin `None`. Fix: `tool_input` vuelve a ser un único modelo `RouterToolInput` con todos los campos opcionales (garantía de campos obligatorios via `model_validator`, no via schema — más débil, pero la única opción que no revienta).
  2. **Causa raíz #2 (preexistente desde la investigación de robustez del router, 2026-07-11 sesión anterior, expuesta por la misma auditoría) — degradación silenciosa de `FormulaNode`.** El `anyOf` nullable de `formula` no revienta, pero `_get_properties_from_schema` colapsa las 4 formas a la ÚLTIMA rama tipo OBJECT: Gemini solo veía la forma de `op`/`args`, nunca `const`/`col`/`agg`. Aplanado a un único modelo con 4 campos opcionales + `model_validator` de forma exacta.
  3. **Causa raíz #3 (introducida por el fix #2, atrapada por verificación real inmediata, nunca llegó a producción).** El primer aplanado de `FormulaNode` seguía usando `args: list[FormulaNode]` — verdaderamente recursivo. La API real de Gemini lo rechazó con 400 en el primer router real (`GenerateContentRequest...properties[args].items: missing field`) porque `dereference_refs` no puede inlinar una auto-referencia genuina. Fix: `args` usa `FormulaLeaf` (sin `op`/`args` propios, sin recursión) — cierra estructuralmente la regla que `router_v1.md` ya pedía en texto ("op" nunca anidado dentro de "op"), que antes `model_validate()` toleraba igual.
  4. **Nueva disciplina de pruebas, aplicada a los 5 schemas de salida estructurada del grafo.** `tests/test_agent_router_contracts.py` reescrito: (a) construye el `FunctionDeclaration` real vía `app.llm.factory.get_structured_chat_model("google", ...)` (el mismo camino que usa producción, sin red, sin API key real) para `PlannerOutput`/`RouterOutput`/`SynthesisOutput`/`ClaimSpecPayload`/`FormulaNode` — la prueba que habría atrapado el incidente completo; (b) recorre recursivamente cada schema real buscando cualquier propiedad ARRAY sin `items` (la forma exacta de la causa #3) para las 5 schemas. Regla general documentada para cualquier cambio futuro de schema: probar contra el conversor del proveedor REALMENTE configurado, nunca contra `convert_to_openai_tool` u otro genérico.
  5. **Verificación real, no solo local (autorizada explícitamente por el usuario, cuota gastada a propósito).** `pg_stat_activity` limpio antes de cada corrida. (a) `execute_agent_run_async` directo contra el caso que antes fallaba en el paso 1: ahora 9 pasos reales, sin errores, termina `no_evidence` honesto (antes: 1 paso, `failed`/`INTERNAL`). (b) `eval.run --limit 5` contra golden-v1 (seed 601201): 0/5 aprobados en el sentido estricto del harness, pero **cero fallas de schema** en los 5 — 3 terminaron `no_evidence` limpiamente (búsqueda no encontró dataset, comportamiento honesto), 1 `failed` por timeout 504 transitorio del proveedor (`LLM_PROVIDER_ERROR`, no relacionado), y **1 terminó `completed`**: exactamente el caso `pilot-003-salud-vigilancia` / dataset `4hyg-wa9d` que originó el hallazgo del claim `direct` de dos columnas en la ronda 1 — produjo 3 claims `direct` separados (uno por evento: agresiones por animales, varicela, dengue), cada uno con una columna, `fabrication_count=0`, `recall_hit=true`; no aprobó solo porque el valor exacto no coincidió con `expected_facts` dentro de tolerancia (posible desactualización del golden set, no investigado más en esta ronda).
  - **Lección de proceso explícita:** el agente evaluador (rol separado, solo prueba/reporta) fue quien detectó esto — el patrón de roles separados ([[feedback_verification_rigor]]) funcionó exactamente como debía. Ninguna cantidad de pruebas con dobles/mocks locales puede sustituir una verificación contra el proveedor real cuando el cambio toca la forma del schema que se le envía.
  - Verificación: `ruff check .` limpio; `pytest -m "not integration"` → 479 passed, 0 failed.
  - **Pendiente:** correr el resto del golden set (o al menos los 8 casos originales) para tener una tasa de éxito real comparable; investigar por qué `pilot-003` no coincide en tolerancia; puertas 8/9 completas del documento de evaluación siguen pendientes.

- [x] **T-404 Nodo T8 `comparabilidad_territorial` + recomendación de fuentes externas (research.md §18, RF-202).** Requiere T-207 completada. Implementar `app/quality/territorial.py` según `contracts/agent-tools.md` T8: se ejecuta automáticamente cuando T3 resuelve ≥2 `divipola_code` en la misma corrida, consulta `territorio_tipologia` y devuelve `comparable`/`reasons` sin exponer `poblacion`/`ingresos_totales_cop`. Actualizar `synthesizer_v1.md` con la regla de advertencia de comparabilidad (nivel distinto o brecha de tipología ⇒ frase de advertencia, nunca una cifra nueva). Crear `app/quality/external_sources.yaml` (catálogo curado: DANE, DNP/TerriData, Contraloría, MinSalud/SISPRO, MinEducación, IGAC) + lógica determinista de match por palabras clave que puebla `no_evidence_report.external_sources` (`contracts/api-rest.md` §4).
  - ✅ **Refinamiento sobre el diseño original, documentado aquí:** en vez de "el LLM redacta `por_que`", se implementó `por_que` también fijo en el YAML y el campo completo (`entidad`/`url`/`por_que`) se sobrescribe en `_synthesizer_node` DESPUÉS de la llamada al LLM (`app/agent/graph.py`), ignorando cualquier valor que el modelo haya podido producir en ese campo — cero intervención del LLM en vez de intervención parcial, mismo principio de `contracts/api-rest.md` §4 pero con una garantía más fuerte (no hay superficie para que el LLM redacte nada relacionado con la fuente externa).
  - ✅ Wiring del grafo: `resolver_geografia` deja de tener arista fija a `router`; pasa por `_after_resolver_geografia` (nuevo), que redirige a `territorial_comparability` solo cuando el conjunto de códigos DIVIPOLA resueltos en la corrida (top match de cada llamada exitosa a T3, deduplicado) crece a ≥2 y cambió desde la última verificación (`state["territorial_checked_codes"]`) — evita repetir T8 si el usuario vuelve a preguntar por los mismos territorios. `territorial_comparability` añadido a `GRAPH_NODES` (persistencia/SSE por paso, T-300) y wired de vuelta a `router`. `GraphDependencies.territorial_loader` (mismo patrón que `metadata_loader` de T6) permite inyectar un doble en pruebas sin tocar Postgres.
  - ✅ **Corrida real de punta a punta contra Postgres** (`territorio_tipologia` cargada por T-207 con datos reales del DNP): `comparabilidad_territorial(["11001", "05148"], engine=...)` → Bogotá (`Bogotá`/`ESP`) vs. El Carmen de Viboral (`5`/`6`) → `comparable: false, reasons: ["tipologia_gap"]`, sin `poblacion`/`ingresos_totales_cop` en la salida (verificado explícitamente en la prueba). Código DIVIPOLA sin fila en `territorio_tipologia` → `comparable: null, reasons: ["sin_tipologia"]`.
  - ✅ 29 pruebas nuevas en verde: 12 unitarias de `evaluate_comparability` (`tests/test_quality_territorial.py`, incluye escala municipal, top-tier Bogotá/Ciudades grandes, tipología fuera de escala conocida sin afirmar una brecha no sustentable), 12 de `suggest_external_sources` + integridad del fixture real (`tests/test_external_sources.py`, mismo espíritu que `test_pii_patterns_fixture.py`: URLs `https://`, nombres de entidad únicos, ningún resultado con URL ausente del catálogo), 4 de integración del grafo (`tests/test_agent_graph_territorial.py`: T8 se dispara con 2 territorios y NO con 1; `external_sources` aparece determinista cuando la pregunta matchea un tema y queda vacío cuando no matchea ninguno) y 2 de integración real contra Postgres (`tests/integration/test_territorial.py`). `ruff check .` en verde; suite completa sin regresiones (384 pasan vs. 357 antes de T-404, la única falla es `test_llm_factory.py` preexistente y no relacionada).
  - ⚠️ **Nota de reconciliación (2026-07-11):** esta tarea se completó sobre una rama local que no había sincronizado `origin/v2`; al fusionar se descubrió que T-402 (arriba) seguía genuinamente abierta ahí también, y que la corrección real de `_claim_builder_node` (filtro `classification == "no_recomendada"`) documentada en el progreso de T-402 ya vive en `graph.py` desde antes de este merge — T8 no la duplica ni la contradice, son cambios en regiones distintas del mismo archivo (confirmado con `git diff`, sin conflicto real más allá de esta tabla).

## FASE 5 — Frontend v2 (RF-101…104, RF-501…503, RNF-007/008, Art. V)

- [ ] **T-501 Sistema de diseño azul.** Aplicar los tokens de plan.md §7 en `tailwind.config.js`; refactorizar componentes a la paleta; eliminar colores fuera de paleta salvo semánticos. Objetivo de accesibilidad: WCAG 2.2 nivel AA.
  - ✅ Revisión visual página por página + captura de pantalla en el PR; Lighthouse/axe accesibilidad ≥ 95 como puerta parcial (RNF-007) — la conformidad se completa con la revisión manual de T-505.
- [ ] **T-502 Conectar el copiloto al backend v2.** Reemplazar el fetch a `/api/consultar_v2` por el flujo `query` → SSE consumido con fetch-stream (el token Bearer viaja por encabezado; `EventSource` nativo no sirve — plan.md §11): pasos del agente renderizados en vivo como línea de tiempo en lenguaje claro, con detalle técnico expandible (ESC-04); reconexión automática con `Last-Event-ID`; el `run_access_token` se guarda junto al historial local de la sesión. Aviso de consentimiento ANTES de la primera investigación (RF-802: qué se guarda, para qué, por cuánto tiempo, cómo borrarlo) y acción "borrar esta investigación" que llama al `DELETE` (RF-803). Historial de sesión con re-ejecución/refinado de consultas anteriores (RF-502); todos los textos en español (RNF-012). Conexión DIRECTA del navegador al backend vía `NEXT_PUBLIC_BACKEND_URL` (sin proxy en Next.js); configurar `CORSMiddleware` en FastAPI restringido a los orígenes del frontend (plan.md §11).
  - ✅ RNF-008 medido: primer feedback < 500 ms, primer paso < 2 s; cortar la red a mitad de corrida y recuperarla continúa el stream sin duplicar pasos; el aviso de consentimiento aparece antes de la primera investigación.
- [ ] **T-503 Tarjetas de evidencia v2.** Mostrar tabla + badge de calidad + advertencias + narrativa citable + botón "insertar en sección" y descarga CSV (RF-501). Gráfica simple cuando `chart_suggestion` no es null (RF-503). Flujo de confirmación para `no_recomendada` (RF-404).
  - ✅ ESC-01 completo de punta a punta en local.
- [ ] **T-504 Citas y persistencia del documento.** Extensión Tiptap "cita de evidencia" que fija el objeto `citation` al fragmento insertado (RF-103); autoguardado en localStorage ≤ 5 s (RF-102); exportación a `.docx` con citas al pie (RF-102/103).
  - ✅ ESC-08: cerrar y reabrir el navegador conserva documento y citas; el .docx exportado muestra las citas.
- [ ] **T-505 Pruebas E2E, accesibilidad y RNF-012.** Playwright: ESC-01 feliz, ESC-03 sin evidencia, navegación por teclado del flujo principal. Además, revisión MANUAL de accesibilidad WCAG 2.2 AA según la lista de pruebas.md §5 (teclado, orden y visibilidad de foco, lector de pantalla, zoom y reflujo a 320 px, anuncios de contenido SSE con `aria-live` sin saturar, reducción de movimiento, no depender solo del color). Revisar RNF-012 con checklist manual: etiquetas del frontend, mensajes de error, `message_user`, advertencias de calidad, estados/mensajes SSE, consentimiento, retención/borrado, "sin evidencia" y ausencia de jerga técnica sin explicación. Las herramientas automáticas son auxiliares (RNF-007).
  - ✅ Suite E2E en CI (con backend mockeado por fixtures) + acta de revisión manual WCAG y acta RNF-012 archivadas en `docs/`.
- [ ] **T-506 Aporte manual del usuario en el canvas (research.md §18).** Componente `components/canvas/ManualDataEntry.js`: botón "Agregar dato manual" en `CanvasHeader`/`SectionCard`, junto al de insertar evidencia (T-503). Formulario: valor/texto, fuente (obligatorio), URL opcional, fecha. Persiste como nodo Tiptap del documento (mismo autoguardado localStorage de T-504) — **NUNCA** en las tablas backend `evidence`/`claims`, esas quedan reservadas para datos verificados por herramienta. **Regla de diseño no negociable (Art. I + V):** la tarjeta insertada debe verse visiblemente distinta de una tarjeta de evidencia verificada — color semántico propio y badge "Aporte manual — no verificado por el agente", sin el ícono de las evidencias reales. Cuando `no_evidence_report.external_sources` (T-404) trae sugerencias, la tarjeta "sin evidencia" del copiloto muestra un botón "Agregar manualmente" que abre este módulo pre-rellenado con la entidad sugerida.
  - ✅ Insertar un aporte manual y uno de evidencia real en la misma sección son distinguibles a simple vista (captura de pantalla en el PR); cerrar y reabrir el navegador conserva el aporte manual (ESC-08, mismo criterio de T-504); auditoría de accesibilidad del formulario nuevo incluida en la revisión manual de T-505.

## FASE 6 — Evaluación técnica OE3 (RF-601…603)

*Objetivo: la batería que produce las métricas del working paper (Producto 3 de la propuesta).*

- [x] **T-601 🧑 Construir el golden set v1 (ESC-06).** 50 casos en `backend/eval/golden/golden-v1.yaml`: ~40 positivos (pregunta + `expected_dataset_ids` + hechos verificados a mano contra el portal) y ~10 negativos (RNF-005). **Requiere tu criterio de politólogo:** las preguntas deben representar necesidades reales de funcionarios (usa los hallazgos del OE1 cuando existan). Documentar cada caso en `notes`.
  - ✅ Revisión cruzada: cada `expected_dataset_id` verificado manualmente en datos.gov.co.
  - ✅ Cierre 2026-07-11: `golden-v1.yaml` quedó congelado con 50 casos (40 positivos y 10 negativos), 50 semillas e IDs únicos y `notes` completos. Los 40 datasets positivos existen, están activos, `eligible`, con publicador `verified` y embedding local; sus 40 hechos se reprodujeron contra URLs oficiales deterministas sin fallos. La revisión sustantiva agrupó 22 diagnósticos territoriales, 18 casos de capacidad institucional y 10 guardas de honestidad; incluye abstención explícita para predicción, causalidad, privacidad, tiempo real, ABM/redes y NLP fuera del catálogo estructurado. Cambios posteriores requieren `golden-v2`.
  - ⚠️ **Cierre técnico parcial (2026-07-11):** consolidados `golden-v1-pilot.yaml` (10 casos) + `golden-v1-extension-40.yaml` (40 casos) en el entregable único `backend/eval/golden/golden-v1.yaml` (50 casos: 40 positivos/10 negativos, borradores eliminados tras consolidar). **Fuentes congeladas técnicamente:** auditoría real encontró que 35 de 40 hechos positivos (no solo los 3 señalados originalmente — presupuesto de Justicia, Antioquia 2011, Villamaría 2017) tenían `source_url` sin `$where`/`$order`, es decir sin ancla determinista a la fila esperada. Los 35 se re-verificaron con consultas reales contra `datos.gov.co` (no simuladas): para cada caso se reconstruyó `$where` a partir de las columnas reales de `catalog_columns`, se ejecutó contra la API en vivo, y se comparó fila por fila contra el `expected_value` ya registrado. **Resultado: los 40 hechos positivos coinciden con los datos reales actuales sin excepción** (0 valores desactualizados encontrados); en los casos donde `$limit=1` sin `$where` devolvía varias filas, se confirmó que esas filas eran idénticas en los campos consultados (determinismo de valor, no solo de identidad de fila) antes de aceptar la consulta como suficiente. Caso especial documentado en `notes`: `pilot-004-justicia-presupuesto` — el mismo `entidad`+`descripcion`+`a_o` aparece dos veces con valores distintos (cortes de ejecución presupuestal a marzo vs. diciembre); se ancló explícitamente al corte de diciembre (`mes='1931-12-01T00:00:00.000'`, formato de fecha tal como lo publica la fuente) para que la consulta sea reproducible.
  - ✅ **Revisión sustantiva humana confirmada (2026-07-11, Juan Camilo Grajales):** el criterio de politólogo que esta tarea exige ("Requiere tu criterio de politólogo") se aplicó sobre las 50 preguntas junto con el agente que consolidó `golden-v1.yaml`; se confirma que representan necesidades reales de funcionarios. En consecuencia `suite.status` en `golden-v1.yaml` quedó en `congelado` (pruebas.md §4.2: cualquier cambio posterior a los casos exige `golden-v2`, `golden-v1` no se edita). Esta línea se marca `[x]` con esa confirmación explícita, cerrando la advertencia anterior sobre revisión pendiente.
- [ ] **T-602 Runner de evaluación (ESC-06).** CLI `python -m eval.run --suite golden-v1 --provider google --model gemini-2.5-flash --seed <int>`: ejecuta cada caso contra el grafo real con semilla y `config_snapshot` persistidos, calcula recall@10, éxito, `socrata_success_rate`, **groundedness por verificación de claims** (cada cifra del texto tiene claim; operandos existen en las filas fuente; la fórmula DSL re-ejecutada reproduce `raw_value`; el redondeo produce `display_value`; el texto coincide con `display_value` — la coincidencia literal por regex queda solo como detector auxiliar de cifras huérfanas, pruebas.md §4.2), fabricaciones en negativos, latencias simples/multi-paso y costo; persiste `eval_runs`/`eval_case_results` y emite reporte Markdown en `backend/eval/reports/`.
  - ✅ Corrida completa contra Gemini termina y el reporte muestra las métricas de RNF-001…005.
- [ ] **T-603 🧑 Corridas comparativas (ESC-06).** Ejecutar la batería con ≥ 2 configuraciones (p. ej. Gemini 2.5 Flash vs Claude vs Gemini Pro). **Tú decides** las configuraciones finales según presupuesto. Analizar en `notebooks/02_analisis_eval.ipynb`.
  - ✅ Tabla comparativa lista para el working paper; configuración ganadora fijada como default.
- [ ] **T-604 Puertas RNF-002.** Configurar tres niveles: cada PR corre pruebas deterministas con LLM guionado y smoke reducido sin LLM real; semanalmente corre golden set con LLM real y abre issue/alerta si hay regresión o `success_rate < 80%`; antes de release corre golden completo y bloquea el release si `success_rate < 80%`.
  - ✅ Workflows visibles en Actions: PR determinista bloqueante, semanal con issue/alerta, y pre-release bloqueante.

### Línea de trabajo del núcleo determinista (enmienda `research.md` §25)

**Objetivo:** construir una ruta de validación independiente para el runtime determinista, diagnosticar fallos por etapa y mejorar utilidad sin sobreajustar `golden-v1`. El legado permanece como rollback. La secuencia es estricta: T-610 → T-611 → T-612 → T-613 → T-614 → T-615 → T-616 → T-617.

**Estado histórico para el siguiente agente (2026-07-17):** T-610…T-614 y
T-615A…T-615F están cerradas.
La aceptación E2E está separada entre `legacy_agent_acceptance` y
`deterministic_agent_acceptance`; el evaluador persiste `stage_diagnostics`
v1.0; las dos corridas normativas RNF-010 están verdes.
`GOLDEN_V2_PROPOSAL.md` continúa sin rango normativo. La enmienda T-615 fue
aprobada y se ejecuta por incrementos con autorización independiente. La
matriz terminal ya definida reautorizó T-615G, que cerró junto con T-615G-R.
T-615H y su corrección de reverificación quedaron cerradas; T-615I es la
siguiente tarea autorizable. En ese estado fechado, T-616 y T-617 permanecían
bloqueadas.

**Estado vigente (2026-07-18):** T-615A…T-615J y T-616 están cerradas con sus
autorizaciones y evidencias registradas abajo. T-617 está abierta en preflight;
no depende ya de un incremento anterior, pero no autoriza las corridas reales
hasta cerrar y auditar su instrumentación mecánica.

**Desviación conocida:** `backend/app/config.py` usa actualmente `deterministic` como default, aunque la política aprobada mantiene `legacy` como runtime operativo durante la validación. T-610 debe registrarlo; los entornos de usuario/producción deben fijar `AGENT_RUNTIME=legacy`. La corrección del default requiere una tarea explícita posterior al incremento solo-pruebas, no un cambio silencioso dentro de T-611/T-612.

- [x] **T-610 Congelar la línea base del rediseño (RF-603, Art. II.3/IV).** Crear `backend/eval/reports/deterministic-development-baseline.md` con commit, fecha, rama, runtime, modelo, suite, número de pruebas recolectadas, resultado no integración, subconjunto determinista, integraciones disponibles, última corrida de 50 casos, distribución conocida de fallos y limitaciones ambientales.
  - **Evidencia ya conocida que debe verificarse y registrar, no copiar a ciegas:** 628 pruebas no integración verdes; subconjunto determinista verde; última línea completa 25/50. Si los números cambiaron, prevalece la corrida actual.
  - **Puerta:** `pytest --collect-only`, no integración y subconjunto determinista reproducibles; cero cambios de comportamiento; el reporte distingue “runner ejecutó” de “agente resolvió”.

- [x] **T-611 Crear aceptación E2E del runtime determinista (RF-201…209, RF-703).**
  - Crear `backend/tests/integration/test_deterministic_agent_acceptance.py`.
  - Entrar exclusivamente por `execute_deterministic_agent_run_async`; cubrir construcción de dependencias, selección de runtime, PostgreSQL, pasos/eventos, evidencia, calidad, claims, respuesta y terminal.
  - Crear fixtures aisladas equivalentes a `deterministic_engine`, `deterministic_settings`, `deterministic_run`, `seed_deterministic_catalog`, clientes falsos de embeddings/LLM, Socrata mockeado y preservación de estado existente.
  - Implementar las nueve historias de `pruebas.md` §4.4: positivo, cambio de candidato, reparación, exploración, privacidad, abstención, proveedor, cancelación y presupuestos.
  - **Puerta:** todas pasan con PostgreSQL real; no se invoca el legado; sobreviven filas preexistentes; suite ejecutable aisladamente; sin cambios de comportamiento productivo.

- [x] **T-612 Separar aceptación legacy y determinista (Art. IV, RNF-002).**
  - Renombrar `test_agent_redesign_acceptance.py` a `test_legacy_agent_acceptance.py`.
  - Registrar marcadores `legacy_agent_acceptance` y `deterministic_agent_acceptance` en `backend/pyproject.toml`; marcar cada suite correctamente.
  - Añadir guardia que impida imports de `app.agent.graph` en la aceptación determinista.
  - **Puerta:** ambos grupos corren de forma independiente; las siete historias existentes siguen verdes como rollback; ninguna prueba está atribuida al runtime equivocado.

- [x] **T-613 Añadir matriz de etapas y motivos de fallo al evaluador (RF-602/603).**
  - Implementar enums/valores canónicos y `stage_diagnostics` JSONB versionado sin cambiar todavía el esquema relacional ni mezclar códigos con texto humano.
  - Persistir los campos definidos en `pruebas.md` §4.4 y renderizar tablas de etapas, motivos y recuperación.
  - Ampliar `test_eval_metrics.py`, `test_eval_persistence.py` y `test_eval_run_error_handling.py` con recuperación fallida, recuperado-no-intentado, plan inválido, cero filas, evidencia/claim rechazados, golden ambiguo y aprobado.
  - **Puerta:** todo caso fallido responde automáticamente etapa, causa, datasets, presupuesto y clasificación agente-versus-golden.
  - ✅ Cierre 2026-07-16: `eval/diagnostics.py` define etapas/códigos canónicos y genera `stage_diagnostics` v1.0 dentro de `eval_case_results.metrics` (JSONB existente). El runner conserva IDs recuperados/intentados y errores tipados de validación como metadatos de traza, sin cambiar decisiones ni presupuestos. El reporte Markdown agrega resultado por caso, fallos por etapa, motivos y recuperación. Pruebas dirigidas: 29 casos diagnósticos/persistencia/error; regresión no integración: 643; aceptación determinista: 14 passed/1 xfailed; aceptación legacy: 7; Ruff verde. Informe: `backend/eval/reports/t613-stage-diagnostics.md`.

- [x] **T-614 Ejecutar smoke determinista de 10 casos y corregir primero recuperación (RNF-004).**
  - Ejecutar la muestra normativa de `pruebas.md` §4.4 y persistir reporte reproducible.
  - Reagrupar los diez fallos de recuperación: no recuperado, fuera de presupuesto, no intentado, perfil fallido, plan incompatible u otro dataset ejecutado.
  - Registrar por variante top-25, scores vectorial/léxico/estructural, metadata y columnas del esperado.
  - Permitir solo mejoras generales demostradas por varios casos: identificadores literales, tokenización, variantes entidad/territorio/periodo, cobertura de columnas, penalización estructural y consenso entre variantes.
  - Prohibido: boost por ID, mapa pregunta→dataset, valores de `expected_facts`, filtros ocultos o aumento de presupuesto sin evidencia.
  - **Puerta:** negativos 2/2; casos sólidos sin regresión; recall@10 de los diez fallos mejora mediblemente; RNF-010 se conserva; repetir smoke tras cada incremento.
  - ⚠️ Avance 2026-07-16: smoke inicial `c40191ef-a095-448a-a357-e6b88c7ec784` (6/10, recall@10 7/8, negativos 2/2) y final `807c3127-9967-4c1c-afe2-714923cb0776` sobre `4df5c98` (7/10, recall@10 8/8, negativos 2/2). La ventana por variante pasó de 10 a 25 conservando 10 candidatos combinados; `pilot-012` pasó de fuera de top 10 a rango 1 y los cuatro sólidos no retrocedieron. **Tarea abierta:** RNF-010 real obtuvo cobertura 100 %, p50 1302,8 ms, p95 1828,8 ms y p99 2061,3 ms; incumple p95 ≤ 1000 ms. `expected_fact_not_found` ya no atribuye automáticamente culpa al golden. Auditoría: `pilot-021` es fallo del agente (`count(*)=1` en vez de `cantidad=65`); `pilot-038` es golden ambiguo porque la hora 13:50 no está en la pregunta. Véase `backend/eval/reports/t614-correction-audit.md`.
  - ✅ **T-614R1 lifecycle (2026-07-16):** cliente Gemini y engine/pool se reutilizan por worker en el Selector loop principal, con cierre idempotente y pruebas de concurrencia/startup/shutdown. RNF-010 mejoró de p50/p95/p99 `1187,6/1659,3/1770,2 ms` a `923,5/1293,6/1696,2 ms`, cobertura 100 %, pero continúa en rojo.
  - ✅ **T-614R2 lexical (2026-07-16):** decisión SDD cerrada; `lexical_search_vector` materializado con GIN y `lexical_rank_vector` exacto, ambos mantenidos por triggers y backfill reversible. SQL+Python p95 bajó de `810,1` a `144,4 ms`; dos corridas normativas consecutivas obtuvieron p50/p95/p99 `376,8/828,0/1635,5 ms` y `359,5/482,0/771,4 ms`, cobertura `100 %` (8394/8394) y cero errores. Suite no integración `653 passed`; aceptación determinista `14 passed, 1 xfailed`; legacy `7 passed`. **T-614 cerrada. T-615 continúa bloqueada y no se inicia automáticamente.** Véase `backend/eval/reports/rnf010-lexical-optimization.md`.

- [x] **T-615 Diseñar e implementar hechos textuales de primera clase
  (RF-210/RNF-013 propuestos; RF-208/RNF-003 preservados).**
  - **Estado vigente:** T-615A…T-615J cerradas mediante incrementos autorizados
    y revisables; cierre integral en
    `backend/eval/reports/t615-final-acceptance.md`. Diseño: `research.md` §27
    y `proposals/textual-claims.md`.
  - **Regla global aplicada durante T-615:** no representar texto mediante
    `raw_value=1`, conteos ficticios ni claims cuantitativos; no editar
    `golden-v1`; no iniciar T-616/T-617 antes del cierre de sus dependencias;
    no retirar el legado; cada incremento requería autorización explícita y
    rollback propio. Las dependencias se cerraron posteriormente en el orden
    aprobado.

  - [x] **T-615A Aprobar la enmienda SDD y congelar decisiones.**
    - **Requisitos:** RF-210/RNF-013 propuestos; Constitución Art. I/II/IV;
      RF-208/RNF-003 no se alteran.
    - **Archivos:** `spec.md`, `research.md`, `plan.md`,
      `contracts/agent-tools.md`, `contracts/api-rest.md`, `data-model.md`,
      `pruebas.md`, `tasks.md`, `quickstart.md`,
      `checklists/requirements.md`, `proposals/textual-claims.md`.
    - **Dependencias:** T-614 cerrada; revisión humana de la propuesta.
    - **Pruebas:** `git diff --check`; `uv run ruff check .`; auditoría de que
      `backend/app`, Alembic, DB y ambos golden no cambiaron.
    - **Aceptación:** decisión explícita sobre tabla, operaciones,
      normalización, empates, hash, API, síntesis, métricas y secuencia.
    - **Rollback:** revertir solo el commit documental.
    - **Gate:** aprobación o devolución humana. Sin aprobación no empieza
      T-615B. El commit documental por sí solo no cierra T-615A.
    - **Cierre:** aprobación humana registrada el 2026-07-17 sobre
      `f7725eeced8afc93ccd7fcc250e248a05f2db616`. Quedan congeladas la API
      textual aditiva; `claims` y `partial_claims` cuantitativos; los futuros
      campos raíz separados `textual_facts` y `partial_textual_facts`;
      `grounded-synthesis-plan-v1`; las operaciones cerradas, incluida
      `canonical_text_set`; `text-es-v1`; `tie_policy=reject`;
      `sha256-jcs-v1`; la persistencia textual separada; y la retención y los
      fingerprints no identificables. Se autoriza exclusivamente T-615B;
      T-615C y posteriores continúan bloqueadas.

  - [x] **T-615B Cerrar modelos de dominio y contrato tipado.**
    - **Requisitos:** RF-210; unión `GroundedFact`; enums cerrados;
      `QuantitativeClaim` compatible.
    - **Archivos previstos:** `backend/app/schemas.py`, nuevo módulo de
      dominio textual bajo `backend/app/quality/`, contratos Pydantic y sus
      pruebas unitarias/REST.
    - **Dependencias:** T-615A aprobada; sin migración todavía.
    - **Pruebas:** validación de seis operaciones, `text-es-v1`, unión
      discriminada, forma cuantitativa histórica e inputs inválidos.
    - **Aceptación:** no quedan `dict[str, Any]` en la superficie pública de
      hechos; los modelos no persisten ni emiten texto.
    - **Rollback:** retirar solo modelos/enums nuevos.
    - **Gate:** contrato unitario verde y revisión de compatibilidad antes de
      T-615C.
    - **Cierre:** modelos tipados, unión interna y contrato cuantitativo
      compatible verificados el 2026-07-17; OpenAPI productivo sin cambios.
      T-615C fue autorizada y cerrada posteriormente el mismo día.

  - [x] **T-615C Añadir persistencia aislada y reversible.**
    - **Requisitos:** RF-703/RF-803/RF-804, modelo `textual_facts` propuesto.
    - **Archivos previstos:** `backend/app/db/models.py`, una migración
      Alembic nueva, repositorio/persistencia y pruebas de base de datos.
    - **Dependencias:** T-615B; autorización específica para migración.
    - **Pruebas:** upgrade/downgrade; checks, FK, índices, round-trip,
      cascades, retención idempotente y ausencia de texto en snapshots OE3.
    - **Aceptación:** `quantitative_claims` queda byte/semánticamente
      compatible; downgrade elimina solo `textual_facts`.
    - **Rollback:** downgrade de la migración y revert del repositorio.
    - **Gate:** PostgreSQL real verde antes de construir hechos.
    - **Cierre:** migración `20260717_0006`, modelo ORM y repositorio aislado
      verificados el 2026-07-17 contra PostgreSQL real: upgrade/downgrade,
      checks, FK, índices, round-trip, cascades, retención idempotente y OE3
      sin contenido textual. `quantitative_claims` conservó su firma completa.
      T-615D fue autorizada y cerrada posteriormente el mismo día.

  - [x] **T-615D Implementar normalización, operaciones y hash puros.**
    - **Requisitos:** RF-210; `direct_text`, `value_presence`,
      `category_selection`, `argmax_label`, `argmin_label`,
      `canonical_text_set`; `text-es-v1`; `tie_policy=reject`.
    - **Archivos previstos:** módulo de dominio de T-615B y pruebas unitarias.
    - **Dependencias:** T-615B; puede desarrollarse sin escribir DB.
    - **Pruebas:** matriz `pruebas.md` §4.5: tildes, puntuación, orden,
      duplicados, nulos, columnas/filas, extremos, empate y estabilidad hash.
    - **Aceptación:** funciones puras y deterministas; ningún texto se
      convierte en número; hash cambia solo con material semántico.
    - **Rollback:** retirar implementación conservando contrato aún inactivo.
    - **Gate:** cobertura de ramas de operación y regresión cuantitativa
      verdes.
    - **Cierre:** funciones puras `text-es-v1`, seis operaciones cerradas,
      extremos con `tie_policy=reject` y hash `sha256-jcs-v1` verificados el
      2026-07-17. JCS usa `rfc8785==0.1.4` y pasa vectores oficiales de
      serialización/primitivos y orden UTF-16. Suite sin PostgreSQL, sin
      persistencia ni integración; T-615E permanece bloqueada y requiere
      autorización específica. Corrección contractual posterior: `raw_values`
      conserva la cadena fuente exacta, `display_value` aplica NFC/espacios,
      `normalized_values` añade `casefold`, y el hash recibe `canonical_soql`
      literalmente desde el renderer.

  - [x] **T-615E Implementar constructor/verificador textual.**
    - **Requisitos:** RF-401/RF-404/RF-210; solo evidencia elegible; plantillas
      deterministas; errores tipados.
    - **Archivos previstos:** `backend/app/quality/`, persistencia de T-615C,
      pruebas unitarias e integración.
    - **Dependencias:** T-615C y T-615D.
    - **Pruebas:** construcción/rechazo por operación; evidencia bloqueada o
      no recomendada; recomputación exacta desde filas; transacción fallida.
    - **Aceptación:** valor, fila, columna, selección y hash se reproducen;
      `count=1` deja de ser una salida válida para texto.
    - **Rollback:** feature inactiva y eliminación del constructor sin tocar
      datos cuantitativos.
    - **Gate:** constructor y persistencia verdes antes de integrar runtime.
    - **Cierre:** constructor puro sobre snapshot defensivo, seis plantillas
      cerradas, verificación independiente y lote transaccional
      construir→verificar→persistir→cargar→reverificar cerrados el
      2026-07-17. Solo `quality_reports.eligibility_status=eligible` con
      clasificación distinta de `no_recomendada` produce hechos; alteraciones
      y sustitutos cuantitativos se rechazan con códigos estables. Pruebas
      unitarias, PostgreSQL real aislado, regresiones cuantitativas, runtimes
      inactivos y OpenAPI productivo sin cambios verificados. T-615F permanece
      bloqueada y requiere autorización separada.

  - [x] **T-615F Integrar planificación y pipeline determinista.**
    - **Requisitos:** RF-201…209/RF-210; presupuestos y terminal único;
      selección textual estructurada.
    - **Archivos previstos:** `backend/app/agent/deterministic_pipeline.py`,
      contratos LLM de planificación, runner/persistence y pruebas dirigidas.
    - **Dependencias:** T-615E; flag/configuración de activación reversible.
    - **Pruebas:** lookup textual, caso mixto, cambio de candidato, rechazo,
      cancelación, presupuesto y cero invocaciones al legado.
    - **Aceptación:** eliminar el fallback semántico de presencia cuantitativa
      solo cuando el reemplazo esté activo y probado.
    - **Rollback:** desactivar la capacidad y restaurar comportamiento
      anterior mediante revert; no hacer fallback automático entre runtimes.
    - **Gate:** aceptación determinista dirigida verde, sin activar API
      textual todavía.
    - **Cierre (2026-07-17):** `DETERMINISTIC_TEXTUAL_FACTS_ENABLED=false`
      conserva el contrato LLM cuantitativo y el fallback histórico; al
      activarlo, solicitudes cerradas por operación se resuelven contra
      esquema/renderer, T-615D prueba filas, orden y empates, y T-615E
      construye/verifica/persiste el lote. Rechazos no producen `count=1`,
      síntesis ni fallback legacy. PostgreSQL real, aceptación determinista y
      legacy verdes; OpenAPI conserva
      `4d607fd6e59d908c8f96e633a1d092f55fc4379394aec8100ad14ba21db44dae`.
      T-615G quedó bloqueada hasta definir la matriz terminal; la corrección
      documental del 2026-07-17 la reautoriza sin iniciar T-615H.

  - [x] **T-615G Habilitar API textual aditiva y lectura histórica.**
    - **Requisitos:** contrato REST §4b; SSE/parciales; compatibilidad.
    - **Archivos previstos:** `backend/app/schemas.py`, rutas/serializadores,
      persistencia de respuesta y pruebas de contrato.
    - **Dependencias:** T-615F.
    - **Pruebas:** snapshots/OpenAPI de `claims` cuantitativo intacto; campos
      `textual_facts`/`partial_textual_facts`; históricos sin esos campos;
      clientes estrictos/tolerantes; `completed`, `interrupted`, `failed`.
    - **Aceptación:** ningún discriminador nuevo en claims cuantitativos;
      históricos equivalen a listas textuales vacías y nunca se infiere texto.
      Una corrida solo textual termina `completed` con resumen fijo,
      `narrative=null`, evidencia vinculada, claims vacíos y sin reporte de no
      evidencia. `no_evidence` mantiene evidencia y listas textuales vacías;
      `interrupted` expone solo persistidos/reverificados como parciales;
      `failed` no expone hechos textuales.
    - **Rollback:** detener emisión textual; tabla/datos pueden permanecer
      inaccesibles hasta reactivar. Con el flag apagado no se cargan hechos
      desde la tabla ni se reescriben payloads históricos.
    - **Gate:** contrato, REST, SSE, históricos, aislamiento y API verdes antes
      de síntesis pública.
    - **Corrección documental (2026-07-17):** la matriz terminal aprobada en
      `research.md` §27 y contrato REST §4b resuelve el bloqueo semántico y
      reautorizó exclusivamente T-615G. T-615H permaneció bloqueada hasta la
      corrección documental específica registrada en su propia entrada.
    - **Cierre (2026-07-17):** modelo público cerrado, payload terminal
      persistido y fronteras REST/SSE/replay exponen las dos listas aditivas;
      históricos se materializan sin reescritura ni inferencia. Casos
      cuantitativo, textual puro, mixto, rollback, aislamiento, cascadas y
      retención verdes contra PostgreSQL real. OpenAPI cambió de
      `4d607fd6e59d908c8f96e633a1d092f55fc4379394aec8100ad14ba21db44dae`
      a `d1627ed9394bb729ec98aa4d9d818db054efdd1ff4ce78654c37789ed651d89b`
      únicamente en la superficie textual aprobada. T-615H no iniciada.
    - **Corrección T-615G-R (2026-07-17):** un fallo exclusivo del lote
      textual ya no degrada un resultado cuantitativo válido: conserva
      `completed`, claims, evidencia y narrativa cuantitativa, y entrega ambas
      listas textuales vacías.

  - [x] **T-615H Restringir síntesis a hechos persistidos.**
    - **Requisitos:** RF-208/RNF-003 y RF-210/RNF-013; IDs existentes;
      conectores y plantillas cerrados.
    - **Archivos previstos:** `backend/app/agent/llm_contracts.py`,
      sintetizador/renderizador, prompts estructurados y pruebas adversarias.
    - **Dependencias:** T-615G.
    - **Pruebas:** ID inexistente, segmento sin ID, valor alterado, evidencia
      no elegible, conector inválido, respuesta mixta y regresión de cifras.
    - **Aceptación:** todo segmento factual es renderizado desde hechos; cero
      hechos/cifras huérfanos; no se promete clasificar prosa factual libre
      porque esa prosa no se admite.
    - **Rollback:** desactivar emisión textual y conservar síntesis
      cuantitativa vigente.
    - **Corrección documental (2026-07-17):**
      `grounded-synthesis-renderer-v1` fija texto literal de cláusulas,
      plantillas, conectores y cierres; `comparison_pair` admite pares
      cuantitativos, textuales o mixtos solo si son distintos y comparten
      corrida y evidencia elegible; `grounded-synthesis-fallback-v1` fija orden,
      límite ocho, plantillas y cierre. La secuencia exige persistir y
      reverificar antes de sintetizar. Se reautoriza exclusivamente T-615H;
      T-615I no se inicia.
    - **Gate:** guardas runtime bloqueantes y pruebas adversarias verdes.
    - **Cierre (2026-07-17):** el flag activado difiere T8 hasta después de
      persistir y reverificar; el conjunto permitido se aísla por corrida y
      evidencia elegible; plan, renderer literal y fallback cerrado comparten
      validador. Evidencia: 236 pruebas dirigidas, 832 sin integración y 47
      relacionadas con PostgreSQL verdes (1 `xfail` preexistente); aceptación
      por `execute_deterministic_agent_run_async`, legacy y guardia verdes;
      OpenAPI canónico sin cambios.
    - **Corrección T-615H-R (2026-07-17):** la reverificación textual ya no
      infiere el certificado `validated_order_is_total` de la operación
      solicitada. Lo reconstruye desde la SoQL canónica y las filas persistidas:
      aliases completos del renderer, `ORDER BY` total sin duplicados,
      `LIMIT >= 2`, fila fuente inicial y claves observadas inequívocas. Los
      snapshots cubren literalmente las tres plantillas, los cuatro conectores
      y los tres cierres aprobados.

  - [x] **T-615I Añadir métricas y snapshots no identificables.**
    - **Requisitos:** extensión RF-602; RNF-003 intacto; métricas RF-210/RNF-013.
    - **Archivos previstos:** `backend/eval/metrics.py`,
      `backend/eval/persistence.py`, reportes y pruebas de evaluación.
    - **Dependencias:** T-615H.
    - **Pruebas:** cobertura/reproducibilidad/display, segmentos huérfanos,
      operación inválida, conjunción total, fingerprints y privacidad.
    - **Aceptación:** métricas cuantitativas no cambian; un promedio nunca
      oculta un fallo; no se almacena contenido textual en
      `eval_case_results`.
    - **Rollback:** retirar nuevas claves manteniendo lectores de reportes
      históricos.
    - **Gate:** suite de evaluación verde; sin ejecutar Gemini ni modificar
      umbrales en este incremento.
    - **Cierre (2026-07-17):** OE3 calcula cobertura de referencias,
      reproducibilidad, coincidencia literal de presentación, segmentos
      huérfanos, operaciones inválidas y la conjunción
      `grounded_fact_integrity`, con semántica explícita no aplicable para
      respuestas sin hechos textuales. `eval_case_results` conserva solo
      métricas, conteos, enums/versiones y fingerprints `source_hash`; la
      calidad se reduce a conteos sin warnings libres. Evidencia: 84 pruebas
      dirigidas, 847 sin integración y 47 integraciones PostgreSQL verdes
      (1 `xfail` preexistente); Ruff y `git diff --check` verdes. No se
      ejecutaron Gemini, Socrata ni golden y T-615J no se inició.
    - **Corrección T-615I-C (2026-07-17):** la aplicabilidad reúne hechos
      públicos, referencias del plan y hechos persistidos/reverificados; la
      reproducibilidad compara el contrato textual completo y toda entrada
      estructurada inválida produce métricas fallidas, nunca un error de
      infraestructura. La prueba de privacidad serializa recursivamente la
      fila OE3 destinada a persistencia. Evidencia: 98 pruebas dirigidas, 865
      sin integración y 47 integraciones focalizadas verdes (1 `xfail`
      preexistente).

  - [x] **T-615I-R Aislar fixtures antiguas de integración.**
    - **Cierre (2026-07-17):** las pruebas que controlan catálogos,
      publicadores o geografía ejecutan cada caso en una base PostgreSQL
      temporal migrada, sin borrar ni respaldar tablas compartidas. Una fila
      centinela ajena se verificó antes y después de cada caso y solo su
      fixture la eliminó al final. Evidencia: 41 pruebas saneadas juntas y dos
      ejecuciones completas idénticas de la marca `integration`, cada una con
      119 aprobadas, 1 omitida y 1 `xfail` estricto preexistente; cero bases
      `t615ir_*`, tablas de respaldo o residuos al terminar.

  - [x] **T-615J Ejecutar aceptación integral y cerrar la puerta.**
    - **Requisitos:** todos los anteriores; Art. II/IV; rollback legacy.
    - **Archivos previstos:** aceptación determinista/legacy, pruebas de
      integración compartidas y acta `backend/eval/reports/` autorizada.
    - **Dependencias:** T-615B…I cerradas.
    - **Pruebas:** no integración, contrato, PostgreSQL, aceptación
      determinista, aceptación legacy, retención y smoke textual dirigido;
      no golden completo salvo autorización separada.
    - **Aceptación:** caso solo textual, mixto, varias filas, empate,
      síntesis adversaria y borrado; cuantitativos sin regresión.
    - **Rollback:** prueba documentada para desactivar emisión textual y
      revertir migración sin perder claims cuantitativos.
    - **Gate:** aprobación humana del cierre. Solo entonces T-616 puede ser
      autorizada; T-617 sigue dependiendo de T-616.
    - **Cierre técnico (2026-07-17):** acta
      `backend/eval/reports/t615-final-acceptance.md`; 265 pruebas dirigidas,
      865 sin integración, 51 aceptaciones/persistencia/retención focalizadas
      y dos marcas completas de integración con 119 aprobadas, 1 omitida y 1
      `xfail` estricto conocido en cada ejecución. OpenAPI canónico y
      `golden-v1` intactos; cero bases temporales o residuos. T-616 y T-617 no
      se iniciaron.

- [x] **T-616 Auditar los 50 casos y construir `golden-v2` (RF-601/602).**
  - No modificar `backend/eval/golden/golden-v1.yaml`.
  - Auditar por caso restricciones de la pregunta, filtros ocultos de URL, respuestas válidas, desempate, corte, fuente, elegibilidad y tipo de hecho.
  - Clasificar: determinado, multi-respuesta, agregado, abstención o incompatible. Proponer esquema con `input_constraints`, `selection_rule`, `acceptable_facts`, `source_urls`, `observed_at` y `data_cutoff_at`.
  - Solo después de aprobación normativa crear `backend/eval/golden/golden-v2.yaml` y sus validaciones.
  - **Puerta:** 50 auditados, positivos derivables, sin filtros ocultos injustificados, suite versionada y autorizada explícitamente. `golden-v1` permanece byte a byte intacto.
  - **T-616A (2026-07-18): auditoría producida, PENDIENTE DE APROBACIÓN.** Se auditaron los 50 casos (`backend/eval/reports/t616a-golden-v2-audit.md` y `t616a-case-audit.json`, no normativos). No se creó `golden-v2.yaml`, no se modificó `golden-v1` (SHA-256 intacto) y no se inició T-616B/T-617. T-616 sigue **abierta**; requiere revisión y autorización explícita de Juan Camilo y del coordinador antes de materializar cualquier suite.
  - **T-616A-R (2026-07-18): auditoría corregida y completada, PENDIENTE DE APROBACIÓN.** La revisión coordinadora encontró rigor insuficiente en T-616A (solo 12/40 positivos verificados contra Socrata, confianza mal calibrada, clasificaciones erróneas en `pilot-001`/`pilot-022`). T-616A-R reaudita los 50 casos desde cero: **40/40 positivos verificados contra el catálogo PostgreSQL local en transacción read-only y contra Socrata real**, reproducible vía `backend/scripts/t616a_audit.py --check`, `--refresh-live-evidence` y `--verify-live`. El manifiesto no normativo `backend/eval/reports/t616a-evidence-manifest.json` (esquema `t616a-evidence-manifest-v2`) persiste metadatos/esquema local y oficial, consultas canónicas completas, cardinalidades físicas/de proyección, nulos, duplicados, selección/empates y hashes; `--verify-live` falla ante diferencias materiales. La corrección final separa pregunta y dataset: `pilot-001`, `pilot-038` y `pilot-039` son ambiguos/multi-respuesta con dataset compatible; `pilot-022` es determinado con ancla v1 equivocada. Artefactos actualizados: `t616a-golden-v2-audit.md`, `t616a-case-audit.json` (esquema `t616a-case-audit-v2`), manifiesto y `t616a_audit.py`. `golden-v1` intacto (SHA-256 sin cambios); `golden-v2.yaml` no creado; no se ejecutó ningún LLM. T-402 conserva su cierre formal como deuda separada. T-616 sigue **abierta**; T-616B y T-617 **no** iniciadas; requiere revisión y autorización explícita de Juan Camilo y del coordinador.
  - **T-616B (2026-07-18): suite normativa materializada y validada.** La
    instrucción humana «Continúa con la siguiente tarea» autorizó expresamente
    el incremento y el coordinador aprobó las propuestas concretas de T-616A-R.
    `backend/eval/golden/golden-v2.yaml` contiene 50 casos (40 positivos/10
    negativos), 59 `acceptable_facts` tipados y 128 proyecciones
    deterministas; `backend/scripts/t616b_materialize.py` reproduce el YAML,
    valida vocabulario cerrado, cardinalidad, desempate, huellas y fuentes, y
    re-verifica las 128 proyecciones mediante 44 consultas oficiales únicas.
    `pilot-016` quedó resuelto con el CSV oficial de 4-72: `153420` urbano y
    `153427` rural, conservando además los valores tipados por Socrata
    `153.42/153.427`; `pilot-038/039` se reescribieron con
    estación+sensor+hora explícitos. El loader acepta exclusivamente
    `golden-v1`/`golden-v2`; los reportes registran el nombre real de suite.
    `golden-v1` permanece byte a byte intacto. T-402 permanece formalmente
    cerrada como trabajo separado y T-617 no se inició dentro de T-616B.

- [ ] **T-617 Ejecutar puerta completa y mantener rollback (RNF-001…005/009).**
  - Orden: unitarias deterministas → no integración → aceptación determinista → integraciones compartidas → smoke 10 → golden-v1 50 → golden-v2 50 → aceptación legacy.
  - Comparar con T-610 y archivar commit/configuración/reportes.
  - **Puerta:** condiciones completas de `pruebas.md` §4.4. Al superarla, certificar `deterministic` como default técnico y autorizar el canary de T-701. Las corridas de evaluación no cuentan como tráfico real. El rollback desplegado se prueba en T-701 y RNF-001/RNF-009 con tráfico real se verifican en T-703; T-617 no puede afirmar esos dos resultados operativos.
  - **T-617B0-R3 (2026-07-18): falsa atribución corregida, PENDIENTE DE AUDITORÍA.** El smoke real de 10 casos (`eval run 9bebcb75-53d2-42f6-938f-bfbd45384128`, archivado sin reescribir) no pasó (`gate_passed=false`, 3/8 positivos) y diagnosticó `pilot-005-empleo-publico` como `intent_mismatch`/`owner=agent`, cuando el terminal real persistido fue `agent_runs.status="failed"` + `terminal_error_code="LLM_PROVIDER_ERROR"` (504 del proveedor en `build_plan`). Corregido exclusivamente en `backend/eval/` (`run.py`, `diagnostics.py`, `gate.py`): se propaga el terminal tipado, se clasifica como `failure_code=provider_error`/`failure_owner=infrastructure` (nunca `intent_mismatch`/`plan_invalid`), y ambas puertas (`smoke`/`full`) ganan una métrica bloqueante `infraestructura` que nunca permite `PASS` con corridas no evaluables, sin excluirlas de los denominadores ni contarlas como regresión semántica. Detalle completo en `backend/eval/reports/t617-gate-preflight.md` §M. No se ejecutó ningún LLM real en este incremento; el smoke real sigue sin reintentarse. T-617 sigue **abierta**; requiere auditoría y autorización explícita de Juan Camilo y del coordinador antes de reintentar el smoke.
  - **T-617B0-R3A (2026-07-18): huecos de atribución cerrados por auditoría, PENDIENTE DE AUDITORÍA.** Una auditoría de R3 encontró cinco huecos: (1) `STRUCTURED_OUTPUT_INVALID` estaba mal incluido como infraestructura/proveedor, contradiciendo `contracts/api-rest.md` §4 (es un fallo del contrato de ejecución del agente, no del proveedor; se reclasifica `owner=agent`/`failure_code=structured_output_invalid` y sigue bloqueando como regresión semántica de un sólido); (2) las excepciones del propio arnés de evaluación (`infrastructure_error`) conservaban `owner=agent` — ahora `owner=infrastructure`/`failure_code=harness_error`; (3) `golden_ambiguous` se evaluaba antes que los fallos no evaluables — ahora la prioridad es infraestructura/proveedor/ejecución → `STRUCTURED_OUTPUT_INVALID` → excepción del arnés → `golden_ambiguous` → semántica; (4) `INTERNAL` compartía el rótulo genérico de "proveedor" — ahora cada código tiene su propio `FailureCode` (`run_timeout`, `heartbeat_expired`, `worker_lost`, `internal_error`); (5) `HEARTBEAT_EXPIRED`/`WORKER_LOST` estaban documentados como soportados pero eran inalcanzables porque `run.py` solo leía `status="failed"` — se persisten con `status="interrupted"`; ahora se propagan ambos estados. R3 corrigió el 504 observado; R3A cierra los huecos que la auditoría encontró en esa misma corrección. Nueva prueba integral (`test_run_suite_propagates_provider_terminal_from_agent_run`) reproduce el punto originalmente defectuoso dentro de `run_suite`, no solo diagnóstico/puerta por separado; las 9 pruebas nuevas/actualizadas fallan contra el baseline `e74f3522853c75f6ae6ad984048bc327669602bb` (verificado en worktree temporal) y pasan tras la corrección. Detalle completo, tabla código→owner→failure_code→efecto en `backend/eval/reports/t617-gate-preflight.md` §N. Sin cambios en `backend/app/agent/**`, golden-v1/v2, preguntas, `expected_facts`, cardinalidades, umbrales ni contratos normativos; no se ejecutó ningún LLM/Socrata/PostgreSQL real; no se ejecutó el smoke ni ninguna otra corrida real. T-617 sigue **abierta**; requiere auditoría y autorización explícita de Juan Camilo y del coordinador antes de reintentar el smoke.
  - **T-617B0-R3B (2026-07-18): dos últimos falsos diagnósticos cerrados, PENDIENTE DE AUDITORÍA.** Auditoría de R3A encontró dos bordes: (1) `run_suite` ya propagaba `status="interrupted"`/`terminal_error_code="RUN_INTERRUPTED"`, pero `eval.diagnostics` no lo reconocía — caía en `expected_dataset_not_retrieved`/`owner=agent`; se agrega `FailureCode.RUN_INTERRUPTED="run_interrupted"` al mapeo de infraestructura (`owner=infrastructure`, bloquea `smoke`/`full` por la métrica `infraestructura`, no cuenta como regresión semántica de un sólido) y se corrige el comentario previo que describía `RUN_INTERRUPTED` como "cancelación": según `plan.md` §11 y `contracts/api-rest.md`, es el arranque idempotente del backend marcando corridas con lease de worker vencida (reinicio/despliegue), conservando evidencias/claims parciales. (2) el segundo intento de `_persist_case_result` (tras un primer fallo, p. ej. `agent_run_id` invalidado) se silenciaba con `except Exception: pass`, permitiendo que `results`/`outcomes` fingieran un caso persistido cuando `eval_case_results` nunca lo recibió, arriesgando un `PASS` con datos incompletos; se añade `EvalPersistenceError(RuntimeError)` tipada que se propaga sin silenciarse (con `case_id`/`agent_run_id`/nombres de excepción, sin datos sensibles), abortando la corrida completa antes de generar reporte o veredicto, mientras el `finally` existente sigue liberando `worker`/`engine`. Nuevas/ampliadas pruebas: parametrización de `test_run_suite_propagates_provider_terminal_from_agent_run` con `RUN_INTERRUPTED`, y `test_run_suite_aborts_when_persistence_fails_twice`; las 3 fallan contra el baseline `2f87a3c5959c9fc8b32961aca411445ba3c85704` (verificado en worktree temporal) y pasan tras la corrección. Detalle completo en `backend/eval/reports/t617-gate-preflight.md` §O. Sin cambios en `backend/app/agent/**`, golden-v1/v2, preguntas, `expected_facts`, cardinalidades, umbrales ni contratos normativos; no se ejecutó ningún LLM/Socrata/PostgreSQL real; no se ejecutó el smoke. T-617 sigue **abierta**; requiere auditoría y autorización explícita de Juan Camilo y del coordinador antes de reintentar el smoke.
  - **T-617B0-R4 (2026-07-19): presupuesto acotado de razonamiento del planificador Gemini, PENDIENTE DE AUDITORÍA.** Diagnóstico causal (`backend/eval/reports/t617b-d1-pilot005-timeout-diagnosis.md`, T-617B-D1) confirmó por aritmética exacta que el timeout de `build_plan` en `pilot-005-empleo-publico` (`timeout=30` × 2 intentos + 2 s de espera) reproduce sin residuo el patrón de ~65 s observado en dos smokes reales. Evidencia adicional aportada (sin ejecutar llamadas reales en este incremento): sin `thinking_budget`, una prueba mínima de disponibilidad del modelo (sin el payload real de `build_plan`) respondió en 1.418 s, pero `build_plan` real de `pilot-005` agotó los dos intentos con 504 tres veces; con `thinking_budget=1024` `build_plan` respondió en 5.17 s con salida estructurada inválida; con `thinking_budget=4096` `build_plan` respondió en 2.899 s con salida estructurada válida. Implementado en `backend/app/agent/deterministic_dependencies.py`: nueva constante `PLANNER_THINKING_BUDGET_TOKENS=4096`, `_model()` gana un parámetro `thinking_budget` opcional que solo se envía cuando `settings.llm_provider=="google"` (Anthropic no lo expone), aplicado únicamente a la construcción de `planner_model` — intent, síntesis y plan de síntesis quedan sin cambios. `timeout=30`/`max_retries=2` intactos para las 4 llamadas y ambos proveedores; sin excepciones por `case_id`/`dataset_id`/pregunta. `normalize_system_owned_operation` (`app/agent/deterministic_runtime.py:463`) sigue aplicándose de forma incondicional tras cada `dependencies.plan` real, sin cambios ni duplicación de su cobertura existente (`tests/test_llm_contracts.py`). 6 pruebas nuevas/actualizadas en `tests/test_deterministic_dependencies.py` (dobles y claves falsas, cero red) fallan contra el baseline `e493e447a5101b8ca4f0627f45c514a091a66724` (verificado en worktree temporal: `ImportError` de `PLANNER_THINKING_BUDGET_TOKENS`) y pasan con la implementación. Detalle completo, tabla proveedor/rol/thinking_budget y resultados exactos en `backend/eval/reports/t617b0-r4-planner-thinking-budget.md`. `golden-v1`/`golden-v2` sin cambios (SHA-256 idénticos). Limitación pendiente: solo una corrida real posterior podrá confirmar calidad y latencia extremo a extremo; no se ejecutó `pilot-005` ni el smoke en este incremento. T-617 sigue **abierta**; requiere auditoría y autorización explícita de Juan Camilo y del coordinador antes de reintentar `pilot-005`.
  - **T-617B0-R4A (2026-07-19): thinking_budget acotado a gemini-2.5-flash, PENDIENTE DE AUDITORÍA.** La evidencia de R4 (thinking_budget=1024→salida inválida, thinking_budget=4096→salida válida) se produjo específicamente contra `gemini-2.5-flash`; R4 aplicaba el presupuesto a cualquier modelo Google configurado. Corregido: nueva constante `_PLANNER_THINKING_BUDGET_LLM_MODEL="gemini-2.5-flash"`; `_model()` solo agrega `thinking_budget` cuando `settings.llm_provider=="google"` **y** `settings.llm_model=="gemini-2.5-flash"` — otro modelo Google (p. ej. `gemini-2.5-pro`) queda sin `thinking_budget`, igual que Anthropic. `timeout=30`/`max_retries=2`, prompts, schemas, `normalize_system_owned_operation`, golden-v1/v2 y contratos sin cambios. Nueva prueba parametrizada `test_thinking_budget_is_scoped_to_google_gemini_2_5_flash` (3 casos: google/gemini-2.5-flash→4096, google/gemini-2.5-pro→ninguno, anthropic→ninguno) falla contra el baseline `c5cda0d184e1b74eac162ca4d7e19d396e720d39` en el caso `google/gemini-2.5-pro` (verificado en worktree temporal) y pasa con la corrección. Se corrige además el comentario de R4 sobre los 1.418 s: corresponden a una prueba mínima de disponibilidad del modelo, no a una llamada `build_plan`. Detalle completo en `backend/eval/reports/t617b0-r4-planner-thinking-budget.md` §10. `golden-v1`/`golden-v2` sin cambios. No se ejecutó Gemini, Socrata, `pilot-005` ni el smoke. T-617 sigue **abierta**; requiere auditoría y autorización explícita de Juan Camilo y del coordinador antes de reintentar `pilot-005`.
  - **T-617B-C6 (2026-07-19): fronteras de presupuesto y observabilidad T5 corregidas, LISTO PARA SMOKE DESDE COMMIT LIMPIO; T-617 sigue abierta.** La auditoría forense del smoke `706bd396-c07e-408a-a163-d8eb4cafebc8` separó siete `budget_exceeded`: reparaciones (`pilot-002/003/013`), llamadas LLM (`pilot-005/012/021`) y candidatos (`pilot-038`). El supervisor aplicaba límites globales antes de permitir que el resultado de la última acción autorizada avanzara por pasos deterministas: el octavo candidato no llegaba a perfilado, el último plan LLM no llegaba a validación/consulta y la última consulta no llegaba a calidad/claims. Corregido sin elevar presupuestos: cada límite se comprueba únicamente antes de iniciar otra acción que lo consume; agotadas las reparaciones se intenta otro candidato disponible; agotado LLM después de claims válidos se usa el fallback determinista verificable. La exploración real recibe el saldo exacto y no puede rebasar `max_explorations` probando varias variantes; solo filtros textuales categóricos `EQ/IN` requieren exploración. La protección de entidad acepta identificadores institucionales inequívocos sin relajar contradicciones. El paso determinista `execute_query` ahora persiste en el mismo `AgentStep` el `tool_input`, resultado crudo resumido, latencia y error de T5, permitiendo medir Socrata en vez de `0/0`, incluso si calidad/claims rechazan después la evidencia. Sin reglas por caso/dataset/pregunta, sin cambios en golden, `expected_facts`, umbrales, cardinalidades, contratos, migraciones ni presupuestos. Verificación: `1071 passed` no integración; aceptación determinista PostgreSQL `19 passed`; integración local `114 passed, 1 skipped`; aceptación legacy `7 passed`; Ruff limpio; hashes golden intactos. Informe: `backend/eval/reports/t617b-c6-budget-boundaries-and-t5-observability.md`. No se ejecutó una corrida real nueva; el siguiente paso permitido es un único smoke canónico desde el commit consolidado y limpio. `full` permanece bloqueada hasta que ese smoke pase.
  - **T-617B-C7 (2026-07-19): smoke C6 auditado; capacidad textual, corte de observación y T4 corregidos; LISTO PARA VALIDACIÓN DIRIGIDA, T-617 sigue abierta.** El smoke real `a2f6d778-87f8-45e5-a674-1b705dbd4732` sobre `0781ee5` mejoró de `0/8` a `2/8` positivos, confirmó `pilot-002`/`pilot-005`, negativos `2/2`, Socrata observable `5/5`, cero infraestructura/fabricaciones/huérfanas e integridad de claims al 100%; siguió en `FAIL` por dos sólidos. `pilot-003` convirtió indebidamente la fecha de observación de la fuente en filtros de año/semana de las filas; se añadió una normalización genérica y conservadora que solo retira esos filtros cuando la fecha está ligada a la observación/consulta/actualización de la fuente y no fue solicitada como periodo fuera de esa cláusula. `pilot-013` encontró la fila textual correcta, pero el smoke había desactivado la capa textual que `pruebas.md` §4.5 exige para ese caso; el runner ahora rechaza `golden-v2` `smoke/full` antes de crear engine o invocar LLM si no se activa explícitamente `--textual-facts-enabled` (sin cambiar el default productivo; `directed` y golden-v1 conservan compatibilidad). `explore_value` persiste entrada/salida/latencia/error para explicar T4 y `socrata_success_rate` queda acotada exclusivamente a `execute_query`, sin contaminar T5. Sin reglas por caso/dataset/fecha concreta, sin cambios en golden, expected_facts, presupuestos, umbrales, contratos ni migraciones. Verificación: focalizadas `139 passed`; no integración `1083 passed`; aceptación determinista PostgreSQL `19 passed`; integración compartida `95 passed, 1 skipped`; legacy `7 passed`; Ruff/formato/diff limpios; hashes golden intactos. Informe: `backend/eval/reports/t617b-c7-textual-capability-source-cutoff-and-t4-observability.md`. Siguiente paso: una evaluación `directed` conjunta de `pilot-003` y `pilot-013` con hechos textuales habilitados; solo si ambos son útiles/verificables y sin bloqueos reales se repite el smoke canónico. `full` sigue bloqueada.
  - **T-617B-C8 (2026-07-19): evidencia textual verificable de una fila preservada sin depender de una solicitud auxiliar del LLM; LISTO PARA VALIDACIÓN DIRIGIDA, T-617 sigue abierta.** La corrida dirigida `e22d108c-fab5-44be-bb81-b7bd1420d441` confirmó `pilot-003` y, para `pilot-013`, recuperó el dataset correcto, ejecutó el `LOOKUP` por código y obtuvo la fila exacta con tipo y nombre, pero descartó esa evidencia porque el planificador omitió `textual_requests` y el pipeline intentó convertir dimensiones de texto en claims numéricos. Se añadió un fallback genérico `direct_text` que solo opera con `LOOKUP`, ausencia de solicitud textual explícita y exactamente una fila devuelta por el ejecutor interno; selecciona únicamente dimensiones `TEXT` cuyo nombre real coincide léxicamente con la intención y excluye aliases cuantitativos. No elige entre filas, no decide por valores y no contiene literales de caso/dataset/pregunta. Con varias filas o campos no solicitados continúa el rechazo. Verificación: focalizadas `60 passed`; aceptación focalizada PostgreSQL `1 passed`; no integración `1085 passed`; aceptación determinista `19 passed`; integración compartida `95 passed, 1 skipped`; legacy `7 passed`; prueba principal fallo→pasa contra `ac7c589`; Ruff/formato/diff limpios; hashes golden intactos. Informe: `backend/eval/reports/t617b-c8-single-row-textual-fallback.md`. Siguiente paso: una única corrida real `directed` de `pilot-013` con hechos textuales habilitados; `full` sigue bloqueada y el smoke solo puede repetirse si esa validación es útil, verificable y sin bloqueos reales.
  - **T-617B-C8A (2026-07-19): falso negativo por representación canónica del SoQL corregido; LISTO PARA UNA REVALIDACIÓN DIRIGIDA, T-617 sigue abierta.** La corrida dirigida `80d4bbec-2ea7-4d71-ba21-d22bb86218dd` sobre `c9009ae` volvió a recuperar en `tmk8-iihq` exactamente una fila correcta para `PRY00062`, sin infraestructura ni fabricación, pero el fallback C8 no se activó: comparaba literalmente el SoQL renderizado interno (minúsculas, sin `OFFSET`) con la forma persistible que devuelve el parser de `ejecutar_soql` (keywords normalizadas y `OFFSET 0`). La consulta era la misma; el doble unitario ocultó la diferencia al devolver el payload sin normalizar. Se elimina únicamente esa comparación de representación: el ejecutor interno sigue recibiendo exclusivamente el SoQL renderizado y su salida canónica continúa siendo la fuente de evidencia/hash/reverificación. La regresión reproduce ahora la forma real normalizada. Verificación: focalizadas `60 passed`; no integración `1085 passed`; aceptación determinista PostgreSQL `19 passed`; Ruff/formato/diff limpios. Un primer intento de aceptación sin `DATABASE_URL` produjo solo errores de setup; repetido con la URL local exportada al proceso pasó completo. No se cambiaron golden, `expected_facts`, presupuestos, umbrales ni contratos. Informe ampliado: `backend/eval/reports/t617b-c8-single-row-textual-fallback.md` §C8A. `full` y smoke siguen bloqueados hasta una única revalidación dirigida satisfactoria.
  - **T-617B-C8B (2026-07-19): revalidación real de `pilot-013` satisfactoria; LISTO PARA SMOKE CANÓNICO, T-617 sigue abierta.** La única revalidación dirigida posterior (`e57d5421-017e-4f54-972e-3ae7b9e3dc9d`, `agent_run_id=3a52133f-b776-4118-8da2-e724d71c963b`, commit `d16b1e1`) completó con el dataset `tmk8-iihq`, filtro exacto `codigo='PRY00062'`, una fila y dos hechos `direct_text` persistidos/reproducibles para el nombre `IP Ibagué - Cajamarca` y el tipo `Iniciativa Privada sin Recursos Públicos`. Integridad textual 100%, cero fabricación/huérfanos/infraestructura; latencia 12.654 s, costo estimado USD 0,005642. La redacción “El valor observado es…” es genérica pero correcta y consultable, por lo que queda como advertencia de presentación no bloqueante conforme a RF-211. Informe ampliado: `backend/eval/reports/t617b-c8-single-row-textual-fallback.md` §C8B. Siguiente paso único: un smoke canónico de 10 casos con hechos textuales habilitados; `full` permanece bloqueado hasta que ese smoke pase.
  - **T-617B-C9 (2026-07-19): smoke C8 auditado; cinco salidas estructuradas inválidas estaban mal tipadas como infraestructura; FULL BLOQUEADO, T-617 sigue abierta.** El smoke formal `1aca0753-0db3-4b7c-81e6-3a46e8a101f5` sobre `fd6cc70` ejecutó 10/10 canónicos: `pilot-003` y `pilot-013` pasaron, negativos 2/2, cero fabricaciones/huérfanas, integridad aplicable 100% y Socrata 3/3; `pilot-022` quedó indeterminado por `expected_fact_not_found`. Los cinco supuestos fallos de infraestructura (`pilot-002/005/012/021/038`) no fueron caídas de Gemini: sus eventos prueban respuestas recibidas que no validaron contra `EnumeratedPlanSelection` (texto libre en `textual_requests`; en `pilot-005`, métricas `operation="lookup"`). `ainvoke_structured_chat_model` lanzó `LLMStructuredOutputError`, pero el runner determinista capturó su clase padre `LLMProviderError` y persistió incorrectamente `LLM_PROVIDER_ERROR`, contradiciendo la atribución contractual R3A. Siguiente incremento: tipar honestamente `STRUCTURED_OUTPUT_INVALID` y normalizar solo representaciones opcionales semánticamente recuperables, sin convertir explicaciones en hechos ni inventar columnas/filtros; pruebas fallo→pasa sin red, luego validación dirigida de los cinco afectados y solo después otro smoke. Informe: `backend/eval/reports/t617b-c9-structured-output-boundary-diagnosis.md`. Sin cambios en golden, `expected_facts`, presupuestos, umbrales o contratos; `full` permanece bloqueado.
  - **T-617B-C9A (2026-07-19): frontera estructurada corregida de forma conservadora; LISTO PARA VALIDACIÓN DIRIGIDA DE CINCO CASOS, T-617 sigue abierta.** `EnumeratedPlanSelection` descarta exclusivamente elementos no estructurados dentro de la lista opcional `textual_requests` (nunca interpreta su texto) y, solo bajo operación superior `LOOKUP`, traslada a dimensiones métricas `lookup` que contienen únicamente un índice integral no negativo; cualquier objeto textual inválido, campo extra, forma fuera de lista u operación contradictoria sigue fallando. El runner captura ahora `LLMStructuredOutputError` antes que `LLMProviderError` y persiste `STRUCTURED_OUTPUT_INVALID`, responsabilidad del agente, conservando la ruta de infraestructura para red/cuota/5xx reales. Fallo→pasa contra `7ac6b18`: `2 failed` de normalización y `1 failed` de atribución; tras el cambio, focalizadas `47 passed`, no integración `1091 passed`, aceptación determinista PostgreSQL `20 passed`, integraciones compartidas `95 passed, 1 skipped`, legacy `7 passed`; Ruff/formato/diff limpios y hashes golden intactos. Informe ampliado: `backend/eval/reports/t617b-c9-structured-output-boundary-diagnosis.md` §C9A. Siguiente paso único: validación `directed` conjunta de `pilot-002/005/012/021/038`; smoke y `full` permanecen bloqueados.
  - **T-617B-C9B (2026-07-19): validación real confirma C9A y descubre la siguiente frontera T4; SMOKE/FULL BLOQUEADOS, T-617 sigue abierta.** La corrida dirigida `54f9e4df-eb31-4f89-9999-7a2eae311aa9` sobre `ff5b5f9` recuperó `pilot-002` y `pilot-005`; los cinco casos quedaron sin `LLM_PROVIDER_ERROR` ni `STRUCTURED_OUTPUT_INVALID`, confirmando la corrección. `pilot-012/021/038` aún agotaron presupuestos sin ejecutar T5. La traza demuestra dos causas genéricas: (1) el orden/corte de variantes T4 usa frase original, frase sin tildes y solo después tokens sin tildes, por lo que nunca prueba `"Regular"` para `"Auditoría Regular"` ni el token original `"Alcalá"` para `"Alcalá (Valle)"`, aunque los datasets esperados estaban primeros; (2) el dataset esperado de `pilot-038` sufrió un transporte Socrata fallido tras el reintento interno, pero el runtime redujo el código a `ValueError`, descartó el candidato y lo reportó como agotamiento semántico, contradiciendo los terminales `SOCRATA_TIMEOUT`/`SOCRATA_ERROR` de plan.md §11. El agregado registra además una cifra huérfana que debe localizarse antes de otra puerta. Informe ampliado: `backend/eval/reports/t617b-c9-structured-output-boundary-diagnosis.md` §C9B. Siguiente incremento: priorizar tokens originales en T4, preservar terminales Socrata tipados y auditar la cifra huérfana; sin cambiar presupuestos/golden/umbrales.
  - **T-617B-C10 (2026-07-19): frontera T4, terminales Socrata e integridad mixta corregidos; LISTO PARA VALIDACIÓN DIRIGIDA, T-617 sigue abierta.** La exploración conserva el mismo presupuesto pero prioriza la frase y tokens Unicode originales antes de variantes sin diacríticos, permitiendo probar términos materiales que el corte anterior omitía sin reglas por caso/dataset/pregunta. `SOCRATA_TIMEOUT`/`SOCRATA_ERROR` de `explorar_valores` se conservan mediante un error tipado: no se degradan a rechazo de candidato ni `CANDIDATE_BUDGET_EXCEEDED`; el paso T4 y el único terminal `failed` persisten el código real, reintentable y sin evidencia/claims. La cifra huérfana de C9B era `2026`, ya respaldada por el `display_value` de un hecho textual persistido; el arnés ahora admite `textual_facts[].display_value` como respaldo, pero un control ausente de claims y hechos sigue bloqueando. Fallo→pasa contra `bfbde28`: `1 failed, 1 passed` en integridad mixta y dos errores de colección por contratos inexistentes; con C10, focalizadas `94 passed`, terminal PostgreSQL `1 passed`, no integración `1097 passed`, aceptación determinista `21 passed`, integraciones compartidas `94 passed, 2 skipped`, legacy `7 passed`; Ruff/diff limpios y hashes golden intactos. Informe: `backend/eval/reports/t617b-c10-exploration-boundary-and-mixed-integrity.md`. Siguiente paso único: evaluación real `directed` conjunta de `pilot-012/021/038`; smoke y `full` permanecen bloqueados hasta auditarla.
  - **T-617B-C10A (2026-07-19): cantidades publicadas y terminales Socrata diagnosticados y corregidos genéricamente; LISTO PARA VALIDACIÓN DIRIGIDA DE DOS CASOS, T-617 sigue abierta.** La corrida real `f0d780fe-6514-4ede-9085-8aa677e3e1f4` confirmó que C10 alcanzó T5, pero `pilot-012` y `pilot-021` ejecutaron `count(*)` aun cuando el esquema publicaba la cantidad solicitada en una columna numérica (`hallazgos_administrativos` y `cantidad`); además, `pilot-012` omitió la entidad institucional ya identificada literalmente en `administrative_terms`. La normalización ahora convierte `COUNT` a `LOOKUP` únicamente cuando pregunta y esquema permiten resolver una medida numérica inequívoca, conserva cardinalidad para filas/registros y ante cualquier empate, y recupera una entidad solo desde un término institucional ya emitido, literal en la pregunta y respaldado por una columna semántica de entidad. No hay reglas por caso, dataset, cifra o pregunta. El terminal real de `pilot-038`, `SOCRATA_TIMEOUT`, se atribuye ahora a infraestructura (`socrata_timeout`) en vez de degradarse a `intent_mismatch`; el recálculo de solo lectura sobre la corrida persistida lo confirmó. Auditoría offline sobre ambos golden: la conversión solo se activa para `pilot-021` en v1 y `pilot-012/021` en v2; controles de puestos, calzadas, ventas, pagos y cardinalidad permanecen `COUNT`. Verificación: focalizadas `151 passed`, no integración `1109 passed`, aceptación determinista PostgreSQL `21 passed`, integraciones compartidas `95 passed, 1 skipped`, legacy `7 passed`; Ruff/formato/diff limpios y hashes golden intactos. Informe: `backend/eval/reports/t617b-c10a-direct-quantity-and-socrata-diagnostics.md`. Siguiente paso único: evaluación real `directed` de `pilot-012/021`; después, si ambos son útiles, verificables y materialmente correctos, repetir el smoke canónico. `full` permanece bloqueado.
  - **T-617B-C10B (2026-07-20): preflight de capacidad textual formal cerrado; LISTO PARA SMOKE CANÓNICO CON FLAG EXPLÍCITO, T-617 sigue abierta.** La validación real `directed` `aeac0584-c748-4ce3-9dd0-adaf8d800473` confirmó C10A (`pilot-012=12`, `pilot-021=65`, fuentes/entidades correctas, claims 100% reproducibles, T5 2/2 observable). El smoke posterior `21ea1576-3242-4a89-b2d7-4f5a3b87f1ac` conservó negativos 2/2, cero fabricaciones/huérfanas e integridad completa, pero su veredicto mecánico fue FAIL por `pilot-013`: el primer dataset esperado ejecutó la consulta correcta y devolvió `PRY00062`, `IP Ibagué - Cajamarca` e `Iniciativa Privada sin Recursos Públicos`, pero el comando había dejado `DETERMINISTIC_TEXTUAL_FACTS_ENABLED=false`; el runtime descartó correctamente valores textuales no habilitados y agotó candidatos. Era una configuración formal inválida, no una regresión semántica. `_validate_eval_capabilities` ahora rechaza antes de crear engine/LLM cualquier `smoke` o `full`, v1 o v2, con la capacidad textual apagada; `directed` conserva ambos modos. Esto aplica `pruebas.md` §4.4/T-615: `pilot-013` debe probar valores textuales, nunca el surrogate histórico `count=1`. Verificación: focalizadas `30 passed`, no integración `1113 passed`, aceptación determinista PostgreSQL `21 passed`, integraciones compartidas `95 passed, 1 skipped`, legacy `7 passed`; Ruff/formato/diff limpios y hashes golden intactos. Informe: `backend/eval/reports/t617b-c10b-formal-textual-capability-preflight.md`. Siguiente paso único: smoke canónico `golden-v1 --textual-facts-enabled`; `full` permanece bloqueado hasta que pase.
  - [ ] **T-617C Etiquetar cifras relevantes y persistir advertencias de presentación (RF-208/RF-211).** La corrida diagnóstica real de `pilot-005-empleo-publico` (`agent_run_id=72143e94-2216-471d-95b3-b2b2c090f629`) preservó correctamente la entidad y produjo evidencia/claims reproducibles, pero sintetizó «764, 719, 2.026 y 2» sin indicar cuáles cifras eran hombres, mujeres, año y código SIGEP. La observación quedó únicamente en `backend/eval/reports/t617b0-pilot005-directed-retry.md`, no en una advertencia estructurada ni visible al usuario. Implementar una solución genérica, sin reglas por `case_id`, `dataset_id`, pregunta o cifra, que: (a) asocie cada `display_value` presentado con una etiqueta humana verificable derivada de la semántica de su columna/claim; (b) priorice los claims que responden materialmente la intención y excluya de la narrativa principal identificadores técnicos o dimensiones auxiliares que el usuario no pidió; (c) nunca invente una etiqueta ausente de la evidencia; y (d) cuando no pueda producir una asociación inequívoca, persista y exponga una advertencia de presentación mediante un campo contractual existente o, si no existe, detenga la implementación hasta aprobar la mínima enmienda de contrato necesaria. La respuesta parcial y verificable sigue siendo entregable conforme a RF-211: esta advertencia no debe convertir por sí sola la corrida en `no_evidence`.
    - **Aceptación mínima:** pruebas deterministas demuestran una salida equivalente a «764 hombres y 719 mujeres» a partir de columnas semánticas, ausencia de año/código no solicitados en la narrativa principal, fallback verificable sin etiquetas fabricadas, advertencia persistida y visible cuando el etiquetado sea ambiguo, y cero regresiones en claims, cifras huérfanas, hechos textuales y respuestas negativas. La solución debe probarse con fixtures genéricos además del caso disparador.
    - **Planificación:** no bloquea la repetición del smoke ni altera sus métricas, cardinalidades o golden; permanece pendiente y debe auditarse antes de cerrar T-617 y autorizar T-701.
    - [x] **T-617C-A (2026-07-19): enmienda documental del contrato de presentación, APROBADA — solo documentación, T-617C sigue abierta.** Fase A de T-617C (`backend/eval/reports/t617c-semantic-claim-labels.md`) confirmó que no existía ningún campo contractual vigente para persistir una advertencia de presentación distinto de `evidence[].quality.warnings_user` (calidad de evidencia, explícitamente descartado por alcance) y que corregirlo exigía o bien una migración de `quantitative_claims` o bien ampliar `agent_runs.final_answer` (JSONB ya existente, sin migración). Se aprobó la segunda alternativa: `research.md` §29 registra la decisión y las alternativas evaluadas; `spec.md` añade **RF-212** (contrato aprobado, implementación pendiente) y dos términos de glosario; `plan.md` §15 documenta el flujo objetivo de etiquetas (`ValidatedQueryPlan` → alias SoQL → claims → advertencia) y la ausencia de migración; `contracts/api-rest.md` §4c define el esquema público exacto (`claims[].label`, `claims[].label_status`, `presentation_warnings[]`, ambos aditivos y retrocompatibles) con ejemplos JSON; `data-model.md` aclara que `columns_used` DEBE ser el nombre de columna real (nunca el alias `dim_N`/`metric_N`, discrepancia código-contrato preexistente ya documentada por los ejemplos previos de `api-rest.md`) y que los tres campos nuevos no tocan la tabla relacional `quantitative_claims`; `contracts/validacion-calidad.md` §6 separa explícitamente `presentation_warnings` de `warnings_user`; `pruebas.md` §4.6 registra la matriz de aceptación determinista y de contrato que deberá satisfacer la implementación. No se modificó `constitution.md` (RF-208/RF-211 ya daban fundamento suficiente, sin contradicción normativa demostrable). No se implementó código, pruebas ni migraciones; `golden-v1`/`golden-v2` sin cambios (SHA-256 idénticos verificados antes/después). Detalle completo en `backend/eval/reports/t617c-a-presentation-contract-amendment.md`. T-617C permanece **abierta** para implementación y su propia auditoría; T-617 y T-701 siguen bloqueadas.
    - [x] **T-617C-R2 (2026-07-19): el verificador de reproducibilidad del arnés (`eval/gate.py`) se puso al día con el contrato RF-212 de T-617C-R1; cierra el falso PASS del smoke — T-617C sigue abierta.** El smoke real `fd2da0f1-94b6-47e4-8ddc-30926266f944` (post T-617C-R1) obtuvo `claims_reproducible=0.0%` con `gate_passed=true`: `_reproduce_one_claim` construía `ClaimSpec.columns` directamente con el nombre público real persistido (`["genero_hombre"]`), pero `evidence.rows` sigue indexada por el alias de ejecución SoQL (`["dim_2"]`) — todo claim cuantitativo se rechazaba como no reproducible, sin que ninguna métrica del smoke lo bloqueara. Corregido de forma mínima: `_reproduce_one_claim` intenta primero el camino directo (compatibilidad con claims previos a R1, donde el nombre público ya es alias/clave de fila); si no coincide, reconstruye el alias real parseando `evidence.soql_query` con el parser SoQL vigente (`app.tools.soql_parser.extract_column_field_names`, sin regex improvisada) — el mismo mecanismo que ya usa `persistence._reverify_quantitative_synthesis_fact` en la ruta productiva — y arma `ClaimSpec(columns=<alias>, column_field_names=<alias→nombre real>)` sin reimplementar el DSL ni el algoritmo de hash. Una columna pública sin alias resoluble falla (`column_unresolvable`); dos alias distintos para el mismo nombre real fallan por ambigüedad (`column_mapping_ambiguous`, nunca selección silenciosa del último); un SoQL no parseable falla con `soql_unparseable`, nunca una excepción sin controlar. Se agregó una métrica bloqueante nueva a `evaluate_smoke_gate`, `integridad_claims`: todo `CaseOutcome` con `claims_integrity.applicable=true` debe tener `integrity_ok=true` (cobertura=100%, reproducibles=100%, huérfanas=0, RF-208/RNF-003); antes de esta ronda ninguna métrica del smoke miraba esa condición. 11 pruebas nuevas en `tests/test_eval_gate.py` (formato R1 con filas alias-keyed, fórmula con alias interno, centinela `count(*)`, camino directo de compatibilidad, columna inexistente, SoQL inválido, mapeo ambiguo, manipulación de `raw_value`/`display_value`/`source_hash`, smoke con integridad rota que no aprueba, smoke con integridad completa que sí aprueba, resto de métricas del smoke intactas) fallan contra el baseline `8bfd7501ad9afb7341a1447f925df0c0735b0470` (verificado en worktree temporal: 10 fallos exactos, incluido el smoke aprobando indebidamente con integridad rota) y pasan con la corrección. Recalculado sin gastar cuota (script temporal fuera del repositorio, solo lectura, sin Gemini/Socrata/nueva corrida) sobre los `final_answer` ya persistidos del smoke real citado: los 7 casos con claims cuantitativos (211 claims en total, incluidos los 200 de `pilot-038-precipitacion`) ahora reproducen al 100% (`claims_reproducible=1.0`, `integrity_ok=true` en los 7), y `evaluate_smoke_gate` recalculado sobre esos mismos resultados certifica `gate_passed=true` de forma genuina, no por un hueco del verificador. `golden-v1`/`golden-v2` sin cambios (SHA-256 idénticos). No se tocó `backend/app/agent/**` ni el runtime productivo, ninguna migración, ningún contrato público ni umbral/cardinalidad de la puerta `full`. Pendiente separado, no corregido en esta ronda: `evidence[].quality.warnings_user` de `pilot-005-empleo-publico` sigue exponiendo el alias interno `dim_5` y describiendo "38% de registros" cuando en realidad son celdas — deuda de presentación de `app/quality/validator.py`/`messages_es.py`, ajena a T-617C. Detalle completo en `backend/eval/reports/t617c-r2-eval-claim-reproducibility.md`. T-617C permanece **abierta**; T-617 y T-701 siguen bloqueadas.

  - **T-617B-C10C (2026-07-19): etiquetas textuales de agregados de una fila corregidas genéricamente; LISTO PARA VALIDACIÓN REAL DIRIGIDA, T-617 sigue abierta.** El smoke real `43e3cac4-5939-48ab-96d5-e00f46a0b9ed` obtuvo `gate_passed=true`, positivos `7/8`, negativos `2/2`, T5 `8/8`, claims 100% reproducibles y cero fabricaciones/huérfanas/infraestructura. Sin embargo, la auditoría humana bloqueó `full`: `pilot-002` respondió solo `Cantidad: 66.723.` aunque la fila respaldada contenía `VALLE DEL CAUCA`, y `pilot-003` respondió solo `Conteo: 1.470.739.` aunque contenía `AGRESIONES POR ANIMALES POTENCIALMENTE TRANSMISORES DE RABIA`. `_prepare_single_row_textual_fallback` amplía el fallback verificable ya existente: para cualquier operación con exactamente una fila, sin solicitud textual explícita, deriva `direct_text` solo de dimensiones TEXT cuyo nombre real fue pedido por la intención; no elige entre varias filas, no promueve columnas no solicitadas, no afirma ganador único y no usa reglas por caso/dataset/pregunta/cifra. El caso positivo falló contra el baseline (`1 failed, 33 passed`) y pasa tras la corrección; focalizadas `34 passed`, no integración `1116 passed`, aceptación determinista PostgreSQL `21 passed`; Ruff/formato/diff limpios y hashes golden intactos. Informe: `backend/eval/reports/t617b-c10c-single-row-group-labels.md`. Siguiente paso único: corrida real `directed` de `pilot-002/003` con capacidad textual; después, si ambas respuestas incluyen categoría y cifra verificables, repetir el smoke canónico. `full` permanece bloqueado.
  - **T-617B-C10D (2026-07-19): falso positivo de cifras huérfanas en etiquetas estructurales cerrado; FULL BLOQUEADO, T-617 sigue abierta.** La validación real dirigida `845a1c48-93c4-4b66-a590-1f986b4b14fb` confirmó que C10C preserva categoría+cifra en `pilot-002/003`; el smoke `d5ee908e-abe9-49fa-989a-1630818b8f85` pasó 10/10 con negativos 2/2, cero fabricaciones/huérfanas/infraestructura e integridad completa. El full `1b405332-aa8c-4870-a35c-a62875ecd599` ejecutó 50/50 y quedó bloqueado (positivos 15/40, negativos 10/10, tres fallos de infraestructura y p95 simple/multipaso sobre umbral). Sus dos supuestas cifras huérfanas eran `5` y `16` dentro de la etiqueta verificada derivada de la columna real `tasa_matriculacion_5_16`, no cantidades fabricadas. El arnés ahora admite números de una etiqueta solo si `derive_claim_label(claim.columns)` reproduce exactamente `label` y `label_status="verified"`; etiquetas ambiguas o manipuladas no pueden blanquear cifras. El control falló contra el baseline (`1 failed, 76 passed`) y pasa tras el fix (`77 passed`); no integración `1119 passed`. Recálculo read-only de los 50 resultados: 2.530/2.530 claims reproducibles, 298 cifras, huérfanas 2→0, cobertura 99,58→100 %, cero casos con integridad rota. Sin cambios en runtime, golden, `expected_facts`, presupuestos, umbrales o contratos. Informe: `backend/eval/reports/t617b-c10d-verified-label-orphan-integrity.md`. Golden-v2 permanece bloqueado; siguiente incremento: diagnóstico causal común de fallos semánticos/presupuesto/selección en el full, sin relajar la puerta.
  - **T-617B-C11 (2026-07-19): la protección anti-sustitución de entidad deja de confundir códigos auxiliares con entidades; LISTO PARA VALIDACIÓN DIRIGIDA, T-617 sigue abierta.** La auditoría de los nueve `budget_exceeded` del full mostró que `pilot-008/016/020/034` ya habían encontrado `Villamaría`, `Rondón`, `Medellín` o `Jepirachi`, pero R5/R5A rechazaba el plan porque la mera existencia de cualquier columna `codigo/id` activaba una exigencia de filtro por identificador, aunque el valor estuviera correctamente anclado en `municipio` o `nombre_proyecto`. La frontera ahora reconoce cualquier filtro `EQ/IN` que preserve inequívocamente la entidad; sin ese anclaje, una columna institucional sigue siendo obligatoria y una columna de código solo activa la protección si la propia entidad contiene un identificador explícito (`PRY00062`, `código 123`). INPEC, `IN` mixtos, etiquetas genéricas y omisiones institucionales continúan bloqueados. El defecto territorial falla contra el baseline y pasa con la corrección; `test_llm_contracts.py` 58 aprobadas, no integración 1.121 aprobadas, aceptación determinista PostgreSQL 21 aprobadas, Ruff/diff limpios. Sin cambios en golden, `expected_facts`, presupuestos, umbrales o contratos. Informe: `backend/eval/reports/t617b-c11-entity-constraint-scope.md`. Siguiente paso: validación real dirigida de `pilot-008/016/020/034/036`; los otros presupuestos tienen fronteras distintas. Smoke/full y golden-v2 permanecen bloqueados.

## FASE 7 — Endurecimiento y despliegue

- [ ] **T-701 🧑 Desplegar el backend y probar canary/rollback.** Railway/Render: servicio desde `backend/` con las variables de `.env.example` como secrets (usa los valores reales de la Fase 0) y `AGENT_RUNTIME=deterministic` explícito. Conectar dominio `api.cuestiondedatos.com` (añadir CNAME en tu DNS). Crear el cron externo de retención cada 6 horas, registrar `ADMIN_TOKEN` y `RETENTION_HASH_SALT` como secretos, verificar la primera ejecución de `POST /v2/admin/retention/run` y revisar fallos. Ejecutar la secuencia desplegada de `pruebas.md` §4.4: smoke determinista → cambio explícito a `legacy` y smoke → restauración a `deterministic` y smoke. Archivar versión, configuración efectiva y los `run_id` sintéticos para excluirlos de T-703. La ventana de una versión del rollback empieza aquí.
  - ✅ `https://api.cuestiondedatos.com/v2/health` responde `ok` desde internet; primera ejecución de retención registrada; ambos runtimes produjeron un terminal en el entorno desplegado; `deterministic` quedó restaurado y la evidencia reversible quedó archivada.
- [ ] **T-702 🧑 Desplegar el frontend.** En Vercel, apuntar el proyecto existente a `frontend/` de la rama `v2` con `NEXT_PUBLIC_BACKEND_URL=https://api.cuestiondedatos.com` como variable de entorno; verificar que el CORS del backend incluye el dominio del preview de Vercel durante las pruebas. Probar en preview antes de promover a producción (cuestiondedatos.com).
  - ✅ ESC-01 funciona en el dominio de preview y luego en producción.
- [ ] **T-703 🧑 Monitoreo y prueba de tráfico real.** UptimeRobot sobre `/v2/health` cada 5 min con alerta a tu correo (RNF-006). Implementar y probar el agregado autenticado de `GET /v2/admin/metrics` definido en `contracts/api-rest.md`; después observar siete días consecutivos del runtime determinista desplegado. Excluir `retention_class=eval` y los `run_id` sintéticos de T-701; reportar tamaños de muestra, nulos, exclusiones, versión/runtime/modelo, p95 simple y multietapa y costo promedio. Aplicar los umbrales y la regla `INSUFFICIENT_EVIDENCE` de `pruebas.md` §4.4 sin mezclar tráfico controlado con tráfico de usuarios.
  - ✅ Monitor activo; endpoint agregado probado sin contenido de usuario; primer reporte archivado en `docs/` con ≥20 corridas simples, ≥20 multietapa, costo completo y veredicto `PASS` para RNF-001/RNF-009. Si el veredicto es `FAIL` o `INSUFFICIENT_EVIDENCE`, T-703 permanece abierta.
- [ ] **T-704 Retirar el endpoint v1.** Eliminar `pages/api/consultar_v2.js`, `utils/systemPrompt.js` y `utils/maestro_divipola.js` (ya reemplazados). Actualizar README.md raíz con la arquitectura v2.0 y CHANGELOG a `2.0.0`.
  - ✅ Sin referencias muertas (`grep` de los archivos borrados no encuentra imports).
- [ ] **T-705 🧑 Cierre documental.** Verificar los criterios de cierre de plan.md §10; capturar evidencia (reportes, métricas, screenshots) en `docs/`; preparar la actualización del registro en herramientas.datos.gov.co y del sitio divulgativo/producto público con la documentación de los nuevos componentes, sin convertir OE1/OE4 en funcionalidad de software.
  - ✅ Checklist de plan.md §10 completo y archivado.

---

## Dependencias entre fases

```
F0 ──▶ F1 ──▶ F2 ──▶ F3 ──▶ F4 ──▶ F5 ──▶ F7
                      └────────────▶ F6 ──▶ F7   (F6 puede iniciar al terminar F3/F4)
```

**Ruta backend ejecutable hasta T-403:** T-105 → T-102 → T-103 → T-104A → T-106 → T-201 → T-201A → T-202 → T-205 → T-104B → T-203 → T-204 → T-300 → T-301 → T-302 → T-401 → T-403. Para cerrar los endpoints y la retención después de T-403: T-303 → T-304 → T-306 → T-305. T-102 y T-104A dependen de la base local de T-105; T-205 depende de T-202 para consultas territoriales; T-104B depende de la decisión humana de T-205.

**Ruta de migración del núcleo determinista:** T-610 → T-611 → T-612 → T-613 → T-614 → T-615 → T-616 → T-617. T-611/T-612 son un incremento exclusivamente de pruebas; T-614 no empieza antes de disponer de diagnósticos T-613; T-615 está bloqueada para código hasta enmienda contractual; T-616 no puede mutar `golden-v1`; T-617 bloquea cualquier retirada del legado.

## Resumen de acciones humanas (🧑) para planear tu agenda

| Tarea | Qué haces tú | Tiempo estimado |
|---|---|---|
| T-001/002/003 | Instalar Docker Desktop y crear cuentas/claves (Anthropic; Supabase y Railway solo para despliegue) | 1–2 h |
| T-205 | Decidir modelo de embeddings y dimensión con el benchmark (research.md §1) | 2–3 h |
| T-601 | Construir y verificar el golden set (criterio experto) | 2–3 días |
| T-603 | Decidir configuraciones comparativas y analizar | 1 día |
| T-701/702/703 | Despliegues, DNS y monitoreo | 2–4 h |
| T-705 | Cierre documental y registro MinTIC | 1 día |

# Plan de Trabajo Ejecutable — Cuestión de Datos v2.0

**Versión:** 1.0.0 · **Fecha:** 2026-07-06
**Fuente:** [`plan.md`](./plan.md) §8 · Los IDs de requisitos (`RF-###`, `RNF-###`) vienen de [`spec.md`](./spec.md)

> **Cómo leer este archivo (para Camilo):** las tareas están en orden de ejecución. Cada una dice *qué* se hace, *por qué*, *qué necesitas hacer tú manualmente* (cuentas, claves, decisiones) y *cómo verificar que quedó bien* antes de pasar a la siguiente. Las marcadas 🧑 requieren acción humana tuya; las demás las puede ejecutar un agente de desarrollo. Marca `[x]` al completar. No saltes fases: cada una asume la anterior verificada.

---

## FASE 0 — Preparación de cuentas y herramientas externas

*Objetivo: tener todas las credenciales e infraestructura gratuita lista ANTES de escribir código, para que ninguna tarea posterior se bloquee esperando un registro.*

- [ ] **T-001 🧑 Preparar las bases de datos (local y despliegue).**
  *El desarrollo diario usa PostgreSQL LOCAL en Docker (Constitución Art. II.2); el servicio gestionado es solo para DESPLIEGUE y puede crearse más tarde (se necesita a partir de T-206/T-701).*
  1. **Local (requisito para desarrollar):** instala [Docker Desktop](https://www.docker.com/products/docker-desktop/). La base local se levantará con el `compose.yaml` que se crea en T-105 — aquí solo asegura que `docker compose version` funciona.
  2. **Despliegue (puede diferirse):** regístrate en [supabase.com](https://supabase.com) (o [neon.tech](https://neon.tech), equivalente) con tu cuenta de GitHub; crea el proyecto `cuestion-de-datos`; en el editor SQL ejecuta `CREATE EXTENSION IF NOT EXISTS vector; CREATE EXTENSION IF NOT EXISTS pg_trgm;`; guarda la cadena de conexión (`postgresql://usuario:clave@host-remoto:5432/base`) en tu gestor de contraseñas.
  - ✅ *Verificación:* `docker compose version` responde; y, cuando crees la gestionada, `SELECT extname FROM pg_extension;` lista `vector` y `pg_trgm`.

- [ ] **T-002 🧑 Verificar/crear las API keys de los modelos.**
  1. **Gemini (defecto):** ya tienes `GOOGLE_API_KEY` en `.env.local` de v1.0. Confirma en [aistudio.google.com](https://aistudio.google.com) que sigue activa y revisa los límites del tier actual (necesitamos ~10 llamadas por investigación).
  2. **Anthropic (comparativa OE3):** crea una key en [console.anthropic.com](https://console.anthropic.com) → `ANTHROPIC_API_KEY`. Carga el crédito mínimo (USD 5 basta para las corridas de evaluación). *Solo se usa en la Fase 6.*
  3. **Socrata:** confirma tu `SOCRATA_APP_TOKEN` actual en [datos.gov.co](https://www.datos.gov.co/profile/edit/developer_settings) (aumenta los límites de tarifa de la API).
  - ✅ *Verificación:* cada key responde a una llamada mínima de prueba (`curl` de ejemplo en quickstart.md §7).

- [ ] **T-003 🧑 Crear el servicio de hosting del backend.**
  1. Regístrate en [railway.app](https://railway.app) (o render.com) con GitHub.
  2. Todavía NO despliegues nada; solo confirma que la cuenta puede crear servicios web con Python. El despliegue real es T-701.
  - ✅ *Verificación:* panel accesible, plan y límites anotados.

- [ ] **T-004 Preparar el repositorio.**
  1. Crear rama `v2` en el repo actual (`cuestion-de-datos-MinTIC-2026`, renombrado desde `cuestion-de-datos-MinTIC-2025`). Todo el trabajo v2.0 ocurre en ramas hijas de `v2` con PRs.
  2. Añadir `LICENSE` (MIT — compromiso de software libre de la propuesta) y `CHANGELOG.md` con la entrada `2.0.0-dev`.
  3. Actualizar `.gitignore`: añadir `backend/.env`, `__pycache__/`, `.venv/`, `*.pyc`.
  - ✅ *Verificación:* rama `v2` existe; `git log` limpio; ningún secreto en el historial.

## FASE 1 — Reorganización del repositorio (sin cambios funcionales)

*Objetivo: estructura de plan.md §3 con la v1.0 funcionando igual que antes. Hacerlo primero evita mezclar refactor con features nuevos.*

- [x] **T-101 Mover el frontend a `frontend/`.** Mover `components/`, `pages/`, `styles/`, `public/`, `data/`, `utils/`, configs de Next/Tailwind/PostCSS y `package.json` a `frontend/`. Actualizar rutas relativas si alguna se rompe.
  - ✅ `cd frontend && npm install && npm run dev` sirve la app idéntica a v1.0 en `localhost:3000`.
- [x] **T-105 Crear `compose.yaml` en la raíz del repositorio.** Define, como mínimo: servicio PostgreSQL con extensión pgvector con **versión fijada por tag completo o digest** (nunca `latest` ni alias flotante como único pin), volumen persistente, healthcheck y variables configurables vía `.env` (usuario, clave, base, puerto). Documentar en el README del repo el comando `docker compose up -d db`. Este archivo es la vía oficial de desarrollo local (Constitución Art. II.2); Supabase/Neon quedan solo para despliegue.
  - ✅ `docker compose up -d db` deja Postgres sano (healthcheck OK); las extensiones `vector` y `pg_trgm` quedan creadas por el script `init` del contenedor local; `psql` o `docker compose exec db psql ... -c "SELECT extname FROM pg_extension;"` lista ambas; los datos sobreviven a `docker compose restart`.
- [x] **T-102 Crear el esqueleto de `backend/`.** Estructura de carpetas de plan.md §3, `pyproject.toml` (Python 3.12; dependencias de runtime/dev: fastapi, uvicorn, langgraph, langgraph-checkpoint-postgres, langchain-core, langchain-google-genai, langchain-anthropic, sqlalchemy async, alembic, psycopg[binary,pool], pgvector, httpx, pydantic-settings, pytest, pytest-asyncio, respx, ruff, pip-audit). Registrar el marcador pytest `integration` en `[tool.pytest.ini_options]` sin `addopts` global que excluya integración. Preparar, si se desea para T-205, el extra opcional `[project.optional-dependencies].benchmark-embeddings` con marcadores de versión para `sentence-transformers` y `torch` (`<VERSION_COMPATIBLE_PY312>`), pero NO instalarlas ni tratarlas como runtime hasta la decisión de T-205. Crear `app/main.py` con `GET /v2/health` (200 `ok` y 503 `degraded` como `HealthResponse`, excepción explícita al sobre estándar), settings async, transformación validada de `DATABASE_URL` según plan.md §12, inicialización idempotente del checkpointer (`AsyncPostgresSaver.setup()`) contra la base local creada en T-105, `.env.example` con todas las variables de plan.md §12/quickstart.md §2, sin valores reales ni secretos, y un mínimo de pruebas deterministas sin red (`backend/tests/test_settings.py` y/o `backend/tests/test_health.py`) para que pytest no falle por ausencia de tests.
  - ✅ Con la base de T-105 arriba, `uvicorn app.main:app` responde en `/v2/health`; antes de T-203/T-204 el check `catalog_index` puede estar `degraded/not_initialized` y la respuesta global ser `503`; el checkpointer inicializa sus tablas; la app usa SQLAlchemy async + psycopg v3; `pytest -m "not integration"` ejecuta al menos una prueba determinista real y termina en verde; `pytest -m integration` es comando válido aunque no tenga casos hasta las fases de integración.
- [x] **T-103 Configurar CI básico.** `.github/workflows/ci.yml`: jobs `backend` (ruff + `pytest -m "not integration"` + pip-audit) y `frontend` (build de Next). El job backend consume las pruebas mínimas creadas en T-102 y añade un servicio PostgreSQL+pgvector con healthcheck cuando ejecute pruebas de contrato o integración que toquen DB; las unitarias puras pueden correr sin DB. La suite `pytest -m integration` queda separada para corridas etiquetadas/diarias según pruebas.md. Debe correr en cada PR a `v2`.
  - ✅ El primer PR muestra los dos checks en verde; el job backend exporta un `DATABASE_URL` de CI compatible con psycopg v3 y confirma extensiones `vector`/`pg_trgm` antes de pruebas con DB. Verificado en [PR #1](https://github.com/Grajales-Camilo/cuestion-de-datos-MinTIC-2026/pull/1): `Backend` y `Frontend` en verde. `pip-audit` corre en modo informativo (`continue-on-error: true`) por incompatibilidad actual de `langgraph`/`langgraph-checkpoint`/`langgraph-sdk` 1.x con `langchain-core < 1` que aún requieren `langchain-google-genai`/`langchain-anthropic`; volver bloqueante antes de T-301 o T-701.
- [x] **T-104A Migraciones iniciales (SIN la tabla vectorial definitiva).** Alembic configurado; migración 001 crea todas las tablas de `data-model.md` EXCEPTO `catalog_embeddings`, cuya dimensión depende del benchmark de embeddings (T-205 → T-104B). Modelos SQLAlchemy en `app/db/models.py` espejando el documento exactamente (nombres, constraints y FKs), incluidos `official_publishers`, `official_publisher_aliases` con índice parcial único `alias_normalized WHERE ambiguous=false`, `worker_instances`, campos de token por `retention_class`, elegibilidad/PII, metadatos de revisión PII, cortes por evidencia, eventos (`agent_run_events`), `quantitative_claims` con `source_hash` de contenido reproducible, `technical_metrics.source_run_hash UNIQUE`, semillas/configuración eval, constraints de dominio y `eval_case_results.agent_run_id ON DELETE SET NULL`.
  - ✅ Con la base local ya levantada en T-105, `alembic upgrade head` crea las tablas; `psql`/`docker compose exec db psql ... -c "\\dt"` las lista; NO existe aún `catalog_embeddings`; duplicar un alias no ambiguo falla y dos ambiguos con el mismo texto son válidos; `eval_case_results.agent_run_id` permite `NULL`; los CHECK de status/retention/PII/elegibilidad/confianza rechazan valores inválidos; `technical_metrics.source_run_hash` impide duplicados.
- [x] **T-106 Registro canónico inicial de publicadores oficiales.** Crear fixture versionado, script de carga y endpoint admin `POST /v2/admin/publishers/reload` para `official_publishers` y `official_publisher_aliases` según `data-model.md` y `contracts/validacion-calidad.md`: `id`, nombre canónico, alias normalizados, tipo de entidad, vigencia, sucesor, estado, fuente de verificación y fecha de actualización. Incluir el conjunto mínimo para arrancar la ingesta: DANE, ministerios, gobernaciones/alcaldías piloto, universidades públicas, empresas industriales y comerciales del Estado y establecimientos públicos ya identificados. No exige cobertura de catálogo completo porque esa medición depende de T-201.
  - ✅ Cargar el fixture en la base local resuelve nombre canónico, alias único, mayúsculas/tildes y entidad abreviada; alias no único queda marcado `ambiguous=true` y produce `publisher_verification_status="unknown"`; publicador privado queda `private_or_non_official`. Verificado: `backend/data/official_publishers.json` (16 publicadores con las 7 categorías `entity_type` no genéricas, cada uno y cada alias con `verification_source` real verificado por búsqueda web — DANE, 9 ministerios, Gobernación de Antioquia, Alcaldías de Bogotá/Medellín, Universidad Nacional, SENA, Fondo Nacional del Ahorro — más `known_private_publishers` con Universidad de los Andes); `backend/app/db/publishers.py` (normalización, carga idempotente, `resolve_publisher`); `backend/scripts/load_official_publishers.py`; endpoint `POST /v2/admin/publishers/reload` con `X-Admin-Token` (401 sin token, 401 con token inválido, 202 con resumen) en `backend/app/main.py`. `pytest -m integration -k publishers` (9 casos) contra Postgres local confirma: alta/actualización idempotente, resolución por nombre canónico, insensibilidad a mayúsculas/tildes, resolución por alias abreviado (`MinTIC`), alias ambiguo (`Secretaria de Educacion`, compartido entre las dos alcaldías) → `unknown`/`publisher_alias_ambiguous`, publicador privado conocido (Universidad de los Andes) → `private_or_non_official`/`publisher_private`, publicador desconocido → `unknown`/`publisher_unknown`, y que un alias no ambiguo duplicado viola el índice parcial `uq_official_alias_unambiguous`. La resolución de `official_publisher_id`/`status` es la misma que T-201 escribirá en `catalog_datasets.publisher_verification_status`; T-106 no tiene datasets propios que actualizar todavía. `pytest -m "not integration"` (19 casos), `ruff check .` y prueba manual end-to-end con `uvicorn` en verde.

## FASE 2 — Índice semántico del catálogo (RF-301…304, RNF-010)

*Objetivo: pasar de 5 datasets hardcodeados a ~8.000 buscables semánticamente. Es el cimiento del agente: sin esto no hay v2.0.*

- [x] **T-201 Cliente de la Discovery API (ESC-07).** `scripts/ingest_catalog.py`: pagina `https://api.us.socrata.com/api/catalog/v1?domains=www.datos.gov.co&only=dataset` (respetando límites de tarifa), filtra tabulares con API activa y normaliza según plan.md §5. Upsert en `catalog_datasets` + `catalog_columns`, enlazando publicador contra `official_publishers`/aliases, distinguiendo `metadata_synced_at` de `data_updated_at`, calculando `pii_risk_level`, `eligibility_status` y `eligibility_reasons`. Registra `ingest_runs`.
  - ✅ Correr el script llena ≥ 7.000 datasets; re-correrlo no duplica nada (RF-304). Verificado: corrida real completa contra la Discovery API real produjo **8.415 datasets** (100% de `resultSetSize`) y **108.563 columnas**, 0 fallidos; segunda corrida completa confirmó idempotencia exacta (`nuevos: 0, actualizados: 8415`, mismo conteo total, sin duplicados, `api_active=true` en las 8.415 filas). Nombres de campo reales de la Discovery API difieren de los citados en plan.md §5.3 (`data_updated_at` real en vez de `rowsUpdatedAt`; publicador en `owner.display_name`, no en `classification.domain_metadata`) — confirmado con peticiones reales, documentado en `backend/app/catalog/normalize.py`. Se agregaron `backend/app/quality/pii_patterns.yaml` (fixture de patrones PII, sin esquema previo en specs) y `backend/app/quality/eligibility.py` (mapeo de elegibilidad corregido contra `contracts/validacion-calidad.md` §3.1: publicador no verificado → `diagnostic_only`, no `blocked`). `resolve_publisher()` (T-106) se extendió con `reference_date` opcional para vigencia institucional/sucesor sin romper el comportamiento previo. 123 pruebas (103 `pytest -m "not integration"` + 20 `pytest -m integration` contra Postgres real) y `ruff check .` en verde. Con el fixture inicial de 16 publicadores, 163/8415 datasets quedan `verified`/`eligible` o `diagnostic_only`; el resto queda `blocked` por `pii_unknown` (default seguro del Art. VI.1) — comportamiento esperado, la ampliación del fixture PII/publicadores es trabajo de seguimiento (T-201A cubre publicadores; no existe aún una tarea equivalente para PII).
- [x] **T-201A Auditoría de cobertura de publicadores.** Después de la primera ingesta real, generar `backend/data/official_publishers_coverage.json` con conteos por `publisher_verification_status`, aliases no únicos detectados, candidatos a alias nuevos y cobertura de datasets tabulares activos. Actualizar el fixture solo con procedencia verificable.
  - ✅ Nuevo módulo puro `backend/app/catalog/coverage.py` (conteos por `publisher_verification_status`, aliases `ambiguous=true` agrupados, candidatos `unknown` priorizados por volumen de datasets) + CLI `backend/scripts/audit_publisher_coverage.py`, probados con 5 casos unitarios (`backend/tests/test_publisher_coverage.py`) y 3 de integración contra Postgres real (`backend/tests/integration/test_publisher_coverage_queries.py`). Auditoría inicial contra los 8.415/8.417 datasets reales ya ingeridos por T-201: 163 `verified` (1,94%) — brecha explícita frente al 90%. Se amplió `backend/data/official_publishers.json` de 16 a 61 publicadores (45 nuevos: 23 entidades nacionales — UPRA, Superintendencias de Servicios Públicos/Financiera, Invima, Ideam, DNP, Icfes, Unidad para las Víctimas, Función Pública, Cancillería, ARN, ANDJE, Medicina Legal, ESAP, MinMinas, Prosperidad Social, Corte Constitucional, Contraloría y Procuraduría Generales, Presidencia, ANI, ICA, MinVivienda —, 10 gobernaciones — Valle del Cauca, Boyacá, Casanare, Santander, Sucre, Magdalena, Atlántico, Caldas, Meta, Amazonas —, 10 alcaldías de mayor volumen — Palmira, Pereira, Bucaramanga, Neiva, Barranquilla, Chía, Fusagasugá, Yopal, Guadalajara de Buga, San Pedro de los Milagros — y Universidad del Quindío + Gobernación de Risaralda), cada uno con `verification_source` real verificado por búsqueda web (sitio institucional `.gov.co`/`.edu.co`), misma disciplina que T-106: sin listas inventadas y sin heurística de coincidencia difusa (alias exactos sobre el texto crudo observado en `catalog_datasets.publisher` cuando difiere del nombre canónico). Recarga del fixture + re-ejecución completa de `scripts/ingest_catalog.py` (idempotente, confirmado por `datasets_new≈0` en la segunda corrida) elevó la cobertura real a **2.376/8.416 = 28,23% `verified`** (+2.213 datasets `unknown`→`verified` netos frente a la línea base de 163, ~14.5× la cobertura inicial). **Brecha explícita documentada** (`coverage_gap: true`, campo `gap_explanation` en el reporte): no se alcanza el 90% porque el catálogo tiene una cola larga de 1.500+ textos de `publisher` distintos sin resolver (la mayoría con ≤10 datasets cada uno: notarías, ESE hospitalarias, cámaras de comercio —privadas, no estatales—, personas naturales como responsable de datos, y cientos de alcaldías municipales de bajo volumen); cerrar esa brecha requiere onboarding sistemático municipio por municipio, fuera del alcance verificable de un único PR y candidato natural a apoyarse en el maestro DIVIPOLA de T-202. Ningún alias no único (`ambiguous=true`, 1 caso: "Secretaria de Educacion" compartido por Bogotá/Medellín) asigna publicador automáticamente — verificado en `test_resolve_ambiguous_alias_is_unknown` y en el propio reporte de cobertura. `ruff check .`, `pytest -m "not integration"` (108 casos) y `pytest -m integration` (23 casos + 1 skip) en verde contra Postgres local real. Seguimiento operativo (agilizar rondas futuras de ampliación, no forma parte del criterio de aceptación de T-201A): `backend/app/db/publishers_admin.py` + CLI `backend/scripts/add_publishers.py` fusionan entidades nuevas desde un CSV (`id,canonical_name,entity_type,verification_source,aliases,ambiguous_aliases,active`) sobre el fixture existente sin editar el JSON a mano, con validación de `entity_type`, detección de colisión de alias/`canonical_name` contra otras entidades y modo `--dry-run`; sigue exigiendo `verification_source` real verificado por un humano por cada fila (no automatiza la verificación, solo la fusión mecánica). 11 pruebas unitarias en `backend/tests/test_add_publishers.py`. Documentado en `quickstart.md` §3.

Segunda ronda (petición explícita de Camilo de una lista curada de 47 entidades responsables + las 32 gobernaciones departamentales): usando `add_publishers.py` sobre un CSV verificado, se agregaron 45 publicadores más (total **106**, hasta las 32 gobernaciones completas — antes 12) y 26 alias nuevos (incluye ampliar `funcion-publica`, `prosperidad-social`, `minhacienda`, `minvivienda` y `upra` ya existentes), cada uno con `verification_source` verificado por búsqueda web antes de escribirse. Dos casos se resolvieron explícitamente SIN agregarlos como oficiales, siguiendo la misma disciplina de no inventar ni clasificar mal: **Confederación Colombiana de Cámaras de Comercio (Confecámaras)** — verificado como "entidad privada, de utilidad común, gremial y sin ánimo de lucro" (fuente: confecamaras.org.co) — se registró en `known_private_publishers` (no en `official_publishers`), no como estatal; **"Paratec"** no se agregó porque no es una entidad legal propia sino una plataforma técnica (`paratec.xm.com.co`) operada por XM S.A. E.S.P., empresa de **naturaleza mixta** (no estatal pura, verificado), por lo que no hay una clasificación de `entity_type` verificable sin arriesgar una asignación incorrecta — pendiente de decisión humana si se detectan datasets reales bajo ese nombre. Re-ingesta completa + auditoría: cobertura sube a **2.433/8.416 = 28,91% verified**. `ruff check .`, `pytest -m "not integration"` (119 casos) y `pytest -m integration` (23 + 1 skip) en verde.

Tercera ronda (evaluación del dataset MinTIC `fn2v-r4gu` — "Hoja de Ruta Nacional de Datos Abiertos Estratégicos del Estado Colombiano", el mismo del que sale la lista de 47 entidades — y su uso como técnica de verificación): la columna `enlace_portal_dato_abierto` de ese dataset apunta a `dataset_id` reales de `catalog_datasets`; cruzar esos 86 enlaces contra el texto real de `publisher` en la base (en vez de adivinar/buscar) reveló 24 textos crudos sin resolver para entidades ya verificadas, cada uno confirmado además por la columna `entidades_responsables` del propio dataset de MinTIC como evidencia cruzada. Se aplicaron con `add_publishers.py`: 12 alias nuevos sobre entidades existentes (`apc-colombia`, `minagricultura`, `colombia-compra-eficiente`, `supertransporte`, `mindefensa`, `minambiente`, `dane`, `registraduria`, `servicios-postales-nacionales`, `mintransporte`, `auditoria-general`, `instituto-nacional-salud`) y 1 entidad nueva verificada (**INVIAS** — Instituto Nacional de Vías, establecimiento público adscrito a MinTransporte). Dos casos adicionales se identificaron como NO estatales y se registraron en `known_private_publishers` en vez de `official_publishers`: **RUNT 2.0** (Registro Único Nacional de Tránsito) opera bajo una concesión privada (Concesión RUNT 2.0 S.A.S., contrato 604 de 2022 con MinTransporte, verificado) — mismo patrón que "Paratec"/XM de la ronda anterior; **"serviciorues"** (RUES) confirmado como servicio de Confecámaras (privado). Se descartaron deliberadamente por riesgo de mala atribución: un nombre de persona natural ligado a un dataset de la DIAN (los alias institucionales no deben identificar personas, aunque el roadmap confirmaba su procedencia con DIAN) y dos textos genéricos de oficina ("OficinaTI", "Gestión de Tecnologías de la Información") que aparecen con múltiples datasets en el catálogo pero solo una coincidencia confirmada cada uno contra el roadmap — agregarlos como alias no ambiguos habría asignado automáticamente datasets de otras entidades sin evidencia. Re-ingesta completa + auditoría: cobertura sube a **2.611/8.416 = 31,02% verified** (+178 datasets). `ruff check .`, `pytest -m "not integration"` (119 casos) y `pytest -m integration` (23 + 1 skip) en verde. Evaluación de la API: el endpoint público estable es `https://www.datos.gov.co/resource/fn2v-r4gu.json` (SODA v2.1, mismo patrón que `discovery_client.py`); el endpoint `/api/v3/views/{id}/query.json` dado inicialmente es la API interna no documentada de la UI de Socrata y no debe usarse para integraciones. El dataset es útil como fuente de procedencia y como técnica de verificación cruzada de alias (no de descubrimiento de entidades nuevas más allá de las 48 que ya cubre); no cierra la brecha del 90% porque solo cubre el conjunto "prioritario" de MinTIC, no la cola larga de ~1.500 textos de `publisher` distintos del catálogo completo.
- [x] **T-202 Carga del maestro DIVIPOLA.** Script que descarga el dataset oficial DIVIPOLA de datos.gov.co (dataset del DANE, id `gdxc-w37w` o su vigente — confirmar en el portal) y llena `divipola_entries` con `name_normalized` y `alt_names`.
  - ✅ Id `gdxc-w37w` confirmado vigente en el portal (2026-07-08): "DIVIPOLA- Códigos municipios", publicado bajo licencia DANE (CC BY-SA 4.0), recurso SODA `https://www.datos.gov.co/resource/gdxc-w37w.json`, 1.122 filas, columnas `cod_dpto`/`dpto`/`cod_mpio`/`nom_mpio`/`tipo_municipio`/`longitud`/`latitud`. El recurso solo lista municipios (incluye `tipo_municipio` "Isla" y "Área no municipalizada" bajo el mismo nivel de 5 dígitos); las filas `level="department"` se derivan agrupando los pares `(cod_dpto, dpto)` únicos que ya traen esas mismas filas, porque el DANE no publica en este id un recurso separado de departamentos — divergencia menor de plan.md/data-model.md documentada en el docstring de `backend/app/db/divipola.py`, mismo espíritu que las divergencias de Discovery API documentadas en T-201. Nuevos módulos: `backend/app/catalog/divipola_client.py` (cliente SODA paginado con `$limit`/`$offset` ordenado por `:id`, timeout 10s + 1 reintento en 5xx/timeout, mismo patrón que `discovery_client.py`), `backend/app/db/divipola.py` (normalización `name_normalized`, `KNOWN_ALT_NAMES` curado para Bogotá D.C./Santafé de Bogotá, derivación de departamentos, upsert idempotente por `code` vía `pg_insert...on_conflict_do_update`, mismo patrón que `app/db/publishers.py`), CLI delgado `backend/scripts/load_divipola.py`. Corrida real contra Postgres local: **1.122 municipios + 33 departamentos = 1.155 filas** (`creados: 1150, actualizados: 5` en la primera corrida sobre una base con residuos previos de prueba); segunda corrida confirma idempotencia exacta (`creados: 0, actualizados: 1155`, mismo conteo total, sin duplicados). Criterios de aceptación verificados contra la base real: `SELECT count(*) FROM divipola_entries WHERE level='municipality';` → **1122** (≥ 1.100); `SELECT code, name FROM divipola_entries WHERE level='municipality' AND name_normalized % 'CARMEN VIBORAL' ORDER BY similarity(name_normalized, 'CARMEN VIBORAL') DESC LIMIT 1;` → **05148, EL CARMEN DE VIBORAL** (mejor coincidencia trigram, por encima de "El Carmen" y "Carmen de Carupá"). 24 pruebas unitarias sin red (`tests/test_divipola_normalize.py`, `tests/test_divipola_client.py`: normalización de tildes/mayúsculas, filas incompletas rechazadas, alias conocidos, deduplicación de departamentos, paginación/reintento/token del cliente HTTP) + 4 de integración contra Postgres real (`tests/integration/test_divipola.py`: upsert crea departamentos+municipios, idempotencia, búsqueda trigram; `tests/integration/test_divipola_live.py`: una página real del dataset produce filas válidas, deliberadamente pequeña como el equivalente de T-201 para Discovery API). `ruff check .` y `pytest -m "not integration"` (137 casos totales, 18 nuevos de T-202) en verde; `pytest -m integration -k divipola` (4 casos) en verde contra Postgres local real.
- [x] **T-205 🧑 Benchmark de embeddings y decisión (ANTES de crear la tabla vectorial).** Notebook `notebooks/01_benchmark_embeddings.ipynb`: instalar entorno con `pip install -e ".[dev,benchmark-embeddings]"`, fijando antes en `backend/pyproject.toml` versiones compatibles con Python 3.12 para `sentence-transformers`, PyTorch y dependencias relevantes del candidato local. Comparar `intfloat/multilingual-e5-large` (local), `gemini-embedding-2` (gestionado) y, si se justifica en `research.md`, otro modelo multilingüe actual, sobre una muestra de los metadatos ya ingeridos (T-201) y el maestro DIVIPOLA cargado (T-202), con 30+ consultas de prueba. Evaluar TODOS los criterios de `research.md` §1: recuperación en español, calidad en consultas territoriales colombianas, dimensión de vectores, latencia, costo, ejecución local, dependencia de proveedor, reproducibilidad, tamaño del índice y compatibilidad pgvector. Registrar versión de Python, versiones de librerías, identificador y versión exacta del modelo, CPU/RAM/dispositivo, tamaño de lote, parámetros de codificación, normalización de vectores, semillas, tiempo de carga separado de latencia por consulta, costo/configuración del candidato gestionado y comandos/notebook de reproducción. Si se mantiene `vector(<DIM>)`, aceptar solo `DIM <= 2000`; si se elige una dimensión mayor, documentar cambio a `halfvec` antes de T-104B. **Tú decides** el modelo y con él la dimensión; registrar la decisión y su justificación en `research.md` §1 y propagar el resultado a `data-model.md`, dependencias definitivas de `pyproject.toml`, `.env.example`, `plan.md` y `quickstart.md`.
  - ✅ **Decisión (2026-07-09, firmada por Juan Camilo Grajales B. en `notebooks/01_benchmark_embeddings.ipynb` §7):** `gemini-embedding-2` (Google, gestionado), 768 dimensiones, `vector(768)` + `vector_cosine_ops` (no requiere `halfvec`). Corrida real contra el fixture congelado (`notebooks/fixtures/`: 200 datasets reales de T-201 vía `scripts/export_benchmark_fixture.py`, 1.155 filas reales de T-202) con 34 consultas anotadas a mano (11 territoriales, verificadas contra `divipola_master.json` real — 3 códigos DIVIPOLA mal citados en un borrador previo se corrigieron antes de la corrida final: Medellín 05001, Risaralda 66, Tunja 15001). Resultado: recall@10 empatado en ambos candidatos (94,12% general, 90,91% territorial — no discrimina); `gemini-embedding-2` gana en latencia real (p95 41,8ms vs 120,2ms del local) y sobre todo en costo de infraestructura: el pico de RAM medido del local (`PeakRSSTracker`, muestreo real, no `ram_total_gb` de la máquina) fue **2.430,7 MB**, incompatible con el tier gratuito de Railway (0,5 GB) y, como RF-302 exige embeber la consulta "en caliente" en el mismo proceso del backend, esa RAM quedaría reservada permanentemente dentro del tier Hobby (~USD 5/mes ya presupuestado en `plan.md` §2) sin importar el volumen de uso. Costo real de la API gestionada: $0,0092 por el corpus+consultas de prueba (45.818 tokens reales) y $0,39 para una reindexación completa del catálogo activo (~8.416 datasets, evento raro no recurrente) — marginal al volumen del piloto (`spec.md` SUP-02). Se acepta perder los criterios 7 y 8 (dependencia de proveedor y reproducibilidad, donde el local es superior) por precedente arquitectónico ya existente (capa multi-proveedor del LLM, RF-206) y porque cambiar de modelo más adelante tiene costo conocido y acotado (regenerar el índice, ~$0,39 al tamaño actual). Propagación completa en el mismo PR: `research.md` §1 (`DECIDIDA`, tabla de evidencia + justificación completa), `data-model.md` (`<DIM>` → `768` en `catalog_embeddings`, dos ocurrencias), `plan.md` (fila de stack de embeddings + condición de T-104B), `backend/.env.example` y `quickstart.md` (`EMBEDDING_MODEL=gemini-embedding-2`), `backend/pyproject.toml` (comentario aclarando que no se agrega dependencia de runtime nueva — `langchain-google-genai`/`httpx` ya estaban). T-104B queda desbloqueada (pendiente de implementar la migración Alembic en sí, fuera del alcance de T-205).
- [x] **T-104B Migración definitiva de `catalog_embeddings`.** Con el modelo y la dimensión decididos en T-205: migración Alembic que crea `catalog_embeddings` con `vector(<dimensión elegida>)` + `vector_cosine_ops` si `DIM <= 2000`, o `halfvec(<dimensión elegida>)` + operador compatible si `DIM > 2000`. Actualizar `data-model.md` reemplazando `<DIM>` por el valor real en el mismo PR.
  - ✅ Implementado con `gemini-embedding-2`, 768 dimensiones: `backend/app/db/models.py` agrega `CatalogEmbedding` con `dataset_id text` PK/FK a `catalog_datasets.id ON DELETE CASCADE`, `embedding Vector(768) NOT NULL`, `model text NOT NULL` e índice SQLAlchemy `Index(..., postgresql_using="hnsw", postgresql_ops={"embedding": "vector_cosine_ops"})`; se eligió `ON DELETE CASCADE` porque la relación es 1:1 dependiente y sigue el criterio ya usado por `catalog_columns` para metadatos derivados del dataset. Migración nueva `backend/migrations/versions/20260709_0002_catalog_embeddings.py`, `down_revision="20260708_0001"`, crea solo tabla e índice; NO ejecuta `CREATE EXTENSION`, coherente con plan.md §2/T-105. Registro de tipo pgvector para SQLAlchemy async + psycopg v3 centralizado en `backend/app/db/engine.py` con `register_vector_async` en evento `engine.sync_engine.connect`; se aplicó a `main.py` y scripts existentes que crean engines de aplicación. El checkpointer (`AsyncPostgresSaver`) no requiere registro pgvector porque no lee/escribe columnas `vector`; las escrituras futuras de T-203 deben usar el helper `create_app_async_engine`.
  - ✅ Verificación local real contra Docker Postgres (`POSTGRES_PORT=5433`, `DATABASE_URL=postgresql://usuario:clave@localhost:5433/cuestion_de_datos`): antes de migrar, `alembic current` → `20260708_0001`, `SELECT to_regclass('public.catalog_embeddings')` → `NULL`, y `docker compose exec db psql ... -c "\dx"` ya listaba `vector 0.8.0` y `pg_trgm 1.6` creadas por el init del contenedor. `alembic upgrade head` ejecutó `Running upgrade 20260708_0001 -> 20260709_0002, Add catalog embeddings table.`. `docker compose exec db psql -U usuario -d cuestion_de_datos -c "\d catalog_embeddings"` mostró `embedding vector(768) not null`, PK btree en `dataset_id`, índice `ix_catalog_embeddings_embedding_hnsw hnsw (embedding vector_cosine_ops)` y FK `ON DELETE CASCADE`; consulta a `pg_indexes` confirmó `CREATE INDEX ix_catalog_embeddings_embedding_hnsw ON public.catalog_embeddings USING hnsw (embedding vector_cosine_ops)`. Prueba transaccional ORM insertó y leyó un `CatalogEmbedding` de 768 floats con `model=gemini-embedding-2` usando `SelectorEventLoop` en Windows y luego hizo rollback (`insert_and_select_ok model=gemini-embedding-2`). `Select-String data-model.md '<DIM>'` no devolvió resultados. `ruff check .`, `pytest -m "not integration"` (137 passed, 28 deselected) y `pytest -m integration` (27 passed, 1 skipped) en verde.
- [x] **T-203 Generación de embeddings (ESC-07).** `scripts/build_embeddings.py`: construye `embedding_text` (plan.md §5.4), genera embeddings por lotes con el modelo seleccionado y hace upsert en `catalog_embeddings`.
  - ✅ Implementado en `backend/app/catalog/embeddings.py` + CLI `backend/scripts/build_embeddings.py`: reutiliza `build_embedding_text()` para auditar `catalog_datasets.embedding_text`, exige `EMBEDDING_MODEL=gemini-embedding-2`, fija `output_dimensionality=768`, valida dimensión de cada vector, usa `create_app_async_engine()` para SQLAlchemy async + psycopg v3 con pgvector registrado, hace upsert idempotente en `catalog_embeddings`, y bloquea mezcla de modelos salvo regeneración explícita con `--replace-existing-model`. Validación real contra Docker Postgres (`DATABASE_URL=postgresql://usuario:clave@localhost:5433/cuestion_de_datos`): después de `python scripts/ingest_catalog.py` (`8416` datasets nuevos, `0` fallidos), `python scripts/build_embeddings.py --batch-size 25 --only-missing` completó el índice; el intento previo con lote 100 persistió 800 y luego falló por `504 Deadline Exceeded`, por eso el reintento idempotente reportó `Embeddings upserted: 7616 | selected: 7616 | active_datasets: 8416 | model: gemini-embedding-2 | dimension: 768`. SQL directo confirmó `active_datasets=8416`, `embeddings=8416`, `active_missing_embeddings=0`, un único `model=gemini-embedding-2`, `min(vector_dims(embedding))=768`, `max(vector_dims(embedding))=768`, y `8416` datasets activos con `embedding_text` no vacío. Pruebas: `ruff check .`, `pytest -m "not integration"` (143 passed, 31 deselected), `pytest -m integration` (30 passed, 1 skipped) y `tests/integration/test_build_embeddings.py` valida contra Postgres real upsert, idempotencia, dimensión 768 y rechazo de modelos mezclados con cliente de embeddings falso.
- [x] **T-204 Endpoint de búsqueda.** `GET /v2/catalog/search` según `contracts/api-rest.md` §8: valida `q`, limita `k`, excluye datasets inactivos, marca `index_stale` y devuelve `latest_observed_cutoff_at` solo como pista, nunca como corte de evidencia.
  - ✅ Implementado en `backend/app/catalog/search.py` y expuesto en `backend/app/main.py` sin implementar todavía herramientas del agente T1-T5 ni grafo. Usa `catalog_embeddings` existente y genera solo el embedding de la consulta (`RETRIEVAL_QUERY`, `gemini-embedding-2`, 768 dimensiones); la similitud devuelta sigue siendo `1 - (embedding <=> query_embedding)` sobre pgvector/vector_cosine_ops y el orden combina esa similitud con un reordenamiento lexical acotado sobre un subconjunto candidato para recuperar columnas relevantes sin regenerar embeddings. Validaciones de contrato: `q` se trimmea y exige 3-500 caracteres; `k` exige 1-25 y `k=999` devuelve `422 VALIDATION_ERROR` sin recorte silencioso; `api_active=false` se excluye; `index_stale` se calcula con `CATALOG_STALE_AFTER_DAYS`; `latest_observed_cutoff_at` viaja solo como pista y no se expone `data_cutoff_at`. Verificación local real contra Docker Postgres en `localhost:5433` tras `alembic upgrade head`, ingesta real y embeddings existentes/restaurados: `catalog_datasets=8423`, `catalog_embeddings=8416`, `active_datasets=8416`; `GET /v2/catalog/search?q=deserción escolar&k=5` respondió `200` en `627,93 ms` y retornó datasets MEN en top-5 (`sras-4t5p`, `ji8i-4anb`); `k=999` respondió `422 VALIDATION_ERROR`; marcar temporalmente `sras-4t5p` como `api_active=false` lo excluyó del top-5 y luego se restauró a `true`. Pruebas: `ruff check .`, `pytest -m "not integration"` (147 passed, 33 deselected) y `pytest -m integration` (32 passed, 1 skipped, 147 deselected) en verde contra Postgres real.

Seguimiento (revisión de código post-merge, dos huecos detectados y cerrados en el mismo estilo de T-201A): (1) el reordenamiento lexical dependía de `SYNONYMS_ES`, un diccionario de 2 entradas sin documentar, ajustado aparentemente solo para que "deserción escolar" pasara — se documentó en `backend/app/catalog/search.py` su origen real (similitud coseno de los datasets MEN ~0,60-0,63 fuera del top-25 frente a ~0,68-0,70 de competidores, medido con el índice real), su alcance explícito (parche acotado, no tesauro general) y su límite conocido (no generaliza; ampliar solo con evidencia real, no por intuición); se agregaron 3 pruebas nuevas en `backend/tests/test_catalog_embeddings.py` que verifican que consultas sin sinónimo conocido (`"trafico portuario maritimo"`) no se rompen ni inventan términos. (2) RNF-010 ("medir al menos 100 consultas representativas... p95 ≤ 1 s, cobertura ≥ 90%", `pruebas.md` §2.3) nunca se había implementado — la única evidencia de latencia era una consulta manual (627,93 ms). Se agregó `backend/app/catalog/search_benchmark.py` (percentiles + generador determinista de consultas representativas a partir de categorías reales de `catalog_datasets` y departamentos reales de `divipola_entries`, sin anotación de relevancia — eso sigue siendo responsabilidad de `research.md` §1) con pruebas unitarias en `backend/tests/test_catalog_search_benchmark.py`, y `backend/tests/integration/test_catalog_search_rnf010.py` que corre 100 consultas reales contra `/v2/catalog/search` con el índice real. **Evidencia real (aislada, catálogo completo restaurado — `catalog_datasets=8423`, `catalog_embeddings=8416`, `active_datasets=8416`):** p50=454,5 ms, **p95=921,3 ms** (dentro del presupuesto de 1.000 ms), p99=1.150,6 ms; cobertura=100,00% (8.416/8.416 activos, ≥ 90% exigido); excluidos de la búsqueda por `api_active=false`=0,08% (7/8.423); activos pero no elegibles como evidencia (RF-401, gap ya documentado en T-201A, no filtrado por esta búsqueda)=100,00% — se reportan ambas exclusiones por separado porque mezclarlas en un solo porcentaje ("excluidos por api_active/elegibilidad") es engañoso: la búsqueda sí devuelve resultados para el 100% de los activos, la no-elegibilidad es un gate posterior de la capa de calidad, no de este endpoint. Nota de aislamiento: la prueba se salta con diagnóstico claro (no falla) si otra prueba de la misma sesión ya vació `catalog_datasets` antes de que corriera — problema de orden compartido de la suite, no de RNF-010; corrida aislada (`pytest -m integration -k rnf010`) para evidencia real. `ruff check .`, `pytest -m "not integration"` (158 passed) y `pytest -m integration` (32 passed, 2 skipped) en verde.
- [ ] **T-206 Cron de ingesta (ESC-07).** `.github/workflows/ingest-cron.yml` semanal (domingo 3:00 UTC) que ejecuta T-201+T-203 contra la base productiva usando secrets del repo. Implementa `POST /v2/admin/ingest` y `GET /v2/admin/ingest/runs` (RF-701).
  - ✅ Ejecución manual del workflow termina en verde y crea un `ingest_runs` nuevo.

## FASE 3 — Agente multi-paso (RF-201…209)

*Objetivo: el grafo LangGraph con sus herramientas reemplaza el bucle manual de v1.0, con durabilidad desde el primer día.*

- [x] **T-300 Prueba de concepto de durabilidad (ANTES de construir el grafo completo).** Prototipo mínimo (un endpoint + un grafo de juguete de 3 nodos) que demuestre de punta a punta la semántica única de plan.md §11: `worker_instance_id` + lease con TTL y renovación, cierre idempotente solo de instancias con lease vencida, eventos SSE numerados persistidos en `agent_run_events` con reserva atómica de `last_event_seq` vía `UPDATE agent_runs ... RETURNING last_event_seq` dentro de la misma transacción que inserta el evento, evento terminal `error` con `status`, `thread_id=run_id`, reconexión con `Last-Event-ID` sin pérdida ni duplicación, reinicio real ⇒ `interrupted/RUN_INTERRUPTED`, heartbeat vencido ⇒ `interrupted/HEARTBEAT_EXPIRED`, duración máxima ⇒ `failed/RUN_TIMEOUT`, DELETE de corrida activa y limpieza de checkpoints con `adelete_thread`. Cubrir dos emisores concurrentes, timeout compitiendo con evento normal, detector de worker perdido, escritura duplicada del evento terminal, rollback tras reservar secuencia y reintento idempotente. Si algo del diseño no funciona en la práctica, se corrige plan.md/contratos AQUÍ, no en la Fase 5.
  - ✅ Implementado en `backend/app/agent/` (nuevo): `durability.py` (`reserve_and_emit_event` — reserva atómica de `seq` + insert en la misma transacción; `write_terminal_event_once` — UPDATE condicional `WHERE terminal_event_written_at IS NULL` que reserva `seq` y fija el estado terminal en una sola sentencia, garantía de como máximo un evento terminal), `worker_lease.py` (lease de `worker_instances`, renovación, `close_stale_running_runs` para arranque idempotente), `heartbeat_sweep.py` (barrido periódico: `RUN_TIMEOUT`/`HEARTBEAT_EXPIRED`/`WORKER_LOST`, en ese orden de prioridad), `toy_graph.py` (grafo LangGraph de 3 nodos compilado con `interrupt_after=NODES`, cada nodo emite un evento `step`), `runner.py` (orquesta la corrida completa dentro de una única llamada `asyncio.to_thread(..., loop_factory=SelectorEventLoop)` en Windows — un solo loop para toda la corrida evita reabrir conexiones psycopg-async entre pasos; cancelación cooperativa vía `threading.Event`, NO `Task.cancel()`, porque cancelar la tarea que envuelve `to_thread` no interrumpe el hilo subyacente). Endpoints nuevos en `backend/app/main.py` bajo el prefijo deliberado `/v2/_poc/agent/...` (NO el contrato final de T-304): `POST /v2/_poc/agent/query`, `GET /v2/_poc/agent/stream/{run_id}` (SSE con reconexión `Last-Event-ID`, poll cada 300ms sobre `agent_run_events`, ping cada 15s), `DELETE /v2/_poc/agent/runs/{run_id}` (cancelación cooperativa con `DELETE_ACTIVE_GRACE_S` + `adelete_thread` + borrado de la fila). Lifespan extendido: registra `worker_instance_id`, corre el cierre idempotente de huérfanas, y lanza dos tareas de fondo (renovación de lease cada `WORKER_LEASE_TTL_S/3`, barrido cada 2s).
  - ✅ **Decisiones de alcance documentadas:** sin `Authorization: Bearer` (RF-801) en los endpoints del PoC — no está en la lista literal de "qué construir" de T-300, pertenece a T-304; el `run_access_token` igual se genera y su hash se persiste (columnas NOT NULL de `agent_runs`) pero se descarta sin exponerlo. **Aclaración de ambigüedad de plan.md §11 encontrada en la práctica** (documentada en `research.md` §2, 2026-07-09): el texto normativo permite `RUN_INTERRUPTED` **o** `WORKER_LOST` sin regla de desambiguación; se adoptó la convención `RUN_INTERRUPTED` = arranque idempotente, `WORKER_LOST` = barrido periódico en operación. Se verificó además que el piso `WORKER_LEASE_TTL_S >= 30` (constraint ya existente en `config.py`) implica que reiniciar el proceso inmediatamente después de matarlo NO produce interrupción instantánea si la lease aún no venció — comportamiento correcto (protege despliegues solapados), pero exige que la demo/prueba de kill real espere ese piso antes de reconectar.
  - ✅ **Demo real de matar el proceso** (criterio de aceptación, ejecutada de verdad, no simulada): `backend/tests/integration/test_t300_durability.py::test_real_process_kill_demo_interrupts_and_reconnects_without_loss` lanza el backend real como subproceso (`subprocess.Popen`), crea una corrida, consume 2 eventos reales por HTTP/SSE, mata el proceso con `Popen.kill()` (`TerminateProcess` en Windows — confirmado `returncode != 0`, terminación dura, no shutdown limpio), espera el vencimiento real de la lease (30s + margen), levanta un segundo subproceso apuntando a la misma base, reconecta con `Last-Event-ID` y verifica: eventos faltantes recibidos exactamente una vez cada uno, sin huecos, y estado terminal `error`/`RUN_INTERRUPTED`. Corrida real: **PASSED en 52,32s**. Log real observado: servidor #1 registra `202 Accepted` + `200 OK` (stream) antes del kill; servidor #2 registra el arranque, y la reconexión trae `seq=3, event=error` con el código `RUN_INTERRUPTED`.
  - ✅ **Pruebas de concurrencia real** (`pytest -m integration -k t300_durability`, `asyncio.gather` con conexiones/sesiones independientes contra Postgres real, nunca awaits secuenciales disfrazados) — **11 casos, todos en verde**: dos emisores concurrentes (12 llamadas simultáneas a `reserve_and_emit_event`) producen `seq` 1..12 contiguo sin huecos ni duplicados; timeout compitiendo con un evento normal produce exactamente un evento terminal; detector de worker perdido (`WORKER_LOST`) vía barrido periódico, repetirlo no duplica; `write_terminal_event_once` llamado dos veces es no-op la segunda vez; reservar `seq` y forzar rollback sin insertar el evento permite que el número se reutilice sin hueco observable; reconexión repetida con el mismo `Last-Event-ID` no pierde ni duplica eventos; una instancia nueva con lease vigente en la corrida existente NO la interrumpe (`closed == 0`); el arranque idempotente ejecutado dos veces cierra la corrida huérfana una sola vez (`closed_first=1, closed_second=0`, un solo evento terminal); duración máxima excedida transiciona a `failed/RUN_TIMEOUT` (nunca `interrupted`); heartbeat vencido transiciona a `interrupted/HEARTBEAT_EXPIRED`; y la demo de kill real de arriba. `ruff check .` y `pytest -m "not integration"` (167 passed, incluye 9 pruebas unitarias nuevas en `tests/test_t300_durability_unit.py`) en verde. Verificado manualmente además con `uvicorn` real: ciclo completo query→stream→respuesta, reconexión parcial con `Last-Event-ID`, `DELETE` de corrida terminada (204→404 idempotente) y `DELETE` de corrida activa (cancelación cooperativa observada en 0,54s, muy por debajo de `DELETE_ACTIVE_GRACE_S=5s`).
- [x] **T-301 Capa multi-proveedor LLM.** `app/llm/factory.py`: devuelve el chat model según `LLM_PROVIDER`/`LLM_MODEL` (google | anthropic), con structured output y conteo de tokens unificado (RF-206).
  - ✅ Implementado en `backend/app/llm/factory.py`: `get_chat_model`/`get_chat_model_from_settings` instancian `ChatGoogleGenerativeAI`/`ChatAnthropic` según `llm_provider`/`llm_model`, fallando temprano (antes de red) con `LLMConfigurationError` ante proveedor desconocido o API key ausente/vacía. `get_structured_chat_model` envuelve `with_structured_output` de forma uniforme (mismo código para ambos proveedores, sin ramas). **Decisión de diseño verificada leyendo el código fuente de ambos paquetes** (no asumida): `langchain-google-genai` y `langchain-anthropic` exponen el uso crudo en formatos distintos (`usage_metadata.prompt_token_count`/`candidates_token_count` en Google vs `usage.input_tokens`/`output_tokens` en Anthropic), pero ambos pueblan `AIMessage.usage_metadata` con el mismo esquema normalizado de LangChain — por eso `usage_from_message` lee siempre ese campo normalizado y no necesita ramas por proveedor. `estimate_cost_usd`/`sum_usage` calculan `estimated_cost_usd` con una tabla de precios USD/1M tokens versionada en el módulo (fecha de vigencia 2026-07-09, fuente `ai.google.dev`/`platform.claude.com`, fácil de extender para T-602) y agregan tokens/costo entre pasos de una misma corrida, produciendo valores compatibles con las columnas `llm_provider`/`llm_model`/`input_tokens`/`output_tokens`/`estimated_cost_usd` de `agent_runs`. `ainvoke_chat_model` traduce errores definitivos del SDK (`google.api_core.exceptions.GoogleAPICallError`, `anthropic.APIError`: cuota, auth, modelo inexistente) a `LLMProviderError`, distinguible para que T-303/T-304 lo mapeen a `LLM_PROVIDER_ERROR` (contracts/api-rest.md §6).
  - ✅ Prueba unitaria instancia ambos proveedores (con mock) y el conteo de tokens/costo funciona: `backend/tests/test_llm_factory.py`, 17 casos — instancia real de `ChatGoogleGenerativeAI`/`ChatAnthropic` con API keys falsas (sin red), proveedor desconocido y API key faltante fallan con `LLMConfigurationError`, `with_structured_output` funciona igual para ambos proveedores, `usage_from_message`/`estimate_cost_usd`/`sum_usage` normalizan tokens y calculan costo correctamente, modelo sin tarifa configurada falla con `LLMPricingError`, mezclar proveedores en `sum_usage` falla con `ValueError`. `ruff check .` y `pytest -m "not integration"`: **184 passed** (167 preexistentes + 17 nuevos), en verde.
- [x] **T-302 Implementar las herramientas T1–T5** (las ÚNICAS invocables por el enrutador LLM, `contracts/agent-tools.md`) en `app/tools/`, cada una con esquema Pydantic, sanitización (T4), guardia SoQL estructural (T5) y pruebas unitarias con HTTP mockeado (respx). `resolver_geografia` (T3) depende de T-202 y de que `divipola_entries` esté cargada con el maestro DIVIPOLA; no debe volver al arreglo hardcodeado de v1.0. La guardia usa gramática restringida propia en `app/tools/soql_parser.py`, canonicaliza la consulta, prohíbe `SELECT *`, limita `OFFSET`, valida alias/literales, aplica la guardia PII/elegibilidad antes de Socrata y mantiene lista blanca de funciones. Los nodos deterministas T6 y T7 NO van aquí: se implementan en T-401 y T-403.
  - ✅ Implementado en `backend/app/tools/`: `soql_parser.py` (lexer + parser recursivo-descendente propio para la gramática restringida: `SELECT/WHERE/GROUP BY/HAVING/ORDER BY/LIMIT/OFFSET`, funciones agregadas/escalares de lista blanca, predicados `LIKE`/`IS [NOT] NULL`, canonicalización estable, límites de complejidad, política PII `medium`, más lista negra de defensa en profundidad); `soda_client.py` (cliente SODA genérico con `$select/$where/$group/$having/$order/$limit/$offset`, mismo patrón timeout/reintento que `discovery_client.py`); `catalog_lookup.py` (lecturas/escrituras compartidas de `catalog_datasets`/`catalog_columns`); `buscar_catalogo.py` (T1, envuelve `search_catalog` de T-204 y añade columnas completas + descripción truncada a 300 chars); `perfilar_dataset.py` (T2, `asyncio.gather` para count/null-ratio/distinct-values concurrentes, pista de corte heurística por nombre/tipo de columna, cachea `null_ratio`/`sample_values` — nunca para PII `medium/high/unknown`); `resolver_geografia.py` (T3, similitud trigram sobre `name_normalized` + coincidencia exacta contra `alt_names` normalizados en Python ya que la base no tiene `unaccent`, `like_pattern` con vocales acentuables → `_`); `explorar_valores.py` (T4, sanitiza `'`/`%`/`_`/`\` antes de interpolar en `LIKE ... ESCAPE '\'`); `ejecutar_soql.py` (T5, solo llama a Socrata tras pasar la guardia).
  - ✅ **Decisiones de diseño documentadas** (no evidentes en el contrato, resueltas y anotadas en el propio código): códigos de error de rechazos estructurales (sentencias múltiples, cláusulas/funciones fuera de lista blanca, `SELECT *`, literales inválidos, complejidad, `OFFSET > 5000`, alias no resoluble en `ORDER BY`) unificados bajo `SOQL_FORBIDDEN`, reservando `SOQL_UNKNOWN_COLUMN` solo para columnas referenciadas que no existen en `catalog_columns`; `ORDER BY` solo resuelve contra alias declarado o columna presente en el propio `SELECT` (evita la ambigüedad entre ambos códigos de forma determinista); funciones agregadas prohibidas dentro de `WHERE` (inválido en SQL/SoQL); `rows` de T5 no se trunca a 50 (viaja completo hasta 1000 filas para la futura Evidencia de T6); `llm_view.rows_shown` es solo metadato para quien construya el mensaje hacia el LLM (T-303). Verificado contra Postgres real (no solo mocks): `resolver_geografia` corrido a mano contra los datos DIVIPOLA ya cargados resuelve "Bogotá"/"bogota"/"Bogotá D.C."/"Santafé de Bogotá" → `11001` y "Carmen de Viboral" → `05148`.
  - ✅ Suite de pruebas de herramientas en verde (casos de pruebas.md §2.1, no §4.1): `test_soql_guard.py` (29 casos: aceptación, todos los rechazos estructurales listados en pruebas.md, `SOQL_UNKNOWN_COLUMN` con columnas válidas, complejidad, defensa en profundidad, límites de `LIMIT`/`OFFSET`, canonicalización estable, elegibilidad/PII antes de Socrata, política PII `medium`), `test_divipola_geo.py` (8 casos — **nota de nomenclatura:** pruebas.md sugiere `test_divipola.py`, pero ya existe `tests/integration/test_divipola.py` de T-202 y pytest sin paquetes falla por colisión de nombre de módulo entre ambos; se optó por `test_divipola_geo.py`, dejando además `tests/integration/test_resolver_geografia.py` con los mismos casos contra Postgres real), `test_tools_buscar_catalogo.py` (4), `test_tools_perfilar_dataset.py` (5), `test_tools_explorar_valores.py` (7), `test_tools_ejecutar_soql.py` (8). `ruff check .` y `pytest -m "not integration"`: **245 passed** (184 preexistentes tras mergear T-301 + 61 nuevos), en verde.
- [x] **T-401 Implementar `app/quality/` (nodo determinista T6).** Exactamente según `contracts/validacion-calidad.md`: elegibilidad separada de calidad, 4 dimensiones, pesos, reglas duras, política PII `unknown`/`medium`/`high`, agregación mínima, inferencia de `data_cutoff_at` sobre las filas de la evidencia, fallback `data_updated_at_fallback` con mensaje correcto, salida ampliada (`eligibility_status`, razones, corte, política de filas), mensajes en `messages_es.py`, `validator_version`. Módulo puro, probado en aislamiento — no requiere el grafo.
  - ✅ Implementado en `backend/app/quality/`: `validator.py` (`validate_evidence(EvidenceDraft) -> QualityResult`, único punto de entrada, sin I/O), `cutoff.py` (`infer_data_cutoff` sobre las `rows` de la evidencia — nunca sobre `catalog_datasets`, busca la mejor columna temporal por nombre y confianza, año suelto se ancla a 31-dic), `placeholders.py`/`placeholders.yaml` (detección contextual: patrones genéricos versionados + códigos de codebook provistos por el llamador — nunca dígitos sueltos como `9`/`99` sin codebook, evitando falsos positivos), `messages_es.py` (mensajes en español claro, sin nombres de checks). Reutiliza `app.quality.eligibility.compute_column_eligibility` (T-201) para elegibilidad por columna combinada con las razones ya vigentes a nivel de dataset (el draft las recibe ya calculadas, T6 no vuelve a resolver publicador/PII) y `app.tools.soql_parser.parse_soql` (T-302) para `traceability.query_reproducible`, detectar columnas de uso numérico y el alias `count(*)` que verifica `aggregation_min_count >= 5` por fila en PII `medium`.
  - ✅ **Decisiones de diseño documentadas** (conflicto de precisión detectado en el propio contrato, resuelto por jerarquía documental — no silenciosamente): la tabla de `completeness.null_ratio` declara "Puntos: 60" pero su regla interna usa una escala 0-100 (`≤5%→100`, `≤20%→60`, `≤50%→30`, `>50%→0`); se interpretó esa escala como porcentaje del máximo de 60 (p. ej. `≤20%` → 60% de 60 = 36 puntos), sumado a `completeness.placeholder_values` (0 o 40 directos) — el ejemplo JSON del contrato (`score: 60`) parece una ilustración abreviada que omite el check de placeholders; se siguió el texto normativo por jerarquía (constitution.md > ... > contracts/), documentado en el docstring de `validator.py`. Además, `row_count == 0` fuerza `classification="no_recomendada"` (no solo D1=0): un `score_total` numérico con D1=0 pero las otras tres dimensiones perfectas daría exactamente 75="alta", contradiciendo "no se presenta como hallazgo". Los casos 11/14 del contrato (§5, resolución de alias/vigencia de publicador) ya están cubiertos por `test_publishers_vigencia.py`/`test_add_publishers.py`/`test_publisher_coverage.py` (T-106/T-201): T6 no resuelve publicadores, solo consume `eligibility_status`/`eligibility_reasons` ya calculados.
  - ✅ Los casos de prueba obligatorios del contrato (§5) pasan: `test_quality_validator.py` (31 casos — los 14 de §5, más fronteras explícitas de pruebas.md §2.1: edad 12/13/24/25/48/49 meses, `null_ratio` = 5,0% exacto, `score_total` = 74 vs 75, y verificación de que ningún `warning_user` menciona nombres técnicos de checks), `test_quality_cutoff.py` (6), `test_quality_placeholders.py` (6). `ruff check .` y `pytest -m "not integration"`: **288 passed** (245 preexistentes tras mergear T-301/T-302 + 43 nuevos), en verde.
- [x] **T-403 Módulo de afirmaciones cuantitativas (nodo determinista T7, RF-208).** `app/quality/claims.py` según `contracts/agent-tools.md` T7: DSL JSON de fórmulas, evaluador seguro, formato es-CO de `display_value`, canonicalización de `source_hash` como hash de contenido reproducible entre corridas (sin `evidence_id`, `claim_id` ni `run_id`), definición normativa de “cifra”, persistencia en `quantitative_claims`, y verificador de cifras huérfanas para el texto del sintetizador. Módulo puro, probado en aislamiento.
  - ✅ Implementado en `backend/app/quality/claims.py` (mismo patrón dataclass-puro de `validator.py`, T-401: no es el sobre `{"ok": ...}` de T1-T5 porque T6/T7 son nodos internos del grafo, no herramientas invocables por el LLM): `ClaimSpec`/`EvidenceContext` de entrada, `build_claims` devuelve `ClaimsBuildResult(claims, rejected)`. Evaluador seguro del DSL (`_eval_node`/`_resolve_col`/`_resolve_agg`/`_apply_op`, sin `eval`/`exec`, lista blanca cerrada de nodos `col`/`const`/`agg`/`op`); `format_es_co` (coma decimal, separador de miles); `compute_source_hash` (JSON canónico UTF-8, claves ordenadas, sin espacios, sobre exactamente los 9 campos del contrato); `find_figures`/`find_orphan_figures` (detector de cifras huérfanas con enmascarado previo de UUID/id de dataset/fechas ISO y largas, más heurísticas de código con cero a la izquierda y referencia de sección).
  - ✅ **Decisiones de diseño documentadas** (no evidentes en el contrato, resueltas en el docstring del módulo): (1) resolución de `{"col": "<columna>"}` cuando `source_row_indexes` referencia más de una fila — el propio ejemplo de T7 en el contrato usa dos filas con `col` simple sin agregación; se adoptó que `col` recolecta valores NO NULOS entre las filas referenciadas y exige que exista exactamente UN valor distinto (cubre tanto el caso de una sola fila como el ejemplo del contrato, donde cada columna tiene un único valor no nulo entre las filas); más de un valor distinto exige `agg` explícito, cero valores no nulos se rechaza como "operando nulo" — las agregaciones (`sum/avg/min/max/count`) sí exigen que TODAS las filas referenciadas tengan valor (nunca descartan datos en silencio, Art. I). (2) `ratio` se implementa idéntico a `div` (a/b, división por cero rechazada): el contrato no define una semántica distinta y no se inventa una (Art. III). (3) `compute_source_hash` ordena `source_row_indexes` ascendentemente de forma intrínseca (no depende de que el llamador lo haga): bajo este DSL el orden nunca afecta `raw_value` (`col` es búsqueda de valor único, las agregaciones son conmutativas), por lo que reordenar los índices de entrada no es "semánticamente relevante" y produce el mismo hash, mientras que cambiar el CONTENIDO de las filas referenciadas o el conjunto de índices sí lo cambia (pruebas.md §2.1).
  - ✅ Casos de pruebas.md §2.1 (claims) en verde en `backend/tests/test_claims.py` (29 casos): claim `direct` con formato es-CO, claim `derived` con división/porcentaje reproduciendo el ejemplo literal del contrato (8.3721 con rounding=1 ⇒ "8,4 %"), el propio ejemplo de dos filas de T7 con `col` simple, `sum()`/`avg()`, los 4 rechazos exigidos (columna inexistente, operando no numérico, operando nulo, división por cero) más operación DSL/agregación no permitida y columna no declarada; detector de cifras huérfanas (definición normativa completa: enteros, decimales coma/punto, porcentajes, monedas, miles, rangos, años analíticos; exclusión de UUID, id de dataset, fecha completa, DIVIPOLA, número de sección) en caso positivo y negativo; estabilidad de `source_hash` entre corridas independientes con el mismo contenido (simulando UUIDs de corrida/evidencia/claim distintos, que `compute_source_hash` ni siquiera recibe como parámetro), cambio de fórmula/fila/`raw_value`/unidad/redondeo cambia el hash, y reordenar `source_row_indexes` no lo cambia (orden no semánticamente relevante bajo este DSL) mientras que referenciar un conjunto distinto de índices sí. `ruff check .` y `pytest -m "not integration"`: **317 passed** (288 preexistentes tras mergear T-401 + 29 nuevos), en verde.
- [x] **T-303 Integrar el grafo del agente.** `app/agent/graph.py` según plan.md §1: planificador → enrutador → herramientas T1–T5 (T-302) → nodo validador T6 (T-401) → nodo constructor de afirmaciones T7 (T-403) → sintetizador restringido (solo `display_value` de claims; cifras huérfanas bloquean y fuerzan re-síntesis, máx. 2 intentos, luego `failed`), con presupuestos del contrato de tools, checkpointer PostgreSQL, heartbeat, eventos numerados en `agent_run_events` y persistencia de `agent_runs`/`agent_steps` (RF-703, RF-209), sobre la base del PoC de T-300. Prompts en `app/agent/prompts/`, versionados.
  - ✅ Implementado en `backend/app/agent/graph.py` (`build_graph`, `GraphDependencies`, `PlannerOutput`/`RouterOutput`/`SynthesisOutput`), `backend/app/agent/persistence.py` (persistencia transaccional de pasos/eventos/evidencia+calidad/claims), prompts versionados en `backend/app/agent/prompts/{planner,router,synthesizer}_v1.md`, CLI real en `backend/scripts/run_agent.py`. El PoC de T-300 queda aislado (`start_toy_run_task`/`build_toy_graph`), sin eliminarse (sus pruebas de durabilidad siguen vigentes); `start_run_task` pasa a significar el grafo real.
  - ✅ **Criterio literal verificado con ejecución real** (Postgres + Gemini + Socrata reales, sin mocks): la pregunta *"¿Cuál fue el promedio de deserción escolar en el departamento de Antioquia entre 2018 y 2022, según el Ministerio de Educación?"* contra el dataset real `ji8i-4anb` (MEN) produjo `status=completed`, `claims[]` con 2 elementos aceptados (`build_claims`/T7), `quality.classification=alta`/`score_total=100`, en 7 pasos (≤10), con `synthesis_attempts=2` — el reintento por cifras huérfanas (`find_orphan_figures`) se activó realmente sobre un intento con cifra huérfana y lo corrigió antes de aceptar la respuesta final, demostrando RNF-003 en runtime, no solo en pruebas guionadas. Matar el proceso a mitad de corrida (T-300, ya verificado) deja `interrupted/RUN_INTERRUPTED` coherente al reiniciar.
  - ✅ **Bloqueo original y su resolución (research.md §13):** la primera ejecución real con la pregunta original de ESC-02 ("Oriente antioqueño") terminó honestamente en `no_evidence` — correcto per Art. I (nunca se relajó PII ni se fabricó una dimensión territorial inexistente), pero reveló, al ejecutar contra datos reales, cuatro problemas invisibles en pruebas con mocks: (1) el clasificador PII casi nunca resolvía `low` (0 de 8.418 datasets `eligible` en el catálogo real) por un bug de anclas en `pii_classifier.py`; (2) `httpx.AsyncClient()` sin `base_url` en `runner.py` hacía que TODA llamada real a Socrata fallara instantáneamente, indistinguible de `SOCRATA_TIMEOUT`; (3) `_count_alias` en `validator.py` exigía alias explícito en `count(*)`, bloqueando evidencia PII `medium` genuinamente agregada; (4) `ClaimSpecPayload.claim_type` como `str` libre permitía que el LLM alucinara valores inválidos. Las cuatro correcciones están documentadas con justificación en research.md §13 y cubiertas con pruebas de regresión (`test_pii_classifier.py`, `test_quality_validator.py`). La pregunta original sobre "Oriente antioqueño" queda como seguimiento separado (falta fixture de subregiones DIVIPOLA), no bloquea T-303.
  - ✅ `ruff check .` y `pytest -m "not integration"`: 331 passed (330 + 1 fallo preexistente de aislamiento de entorno local ajeno a estos cambios, ver research.md §13); pruebas nuevas: `test_agent_graph.py`, `test_t303_agent_graph.py` (integración), regresiones en `test_pii_classifier.py`/`test_quality_validator.py`. CI Backend/Frontend en verde (PR #22).
- [ ] **T-304 Endpoints del agente.** `POST /v2/agent/query` (endpoint público siempre `retention_class=user`, emite `run_access_token` una sola vez y guarda solo el hash), `GET /v2/agent/stream/{run_id}` (SSE con `id:` de secuencia, `Last-Event-ID`, autorización Bearer y evento terminal con `status`), `GET /v2/agent/runs/{run_id}` (Bearer, esquemas para `running`, `completed`, `no_evidence`, `interrupted`, `failed`) y `DELETE /v2/agent/runs/{run_id}` (borrado RF-803 + `adelete_thread`, incluida corrida activa), según el contrato REST; rate limiting global por proceso; cierre de corridas huérfanas y timeout según plan.md §11. Si un acceso encuentra corrida vencida, ejecuta borrado oportunista y responde `404 RUN_NOT_FOUND`.
  - ✅ `curl -N` con Bearer muestra eventos en vivo y termina con `answer`; sin token → 401; token `user` expira según retención; reconexión con `Last-Event-ID` no pierde ni duplica; `GET` valida los cinco estados; `DELETE` borra checkpoints y el `GET` posterior da 404.
- [ ] **T-306 Job de retención y purga.** Implementar barrido RF-804 como servicio/CLI idempotente del backend y endpoint admin `POST /v2/admin/retention/run`: corridas `user`, corridas `eval`, `technical_metrics` vencidas, borrado oportunista compartido con T-304, copia atómica de métricas con `source_run_hash = sha256(run_id + RETENTION_HASH_SALT)`, snapshot eval, `adelete_thread(run_id)`, borrado físico y reintento idempotente ante fallos. Preparar ejecución lógica única ante concurrencia con advisory lock transaccional PostgreSQL `pg_try_advisory_xact_lock(20260707, 804)`; si otro barrido ya tiene el lock, responder/registrar `already_running` sin efectos. Documentar la programación externa cada 6 horas mediante scheduler del proveedor o workflow programado autenticado con `ADMIN_TOKEN`; SLA de borrado físico máximo 24 horas desde vencimiento lógico.
  - ✅ Pruebas con reloj simulado cubren `user`, `eval`, `technical_metrics`, concurrencia DELETE/job, dos barridos simultáneos, reintento tras fallo de checkpoints e idempotencia sin duplicar métricas; quickstart permite ejecución manual local.
- [ ] **T-305 Casos de honestidad.** Probar manualmente ESC-03 (pregunta sin respuesta en el catálogo): el agente debe producir `no_evidence_report` sin cifras inventadas.
  - ✅ 3 preguntas negativas manuales pasan; se registran como semillas del golden set (T-601).

## FASE 4 — Verificación integrada de las capas deterministas (RF-401…404, RF-208)

*Nota de reordenamiento: T-401 y T-403 se ejecutan dentro de la Fase 3 (antes de T-303) porque el grafo los integra; se conservan sus IDs. Esta fase verifica la integración de punta a punta con datos reales.*

- [ ] **T-402 Verificación integrada de calidad y claims.** Con el grafo completo (T-303): el nodo `quality_validator` corre SIEMPRE tras cada `ejecutar_soql` (imposible saltarlo); el objeto `quality` viaja en cada Evidencia; el sintetizador ajusta la narrativa según la clasificación (`baja` ⇒ mencionar limitación; `no_recomendada` ⇒ evidencia no elegible como sustento); los claims heredan las advertencias de su evidencia fuente.
  - ✅ ESC-05 reproducido con un dataset viejo real: advertencia visible en la respuesta; una respuesta real de ESC-01 muestra `claims[]` y cero cifras huérfanas; un dataset de publicador no oficial es rechazado con explicación clara.

## FASE 5 — Frontend v2 (RF-101…104, RF-501…503, RNF-007/008, Art. V)

- [ ] **T-501 Sistema de diseño azul.** Aplicar los tokens de plan.md §7 en `tailwind.config.js`; refactorizar componentes a la paleta; eliminar colores fuera de paleta salvo semánticos. Objetivo de accesibilidad: WCAG 2.2 nivel AA.
  - ✅ Revisión visual página por página + captura de pantalla en el PR; Lighthouse/axe accesibilidad ≥ 95 como puerta parcial (RNF-007) — la conformidad se completa con la revisión manual de T-505.
- [ ] **T-502 Conectar el copiloto al backend v2.** Reemplazar el fetch a `/api/consultar_v2` por el flujo `query` → SSE consumido con fetch-stream (el token Bearer viaja por encabezado; `EventSource` nativo no sirve — plan.md §11): pasos del agente renderizados en vivo como línea de tiempo en lenguaje claro, con detalle técnico expandible (ESC-04); reconexión automática con `Last-Event-ID`; el `run_access_token` se guarda junto al historial local de la sesión. Aviso de consentimiento ANTES de la primera investigación (RF-802: qué se guarda, para qué, por cuánto tiempo, cómo borrarlo) y acción "borrar esta investigación" que llama al `DELETE` (RF-803). Historial de sesión con re-ejecución/refinado de consultas anteriores (RF-502); todos los textos en español (RNF-012). Conexión DIRECTA del navegador al backend vía `NEXT_PUBLIC_BACKEND_URL` (sin proxy en Next.js); configurar `CORSMiddleware` en FastAPI restringido a los orígenes del frontend (plan.md §11).
  - ✅ RNF-008 medido: primer feedback < 500 ms, primer paso < 2 s; cortar la red a mitad de corrida y recuperarla continúa el stream sin duplicar pasos; el aviso de consentimiento aparece antes de la primera investigación.
- [ ] **T-503 Tarjetas de evidencia v2.** Mostrar tabla + badge de calidad + advertencias + narrativa citable + botón "insertar en sección" y descarga CSV (RF-501). Gráfica simple cuando `chart_suggestion` no es null (RF-503). Flujo de confirmación para `no_recomendada` (RF-404).
  - ✅ ESC-01 completo de punta a punta en local.
- [ ] **T-504 Citas y persistencia del documento.** Extensión Tiptap "cita de evidencia" que fija el objeto `citation` al fragmento insertado (RF-103); autoguardado en localStorage ≤ 5 s (RF-102); exportación a `.docx` con citas al pie (RF-102/103).
  - ✅ ESC-08: cerrar y reabrir el navegador conserva documento y citas; el .docx exportado muestra las citas.
- [ ] **T-505 Pruebas E2E, accesibilidad y RNF-012.** Playwright: ESC-01 feliz, ESC-03 sin evidencia, navegación por teclado del flujo principal. Además, revisión MANUAL de accesibilidad WCAG 2.2 AA según la lista de pruebas.md §5 (teclado, orden y visibilidad de foco, lector de pantalla, zoom y reflujo a 320 px, anuncios de contenido SSE con `aria-live` sin saturar, reducción de movimiento, no depender solo del color). Revisar RNF-012 con checklist manual: etiquetas del frontend, mensajes de error, `message_user`, advertencias de calidad, estados/mensajes SSE, consentimiento, retención/borrado, "sin evidencia" y ausencia de jerga técnica sin explicación. Las herramientas automáticas son auxiliares (RNF-007).
  - ✅ Suite E2E en CI (con backend mockeado por fixtures) + acta de revisión manual WCAG y acta RNF-012 archivadas en `docs/`.

## FASE 6 — Evaluación técnica OE3 (RF-601…603)

*Objetivo: la batería que produce las métricas del working paper (Producto 3 de la propuesta).*

- [ ] **T-601 🧑 Construir el golden set v1 (ESC-06).** 50 casos en `backend/eval/golden/golden-v1.yaml`: ~40 positivos (pregunta + `expected_dataset_ids` + hechos verificados a mano contra el portal) y ~10 negativos (RNF-005). **Requiere tu criterio de politólogo:** las preguntas deben representar necesidades reales de funcionarios (usa los hallazgos del OE1 cuando existan). Documentar cada caso en `notes`.
  - ✅ Revisión cruzada: cada `expected_dataset_id` verificado manualmente en datos.gov.co.
- [ ] **T-602 Runner de evaluación (ESC-06).** CLI `python -m eval.run --suite golden-v1 --provider google --model gemini-2.5-flash --seed <int>`: ejecuta cada caso contra el grafo real con semilla y `config_snapshot` persistidos, calcula recall@10, éxito, `socrata_success_rate`, **groundedness por verificación de claims** (cada cifra del texto tiene claim; operandos existen en las filas fuente; la fórmula DSL re-ejecutada reproduce `raw_value`; el redondeo produce `display_value`; el texto coincide con `display_value` — la coincidencia literal por regex queda solo como detector auxiliar de cifras huérfanas, pruebas.md §4.2), fabricaciones en negativos, latencias simples/multi-paso y costo; persiste `eval_runs`/`eval_case_results` y emite reporte Markdown en `backend/eval/reports/`.
  - ✅ Corrida completa contra Gemini termina y el reporte muestra las métricas de RNF-001…005.
- [ ] **T-603 🧑 Corridas comparativas (ESC-06).** Ejecutar la batería con ≥ 2 configuraciones (p. ej. Gemini 2.5 Flash vs Claude vs Gemini Pro). **Tú decides** las configuraciones finales según presupuesto. Analizar en `notebooks/02_analisis_eval.ipynb`.
  - ✅ Tabla comparativa lista para el working paper; configuración ganadora fijada como default.
- [ ] **T-604 Puertas RNF-002.** Configurar tres niveles: cada PR corre pruebas deterministas con LLM guionado y smoke reducido sin LLM real; semanalmente corre golden set con LLM real y abre issue/alerta si hay regresión o `success_rate < 80%`; antes de release corre golden completo y bloquea el release si `success_rate < 80%`.
  - ✅ Workflows visibles en Actions: PR determinista bloqueante, semanal con issue/alerta, y pre-release bloqueante.

## FASE 7 — Endurecimiento y despliegue

- [ ] **T-701 🧑 Desplegar el backend.** Railway/Render: servicio desde `backend/` con las variables de `.env.example` como secrets (usa los valores reales de la Fase 0). Conectar dominio `api.cuestiondedatos.com` (añadir CNAME en tu DNS). Crear el cron externo de retención cada 6 horas, registrar `ADMIN_TOKEN` y `RETENTION_HASH_SALT` como secretos, verificar la primera ejecución de `POST /v2/admin/retention/run` y revisar fallos.
  - ✅ `https://api.cuestiondedatos.com/v2/health` responde `ok` desde internet; primera ejecución de retención registrada en logs/métricas.
- [ ] **T-702 🧑 Desplegar el frontend.** En Vercel, apuntar el proyecto existente a `frontend/` de la rama `v2` con `NEXT_PUBLIC_BACKEND_URL=https://api.cuestiondedatos.com` como variable de entorno; verificar que el CORS del backend incluye el dominio del preview de Vercel durante las pruebas. Probar en preview antes de promover a producción (cuestiondedatos.com).
  - ✅ ESC-01 funciona en el dominio de preview y luego en producción.
- [ ] **T-703 🧑 Monitoreo.** UptimeRobot sobre `/v2/health` cada 5 min con alerta a tu correo (RNF-006); revisar `GET /v2/admin/metrics` tras la primera semana para verificar RNF-001/009 con tráfico real.
  - ✅ Monitor activo; primer reporte semanal de métricas archivado en `docs/`.
- [ ] **T-704 Retirar el endpoint v1.** Eliminar `pages/api/consultar_v2.js`, `utils/systemPrompt.js` y `utils/maestro_divipola.js` (ya reemplazados). Actualizar README.md raíz con la arquitectura v2.0 y CHANGELOG a `2.0.0`.
  - ✅ Sin referencias muertas (`grep` de los archivos borrados no encuentra imports).
- [ ] **T-705 🧑 Cierre documental.** Verificar los criterios de cierre de plan.md §10; capturar evidencia (reportes, métricas, screenshots) en `docs/`; preparar la actualización del registro en herramientas.datos.gov.co y del sitio divulgativo/producto público con la documentación de los nuevos componentes, sin convertir OE1/OE4 en funcionalidad de software.
  - ✅ Checklist de plan.md §10 completo y archivado.

---

## Dependencias entre fases

```
F0 ──▶ F1 ──▶ F2 ──▶ F3 ──▶ F4 ──▶ F5 ──▶ F7
                      └────────────▶ F6 ──▶ F7   (F6 puede iniciar al terminar F3/F4)
```

**Ruta backend ejecutable hasta T-403:** T-105 → T-102 → T-103 → T-104A → T-106 → T-201 → T-201A → T-202 → T-205 → T-104B → T-203 → T-204 → T-300 → T-301 → T-302 → T-401 → T-403. Para cerrar los endpoints y la retención después de T-403: T-303 → T-304 → T-306 → T-305. T-102 y T-104A dependen de la base local de T-105; T-205 depende de T-202 para consultas territoriales; T-104B depende de la decisión humana de T-205.

## Resumen de acciones humanas (🧑) para planear tu agenda

| Tarea | Qué haces tú | Tiempo estimado |
|---|---|---|
| T-001/002/003 | Instalar Docker Desktop y crear cuentas/claves (Anthropic; Supabase y Railway solo para despliegue) | 1–2 h |
| T-205 | Decidir modelo de embeddings y dimensión con el benchmark (research.md §1) | 2–3 h |
| T-601 | Construir y verificar el golden set (criterio experto) | 2–3 días |
| T-603 | Decidir configuraciones comparativas y analizar | 1 día |
| T-701/702/703 | Despliegues, DNS y monitoreo | 2–4 h |
| T-705 | Cierre documental y registro MinTIC | 1 día |
